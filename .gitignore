# Sekrety — nigdy w repozytorium (granica bezwzględna, sekcja 2 promptu startowego)
.env
.env.*
!.env.example

# Zależności i artefakty budowania
node_modules/
.next/
# L-OPS-04 (WWW/089): katalog budowania pochodzi ze zmiennej WWW_DIST,
# domyślnie `.next`. Pomiar buduje do `.next-pomiar`, żeby nie kłaść
# serwera deweloperskiego właściciela — wzorzec `.next-*` obejmuje
# każdy przyszły katalog pomiarowy, nie tylko ten jeden.
.next-*/
out/
coverage/
playwright-report/
test-results/
.vercel/
.lighthouseci/
next-env.d.ts
# Cache przyrostowy `tsc --noEmit` — powstaje przy każdej kontroli typów.
tsconfig.tsbuildinfo

# Lokalne ustawienia Claude Code (per maszyna, poza repo)
.claude/settings.local.json

# Systemowe
.DS_Store
