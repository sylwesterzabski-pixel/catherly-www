# Rejestr warunków powrotu (treści zdjęte lub wstrzymane z powodu braku pokrycia)

Zasada: ADR-018 — brak dowodu = brak obietnicy. Każda pozycja wraca
WYŁĄCZNIE po spełnieniu warunku (dowód wykonaniem, nie przekonanie).
Data założenia: 2026-08-09 (po Z1–Z4).

| # | Treść | Gdzie | Warunek powrotu |
|---|---|---|---|
| 1 | „Rozliczenia" w H1 i podtytule hero | pl/en/de naglowek | Działające rozliczenia end-to-end (Stripe aktywny) + aktualizacja inwentarza przed premierą |
| 2 | Pytanie o fakturę VAT w FAQ cennika | pl/en/de cennik §5 | Testowy zakup z OTRZYMANĄ fakturą (dowód dokumentem) + konfiguracja dashboardu Stripe (dane sprzedawcy; decyzja o Stripe Tax — domyślnie wyłączony) |
| 3 | Trial 14 dni (kod: trial_period_days:14, billing-service.ts:213–227) | przyszły argument cennika | Stripe działający end-to-end; decyzja właściciela o komunikacji trialu |
| 4 | „20 GB przestrzeni na pliki" (karta Pro) + wiersz „Przestrzeń" w tabeli | pl/en/de cennik §3–4 | Klucz Storage aktywny + wykonany test uploadu. Panel: wraca bez ponownego panelu treści, tylko ze zliczeniem znaków |
| 5 | Wiersz „Wywołania AI 100/500/∞" w tabeli | pl/en/de cennik §4 | Aktywny klucz Anthropic (AI przestaje zwracać teksty zapasowe). Język kierunku o asystencie AI w opisach — dozwolony już teraz |
| 6 | Wiersz „Platformy social 2/5/∞" w tabeli | pl/en/de cennik §4 | Zgody platform + działające łączenie kont (dowód połączeniem) |
| 7 | „RODO"/„GDPR"/„DSGVO" jako potwierdzenie | pl/en/de naglowek | Weryfikacja procesów (prawa osób, powierzenie, polityka); wraca jako fakt z mechanizmem na /bezpieczenstwo |
| 8 | TLS / szyfrowanie at-rest platformy | /bezpieczenstwo | Odczyt dashboardu Supabase (raport Z4: brak śladu w repo; twierdzenie w UI aplikacji to stała tekstowa) |
| 9 | Szyfrowanie pól aplikacyjnych (AES-256-GCM: TOTP, tokeny social — FAKT z kodu) | /bezpieczenstwo | Wchodzi przy budowie /bezpieczenstwo z precyzyjnym zakresem („wybrane pola"), nie jako potwierdzenie ≤45 zn (panel F5) |
| 10 | Import wyciągu FL | cała strona | Storage aktywny + ekrany niepuste; bramka GROWTH pozostaje |
| 11 | Fraza Pulsu poza kartą Growth | wszystkie treści | Zawsze pełna forma „W planie Growth…" (warunek panelu cennika, aktualny) |
| 12 | Pozostałe bramki GROWTH z Z1 (Benchmarki, Liga, Hive Coach, Win Reel, Interactive, Stacks, Tag, raporty struktury/sponsora) | ewentualne przyszłe treści | Nieobecne w narracji strony; każde wejście = nowa obietnica → tabela obietnic + panel + decyzja właściciela |

| 13 | robots: noindex,nofollow (layout www — stan przedpremierowy, odnotowany przez adwersarza) | src/app/[locale]/…/layout.tsx | Wyłączyć przy publikacji (Faza 7) — pozycja checklisty premiery |
| 14 | „bez podawania powodu" (rezygnacja) | zamknięcie pkt 25 (odrzucone przez panel) | Weryfikacja przepływu anulowania w aplikacji (czy nie wymusza powodu — dowód) + wpis do tabeli obietnic |
| 15 | Granice e-mail modułów pozyskiwania (formularz/kalendarz/zadania: „nie wyśle e-maila/nie przychodzą e-mailem") | /funkcje/pozyskiwanie | Aktywacja Resend → rewizja trzech granic (panel Etapu B F4, 2026-08-12) |
| 16 | Granica jednokierunkowości subskrypcji kalendarza | /funkcje/pozyskiwanie | Integracja kalendarza dwustronna (dziś SZKIELET) → rewizja granicy modułu 3 |
| 17 | Cel linku kodu QR polecającego (teksty zakładają adres polecający) | /funkcje/pozyskiwanie | Weryfikacja przy zleceniu zrzutów Z9+ (dokąd QR prowadzi w aplikacji) |
| 18 | 7 nazw opisowych modułów bez pozycji słownika (kalendarz, subskrypcja, vCard, QR, program poleceń, zadania, plany rozmów) | /funkcje/* | Potwierdzenie zgodności z i18n aplikacji przy najbliższym zleceniu Z (decyzja właściciela 2026-08-12) |
| 19 | Granica importu (moduł 4: „importu hurtowego nie ma”) | /funkcje/pozyskiwanie + obawy Para 2 | Pojawienie się importu w aplikacji (strona app „Formularze & Import” — potwierdzenie zakresu przy najbliższym Z) → rewizja granicy i Pary 2 (adwersarz B F4) |
| ~~20~~ | **PRZENIESIONE 2026-08-21 → `rejestr-decyzji-stojacych.md` poz. D-1.** Nie był to warunek powrotu, tylko decyzja obowiązująca; nie było czego przywracać. Numer zostaje pusty — przenumerowanie unieważniłoby odesłania w tabeli obietnic, plikach treści i komentarzach kodu | — | — |
| 21 | Granica „nie wygeneruje szablonu” | /funkcje/tresci | Aktywacja klucza Anthropic → rewizja |
| 22 | Granica „zasięgów nie pokaże” | /funkcje/tresci | Statystyki publikacji po zgodach platform → rewizja |
| 23 | Widok liderki w Pierwszych 90 Dniach (W2 odrzucony — brak dowodu) | /funkcje/zespol | Odczyt first90 przy najbliższym Z |
| 24 | „Przesuwasz post” + nazwy poza słownikiem (kalendarz publikacji, tablica postów) | /funkcje/tresci | Weryfikacja przy Z9 / najbliższym Z |

## Pozycje techniczne i procesowe (dopisane 2026-08-14)

Ta sama zasada, inny przedmiot: nie treść zdjęta z powodu braku
pokrycia, tylko rzecz świadomie odłożona z zapisanym warunkiem
powrotu. Trzymane tutaj, a nie w rejestrze korekt treści, bo tamten
jest ściśle o treści podstron funkcji.

| # | Pozycja | Gdzie | Warunek powrotu |
|---|---|---|---|
| T1 | Selektywne ładowanie przestrzeni komunikatów per strona (`next-intl` serializuje KOMPLET komunikatów do ładunku każdej strony) | `src/i18n/`, `src/app/[locale]/**` | Blok designu — decyzja właściciela 2026-08-14 |
| T2 | Audyt nieodwracalnych (ADR-018 pkt 4) — bramka `nieodwracalne` PLANOWO czerwona | `docs/audyt/` | Faza 6 — audyt całościowy przedpremierowy, nie częściowy po etapie (decyzja właściciela 2026-08-14) |
| ~~T3~~ **ZAMKNIĘTE 2026-08-16** | Pomiar wydajności na preview Vercel + mediana jako werdykt (kod gotowy, tryb NIEAKTYWNY) | `lighthouserc.cjs`, `.github/workflows/bramki.yml`, `scripts/reprezentant.mjs`, `scripts/werdykt-po-lcp.mjs`, `docs/faza-4/bramka-na-preview.md` | **Warunek spełniony 2026-08-16, bramka DOMKNIĘTA decyzją właściciela** — cztery człony z dowodami: (1) **transport** — preview odblokowane przez Protection Bypass for Automation, sekret w GitHubie, wartość nigdzie nie drukowana; (2) **prowieniencja** — strażnik `bramka:preview` potwierdza HTTP 200 i 3/3 znaczniki wydania, a rozgrzewka sprawdza zgodność wydania na **każdej** trasie (przebieg 31957994362: „7/7 tras, 116 pobrań zasobów"); (3) **reguła werdyktu** — **NIE `mediana`** z tego wiersza: chimera zszywająca metryki z różnych przebiegów została zabita świadomie, werdykt zapada na JEDNYM prawdziwym przebiegu o medianowym LCP (§4b dokumentu bramki); (4) **rozgrzewka** przed pomiarem. Wynik: 7/7 tras zielonych na `26c38f2`. **Pozostaje otwarty rozrzut pomiaru → T10** |
| T4 | Obietnica „H1 ≤ 3 linie" — część desktopowa (768 px wzwyż) naprawiona przez ADR-029, ale potwierdzona tylko pomiarem lokalnym; **poniżej 768 px nadal nieprawdziwa** (DE 4–5 linii) i niepilnowana | `src/components/Hero.module.css`, `e2e/hero.spec.ts`, `docs/adr/029-prog-i-proporcje-hero.md` | (1) zielony pełny zestaw e2e jako dowód naprawy desktopu, (2) rozstrzygnięcie właściciela, czy obietnica ma obejmować < 768 px — jeśli tak, `clamp` względem kolumny zamiast okna + strażnik na 390 px |
| ~~T5~~ **ZAMKNIĘTE 2026-08-15** | Strony zbudowane w Fazie 4 (cztery podstrony filarów, /dla-kogo) są w ADR-014 wymienione POZA zakresem startu; nie znaleziono ADR-a uchylającego | `docs/adr/014-zakres-zamrozony-iteracji-1.md`, `docs/PLAN.md` §11 | **Rozstrzygnięte przez właściciela 2026-08-15: pięć adresów WCHODZI do zakresu startu** (ADR-014, doprecyzowanie 2026-08-15 II). Uzasadnienie: strony istnieją, są opublikowane ×3 języki, mają testy i przechodzą bramki — rozbieżność była luką formalną, nie sporem o zakres. Forma: doprecyzowanie, nie ADR uchylający; właściciel nazwał to luką formalną świadomie. Pozostałe pozycje listy „poza zakresem startu" **nietknięte** |
| ~~T6~~ **ZAMKNIĘTE 2026-08-16** | `bramka:liczby` **nie widziała warstwy `messages`** — skanowała wyłącznie `.tsx`/`.jsx` i tylko tekst z **cyfrą** poza klamrami, więc każdy ciąg renderowany przez `{t(...)}` był poza bramką, a liczebniki słowne były niewidoczne wszędzie. Karta tonu pkt 5 („bez wyjątku (bramka)") była wobec tego stanu nieprawdziwa | `scripts/lint-liczby.mjs`, `content/liczby-w-tresci.json`, `content/karta-tonu.md:61`, `docs/faza-4/bramka-liczby-warstwa-tresci.md` | **Wykonane 2026-08-16** (zlecenie właściciela po pushu 083d9f0): przebieg 2 bramki czyta `src/i18n/messages/*.json` — cyfry **i** liczebniki słowne 2–1000 ×3 języki; rozstrzygnięcia per ciąg w `content/liczby-w-tresci.json` (kategoria + pokrycie + komplet liczb, zmiana liczby zapala czerwień). **Inwentarz wyszedł na 16 kluczy, nie 14** — szacunek z 2026-08-15 był o dwa za niski (rozbiór: dokument roboczy §2). Karta tonu pkt 5 przepisana: mówi, co bramka pilnuje, i nazywa dwa miejsca, których NIE pilnuje (rodzina „jeden/one/ein", liczebniki porządkowe). Dowody: 11 mutacji, dokument roboczy §5 |
| T7 | **Zdania z datą ważności — brak rejestru i brak mechanizmu.** W serwisie stoją zdania prawdziwe wyłącznie DO PREMIERY, a w repozytorium nie ma ani bramki, ani adnotacji, ani listy, która by je znała. W dniu premiery stają się fałszem o własnym serwisie i nic tego nie zapali. Znane dziś: `StronaLogowania.tresc` („Logowanie będzie dostępne przy premierze aplikacji." ×3 języki), `Stopka.wkrotce` przy czterech dokumentach prawnych i przy kontakcie (`(wkrótce)` / `(coming soon)` / `(folgt in Kürze)`) | `src/i18n/messages/{pl,en,de}.json`, `src/components/Stopka.tsx:74-89`, `src/app/[locale]/login/page.tsx` | **Decyzja właściciela 2026-08-15: pozycja na checkliście premiery, BEZ budowy mechanizmu.** Zakres: inwentarz wszystkich zdań przedpremierowych ×3 języki z decyzją per zdanie (usunąć / przepisać / aktywować link). Wykonanie: przed premierą, nie teraz. Świadomie nie budujemy bramki wygasania — dług ma być **zapisany**, nie zapamiętany |
| T8 | **`/pomoc` wycofana z zakresu startu** (ADR-014, doprecyzowanie 2026-08-15 III) po trzech kompletach werdyktów adwersaryjnych NIE PRZECHODZI. Strona nie istnieje: brak w `ISTNIEJACE_SCIEZKI`, brak w mapie stopki, brak pliku | `docs/adr/014-zakres-zamrozony-iteracji-1.md`, `docs/faza-4/etap-e-pomoc-decyzje.md`, `src/i18n/sciezki.ts` | **Powrót po premierze, warunek potrójny (właściciel 2026-08-15):** (1) treść **z odczytu** — realne pytania użytkowniczek zamiast domysłów redakcji, (2) **istniejący kanał kontaktu**, nie stan „(wkrótce)", (3) **onboarding przetestowany**, nie zapowiedziany. Przed spełnieniem wszystkich trzech strona nie ma czym być — zamknięcie E-1 |
| T9 | **Wskaźnik zagnieżdżenia w mapie stopki — wariant z kreską odłożony.** Sędzia panelu chciał pionowej kreski wzmacniającej hierarchię czterech filarów pod `/funkcje`. Wdrożone **bez kreski**: `--kolor-rola-kreska` daje na powierzchni stopki **1,34:1** przy progu 3:1 (WCAG 1.4.11), więc kreska mogłaby wystąpić wyłącznie jako dekoracja — a dekoracja obok wcięcia myli co do tego, co **niesie** informację. Hierarchię trzyma dziś wcięcie (`.stopka li ul`) plus drzewo DOM (`<ul>` wewnątrz `<li>` rodzica) | `src/components/Stopka.module.css`, `src/styles/generated/tokeny.css:67`, `docs/faza-4/etap-e-pomoc-decyzje.md` §WYKONANIE | **Decyzja właściciela 2026-08-16: BEZ kreski; wariant `--kolor-rola-tekst-drugorzedny` (7,07:1) → do przeglądu przy bloku designu.** Nie jest to dług techniczny do spłaty, tylko decyzja **wizualna** właściciela odłożona do miejsca, gdzie ogląda się żywy materiał, a nie liczbę kontrastu w oderwaniu. Warunek powrotu: blok designu — łącznie z T1 i mobilną częścią T4 |
| T10 | **Rozrzut pomiaru szerszy niż zapas do progu — na 7 z 7 tras.** Przebieg 31957994362 (`26c38f2`, 7 tras × 5 przebiegów): zapasy reprezentantów **+239…+403 ms**, a rozrzut LCP w obrębie jednej trasy **328…1441 ms** (`/funkcje/tresci`: 947 · 1559 · **2388** · 1397 · 957 ms). Reguła werdyktu zdejmuje wpływ pojedynczego wyskoku na **wybór** reprezentanta, ale rozrzutu **nie zmniejsza**: ten sam kod zmierzony ponownie może wskazać inny przebieg, a runner potrafi zmienić `benchmarkIndex` o połowę między porami dnia (2161–2415 rano, 3194–3368 wieczorem 2026-08-16). Zieleń stoi więc na medianie, nie na zapasie odpornym na runnera | `lighthouserc.cjs` (`numberOfRuns: 5`), `scripts/reprezentant.mjs`, `docs/faza-4/bramka-na-preview.md` §4b | **Decyzja właściciela 2026-08-16: ZAMROŻONE ŚWIADOMIE.** Warunek powrotu: **bramka przerzuci się NA MEDIANIE, a nie na wyskoku** — czerwień zapadnie na przebiegu o medianowym LCP. Sam szeroki rozrzut ani wyskok ponad próg w przebiegu **niewybranym** warunku nie spełniają: to jest dokładnie ten hałas, który reguła werdyktu ma pomijać |
| T11 | **`reuseExistingServer: !process.env.CI` w `playwright.config.ts` to mina.** Pełny pakiet uruchomiony lokalnie podpina się do CZYJEGOKOLWIEK procesu na porcie 3000 — także do `next dev` z innego okna, także zepsutego. 2026-08-17 osierocony `next dev` (PID 10467, start 18:59:45) serwował 500 na wszystkich trasach i nadpisywał `.next/` w tle; testy podpięłyby się do niego bez słowa, a asercje na podciągach przechodzą na stronie błędu. Czerwień z takiego przebiegu nie mówi nic o kodzie — i zieleń też nie. | `playwright.config.ts` | Blok przeglądu bramek — decyzja właściciela 2026-08-17: **odnotować, nie naprawiać teraz**. Kierunki: potwierdzanie tożsamości serwera przed przebiegiem (znacznik w odpowiedzi) albo osobny port testowy. |
| T12 | **`max-width: 24ch` na H1 hero — defekt produkcyjny, nie artefakt eksperymentu.** `ch` to szerokość cyfry zero, więc ta sama liczba wyznacza inną kolumnę w każdym kroju: przy 1440 px SF Pro daje **755,44 px**, Roboto **661,16 px** — 94 px różnicy. Skutek **dzisiaj, bez żadnego webfontu**: H1 łamie się na **2 linie na iOS/macOS i 3 linie na Androidzie**, co przesuwa podtytuł i CTA o **55,19 px** (zmierzone 2026-08-17, kadr 1440 px; kadr 390 px trzyma 3 linie na obu). Wymiana kroju losuje to łamanie po raz drugi. | `src/components/Hero.module.css:47` | Blok designu — decyzja właściciela 2026-08-17: zdjąć `24ch`, zastąpić miarą niezależną od kroju (`max-width` w rem + `text-wrap: balance`). Powiązane: T4 (obietnica „H1 ≤ 3 linie"). Arkusz `/proba-kroju` ma pokazywać H1 w OBU wariantach — z `24ch` i z miarą docelową — żeby decyzja zapadła na obrazie. |
| T13 | **Brak tokenów CTA i tokenów typografii — system tokenów wymaga rozszerzenia przy designie.** Recepta CTA (wypełnienie, kolor etykiety, waga, wcięcia, promień) jest powtórzona RĘCZNIE w trzech modułach (`Hero`, `SekcjaPlanow`, `Zamkniecie`) i żaden token jej nie nazywa; obwódki CTA nie ma tam wcale. `tokeny.css` nie zawiera ŻADNEGO tokenu typografii (krój, skala stopni, interlinie, tracking) — 68 linii, same kolory i wymiary. Eksperyment palety obchodzi to nadpisaniem ról `interakcja` / `tekst-na-interakcji` i dokłada obwódkę selektorem po nazwie klasy CSS Modules — to obejście na czas próby, nie rozwiązanie. | `design/tokens.json`, `src/components/{Hero,SekcjaPlanow,Zamkniecie}.module.css` | Blok designu — wprowadzić jawne tokeny CTA (wypełnienie, etykieta, obwódka, stan aktywny) i rodzinę tokenów typografii; dopiero wtedy zdjąć powtórzenia z trzech modułów. Decyzja właściciela 2026-08-17. |
| T14 | **Znacznik `✓` w tabeli cennika zależy od cudzej decyzji projektowej.** `U+2713` występuje 15× na `/cennik` (pl/en/de). Schibsted Grotesk i Geist **nie mają tego glifu w ogóle** — subsetowanie tego nie naprawi, bo nie ma czego wyciąć; niesie go wyłącznie Onest. Znak spada wtedy na fallback systemowy, czyli jest rysowany innym krojem niż reszta tabeli. | `src/components/TabelaPorownawcza.*`, `content/{pl,en,de}` | Blok designu — decyzja właściciela 2026-08-17: znacznik przechodzi na **inline SVG** (`aria-hidden`, dostępna nazwa zostaje w treści komórki), bo to dekoracja niosąca znaczenie już wyrażone tekstem, a uzależnianie jej od glifu w kroju to uzależnienie od cudzej decyzji projektowej. Nie wykonywać przed blokiem designu. |
| T15 | **Wyjątek lintera tokenów dla bloku eksperymentu palety — wygasa 2026-08-31.** `scripts/lint-tokeny.mjs` (funkcja `oslonaWyjatku`) osłania surowe hexy WYŁĄCZNIE między znacznikami `/* === EKSPERYMENT PALETY — DO USUNIĘCIA === */` i `/* === KONIEC EKSPERYMENTU PALETY === */` w `src/app/globals.css`. Data jest strażnikiem, nie komentarzem: po 2026-08-31 linter zapala się na samym ISTNIENIU bloku. Znacznik w innym pliku, blok zdublowany, niedomknięty albo odwrócony = czerwień. Dowody mutacyjne **2026-08-19, commit `a826464`** (cztery mutacje w jednym przebiegu, każda z przywróceniem przez `git checkout --`): hex poza blokiem → **1** naruszenie; data cofnięta na 2026-08-16 → **77** naruszeń; znacznik przeniesiony do `src/components/` → **2** naruszenia; blok niedomknięty (usunięty znacznik zamknięcia) → **77** naruszeń; wszystkie cztery `exit 1`, po każdym przywróceniu linter zielony, sumy SHA-256 obu plików zgodne co do bajtu. **Liczby poprawione — poprzedni zapis mówił 45 tam, gdzie dziś jest 77.** Pomiar źródłowy pochodził z 2026-08-17 (commit `997630f`) i nie niósł ani daty, ani commita; nieaktualny stał się bez niczyjej zmiany w linterze, bo dwie z czterech mutacji mierzą, ILE linii odsłania utrata osłony, a osłaniany blok urósł między tymi dwoma dniami. Rozbiór 77: **74 hexy + 2 wymiary w px + 1 komunikat o samym istnieniu bloku**. Dwa px to literały w komentarzach wewnątrz bloku — osłona jest LINIOWA (`if (osloniete?.has(i)) return` pomija dla osłoniętej linii wszystkie wzorce), więc zakres faktyczny wyjątku jest szerszy niż „wyłącznie barwy" z komentarza w `src/app/globals.css:417-419`; dziś nie kosztuje to nic, ale to rozjazd dokumentacji z mechanizmem → pozycja T20. **Uwaga do samego stempla:** przebieg wykonano na drzewie roboczym commita `72f664a`, który tego samego dnia został poprawiony przez `git commit --amend` (zmiana wyłącznie treści komunikatu) na `a826464` — `72f664a` nie jest przodkiem gałęzi i nie istnieje na `origin`, więc stempel wskazywałby w pustkę. Drzewa obu commitów są tym samym obiektem (`61e52e9`), `git diff 72f664a a826464` pusty, więc pomiar odnosi się do stanu plików commita `a826464` co do bajtu. Reguła kanonu zadziałała na pierwszej własnej liczbie: commit w stemplu musi być commitem OSIĄGALNYM, nie takim, który istniał w chwili pomiaru. | `scripts/lint-tokeny.mjs`, `src/app/globals.css` | Usunięcie bloku palety i bloku kroju wraz z `public/fonts/eksperyment/` po decyzji o palecie i kroju, najpóźniej 2026-08-31; razem z nimi znika `oslonaWyjatku()`. Wyjątek przyznany przez właściciela 2026-08-17, wąski i z datą, na czas stanowiska eksperymentalnego. |
| T16 | **Bramka kontrastu w stanach interaktywnych — nowa, z trzema świadomymi granicami.** `e2e/kontrast-stanow.spec.ts` + `e2e/pomoc/sonda-stanow.mjs` mierzą 1.4.3 / 2.4.11 / 1.4.11 w czterech stanach (spoczynek, hover, active, fokus) przez `CSS.forcePseudoState` na wszystkich adresach z rejestru × 3 języki × 2 kadry. Granice zapisane wprost: **(a) W-GRANICA-01** — kontrolka zawierająca natywne `input`/`select`/`textarea` nie podlega sprawdzeniu granicy 1.4.11, bo stan niesie widżet, a nie malowane tło (pigułka okresu na `/cennik`: `powierzchnia-2` × tło strony 1,00:1). Wyjątek jest warunkowany OBECNOŚCIĄ natywnej kontrolki w chwili pomiaru, nie nazwą klasy — przerobienie pigułki na `div` sterowany JS-em kasuje wyjątek sam z siebie. **(b)** `PROGI.wylaczony = 3` jest DECYZJĄ PROJEKTU PONAD NORMĘ (WCAG wyłącza nieaktywne kontrolki spod 1.4.3/1.4.11); dziś serwis nie ma ani jednej wyłączonej kontrolki, więc próg jest pusty — pilnuje go test czujnika żywego, który wyłącza prawdziwe radio i sprawdza, że sonda je widzi w czterech stanach. **(c)** rysunek widżetów przeglądarki (kółko radia, ptaszek pola wyboru) nie istnieje w stylu wyliczonym i pozostaje poza zasięgiem sondy. Zabezpieczenia przed zerowaniem: `podejrzaneReguly` (reguła `X:hover Y` poza zasięgiem) = 0, ≥10 elementów klikalnych na trasie, ≥1 element zmieniający wygląd pod wymuszeniem, `nieoznaczalne` = 0. Dowody mutacyjne 2026-08-17: przywrócony defekt hover w trzech modułach → **144 naruszenia / 48 z 64 testów czerwonych** (1,34:1, `Zamkniecie_cta` 48×, `SekcjaPlanow_cta` 18×, `Hero_cta` 6×); próg z obu stron — `Hero_cta` `#c5c5c5` = **4,49:1 → 12 naruszeń**, w tym samym przebiegu `Zamkniecie_cta` `#c8c4c8` = **4,50:1 → zero**, różnica 0,007:1 rozdziela czerwień od zieleni. Po przywróceniu 64/64 zielone, sumy SHA-256 trzech modułów bez zmian. | `e2e/kontrast-stanow.spec.ts`, `e2e/pomoc/sonda-stanow.mjs`, `package.json` (`bramka:kontrast-stanow`) | Otwarte na stałe jako opis granic, nie jako dług. Warunek rewizji: powstanie pierwszej wyłączonej kontrolki (próg (b) przestaje być pusty), przerobienie pigułki okresu na kontrolkę bez natywnego `input` (wyjątek (a) wygasa sam — sprawdzić, czy granica faktycznie ma wtedy 3:1), albo pojawienie się reguły stanu stylującej inny element niż nosiciel stanu (bramka zatrzyma się sama na `podejrzaneReguly`). |
| T17 | **Lista tras w `e2e/axe.spec.ts` jest przepisana z ręki — 30 pozycji.** Dziś zgadza się co do jednej z rejestrem `src/i18n/sciezki.ts`, ale zgodność trzyma się wyłącznie na czyjejś pamięci: nowa podstrona wejdzie do mapy stopki i do `ISTNIEJACE_SCIEZKI`, a skan dostępności ominie ją w ciszy i na zielono. Nowa bramka `kontrast-stanow` bierze trasy z rejestru właśnie dlatego. | `e2e/axe.spec.ts` | Blok przeglądu bramek — albo przepiąć `axe.spec.ts` na rejestr (przedmiotem asercji są naruszenia axe, rejestr wyznacza tylko zasięg — ten sam argument, co w `kontrast-stanow.spec.ts`), albo dołożyć asercję porównującą długość listy z rejestrem. Nie robione teraz, bo to zmiana w bramce spoza zakresu stanowiska eksperymentalnego. |
| T18 | **Arkusz próbny kroju `public/proba-kroju.html` — tymczasowy, wygasa 2026-08-31 razem z blokiem eksperymentu palety.** Trzy kroje × trzy palety, H1 w obu miarach (`24ch` i docelowej), widoki 390 i 1440 px. Leży w `public/`, a nie na trasie, z dwóch powodów: matcher middleware przepisuje wszystko spoza `ISTNIEJACE_SCIEZKI` na 404 (ścieżka z kropką wychodzi spod matchera), a wpis do rejestru wciągnąłby arkusz pod bramkę kontrastu stanów, gdzie eksperymentalne palety dają CTA 1,12:1 — czerwień z szumu, nie z defektu. Arkusz **nie ma własnej wartości wizualnej**: barwy i `@font-face` zaciąga w czasie wykonania z żywego builda (`fetch('/')` + przepisanie `<link rel=stylesheet>`), więc nie może się rozjechać ze źródłem prawdy. Ponieważ przeniesienie pliku o katalog obok byłoby najtańszym sposobem ominięcia lintera tokenów, `scripts/lint-tokeny.mjs` skanuje dziś także `public/` (`.html/.css/.svg/.js`) — droga zamknięta strukturalnie, nie obietnicą. Dowód mutacyjny: hex w arkuszu → czerwień, `12px` → czerwień, po przywróceniu SHA-256 zgodna i bramka zielona. **Lekcja pomiarowa do zapamiętania poza tym arkuszem:** pierwsza wersja skryptu weryfikującego dała liczby wyglądające sensownie i fałszywe w dwóch miejscach naraz — (a) ustawiała okno na 390 px, ale nie przełączała kadru w samym arkuszu, więc H1 liczył się z widoku 1440; (b) mierzyła natychmiast po przełączeniu kroju, a przy `font-display: swap` mierzyła jeszcze krój zapasowy — Onest i Geist pokazywały co do setnej piksela wartość system-ui, co wyglądało na wynik („kroje mają tę samą szerokość"), a znaczyło „krój się nie wczytał". Oba defekty dawały czerwień nigdzie: skrypt był zielony, tabela pełna, wnioski nieprawdziwe. Naprawa nie polega na dłuższym czekaniu, tylko na asercji: skrypt pyta CDP `CSS.getPlatformFontsForNode`, co FAKTYCZNIE rasteryzuje H1, i odrzuca pomiar, gdy to nie jest oczekiwany krój. Arkusz kalibruje się na żywej stronie: przy widoku 390 px daje kolumnę 358 px i H1 35,5 px, czyli dokładnie to, co produkcja (zmierzone 2026-08-17). | `public/proba-kroju.html`, `scripts/lint-tokeny.mjs`, `public/fonts/eksperyment/` | Usunąć arkusz razem z katalogiem krojów i blokiem palety po decyzji właściciela o kroju i palecie, najpóźniej 2026-08-31. Do tego czasu arkusz nie jest linkowany z żadnej strony ani z mapy witryny. |
| T19 | **Warianty AVIF/WebP nie mają dowodu, że powstały z BIEŻĄCYCH źródeł.** Rozstrzygnięcie właściciela 2026-08-19: `obrazy:pipeline` **zostaje narzędziem ręcznym przy dostawie kadrów, nie bramką CI**. Powód rozstrzygający: produktem skryptu są artefakty commitowane w `public/`, więc w CI generowałby je do katalogu efemerycznego i wyrzucał — bramka byłaby zielona NIEZALEŻNIE od zawartości repozytorium, czyli „bramka, która nic nie sprawdza". Trzy powody wspierające: skrypt otwiera się destrukcyjnym `rmSync` na plikach śledzonych; wariant „wygeneruj i `git diff --exit-code`" byłby czerwony z powodów niezwiązanych z obrazami (bajtowa powtarzalność AVIF/WebP nie jest gwarantowana między lokalnym sharp 0.35.3 / libaom 3.14.1 / libwebp 1.6.0 / vips 8.18.3 a tym, co da `npm ci` na `ubuntu-latest`); integralność, o którą chodzi, jest już w CI — `e2e/zrzuty-filarow.spec.ts:110` asertuje komplet 36 wariantów, `:132` asertuje bajtową zgodność opublikowanego PNG z dostawą (SHA-256), a spec biegnie w `bramka-pelny-zestaw`. **LUKA, KTÓRA ZOSTAJE:** CI sprawdza, że warianty ISTNIEJĄ i że PNG się zgadza, ale nie sprawdza, że AVIF/WebP wyprowadzono z dzisiejszych źródeł. Podmiana źródła bez przepuszczenia go przez pipeline przechodzi dziś przez wszystkie bramki. | `scripts/obrazy-pipeline.mjs`, `design/pipeline-obrazow.json`, `public/` (36 wariantów), `e2e/zrzuty-filarow.spec.ts:110,132` | **Propozycja mechanizmu właściciela 2026-08-19, BEZ IMPLEMENTACJI:** manifest z sumą SHA-256 każdego ŹRÓDŁA, commitowany razem z wariantami; bramka porównuje sumy dzisiejszych źródeł z manifestem i czerwieni się, gdy źródło zmieniono bez przepuszczenia przez pipeline. Mechanizm sprawdza SKUTEK (czy warianty odpowiadają tym źródłom, z których je zrobiono), a nie odtwarza PROCES — dlatego omija problem niepowtarzalności bajtowej AVIF między środowiskami, który przewraca wariant „wygeneruj i porównaj". Wdrożenie osobnym zadaniem po decyzji, gdzie manifest leży i która bramka go czyta. |
| T20 | **Zakres wyjątku lintera tokenów jest SZERSZY, niż mówi jego własna dokumentacja.** Komentarz w `src/app/globals.css:417-419` opisuje osłonę jako obejmującą „wyłącznie barwy". Mechanizm jest LINIOWY: pętla lintera robi `if (osloniete?.has(i)) return`, czyli dla linii wewnątrz bloku palety pomija WSZYSTKIE wzorce — również „wymiar w px poza tokenami". Wykryte 2026-08-19 (commit `a826464`; przebieg szedł na jego poprzedniej postaci `72f664a` — amend zmienił wyłącznie komunikat, drzewo to ten sam obiekt) przez rozbiór mutacji „data cofnięta": **74 hexy + 2 wymiary w px + 1 komunikat o istnieniu bloku = 77 naruszeń**. Sam pomiar był zielony i wyglądał sensownie — rozjazd wyszedł dopiero z rozbicia sumy na składniki. Dziś nie kosztuje nic: oba literały px siedzą w komentarzach tłumaczących, dlaczego repozytorium pisze `0.0625rem`, a nie `1px`. Koszt pojawiłby się, gdyby ktoś wstawił do bloku palety prawdziwy wymiar w px — przeszedłby po cichu. | `scripts/lint-tokeny.mjs` (`oslonaWyjatku` + pętla główna), `src/app/globals.css:417-419` | Albo zawęzić osłonę do wzorca barw (osłaniać hex, nie linię), albo poprawić komentarz tak, żeby mówił, co osłona robi naprawdę. Znika sama 2026-08-31 razem z blokiem palety i funkcją `oslonaWyjatku()`. **Nie naprawiane 2026-08-19** — poza zakresem zamówionym przez właściciela (zasada „defekt na listę, nie do naprawy z własnej inicjatywy"). |
| T21 | **Nic nie pilnuje, że skróty commitów w dokumentacji wskazują na commity, które istnieją.** Klasa „odwołanie do stanu, który przestał istnieć" (kanon, ADR-018; rodzina `RECZ-286` z projektu aplikacji). Amend, rebase i squash przepisują skróty bez ostrzeżenia, więc **dokument staje się nieprawdziwy bez jednej zmiany w dokumencie** — to jest powód, dla którego pre-commit na plikach w indeksie tego NIE złapie. Weryfikacja wsteczna 2026-08-19 (commit `547b846`, 103 pliki `docs/` + `CLAUDE.md`): **191 tokenów hex 7–40 znaków, 140 odwołań do commitów osiągalnych (wszystkie także na `origin`), 5 wystąpień jednego widma (`72f664a`, wszystkie świadome — to adnotacje opisujące samo widmo), 1 skrót drzewa (`61e52e9`), 45 tokenów niebędących obiektami git**. Rozbiór tych 45: 27 × `agentId` z `docs/faza-4/odzysk-etap-c/fanout-*.jsonl`, 8 × odciski kluczy bypassu Vercela w `docs/faza-4/bramka-na-preview.md`, 4 × prefiksy sum SHA-256 plików, 2 × nazwy paczek webpacka, 1 × celowo zmyślony `deadbeefdead` w opisie mutacji W1, 2 × commit **cudzego repozytorium** (`e35ad8ce…` z repo aplikacji — sprawdzony: istnieje i jest osiągalny TAM, nieweryfikowalny stąd z definicji). | `docs/` (103 pliki), `CLAUDE.md`, `.github/workflows/bramki.yml` (15 × `actions/checkout@v4`) | **PROPOZYCJA STRAŻNIKA, BEZ IMPLEMENTACJI (2026-08-19).** Kryterium przyjęcia właściciela: skrót z reflogu → czerwień, skrót osiągalny → zieleń; do tego kontrola negatywna w tym samym przebiegu (kanon). Sześć wiążących ustaleń konstrukcyjnych, każde z pomiaru, nie z rozumowania: **(1)** test to `git merge-base --is-ancestor`, nigdy `git cat-file -t` — na `72f664a` `cat-file` mówi „commit" (obiekt z reflogu leży na dysku autora), `merge-base` mówi NIE; **(2)** dzisiejsze CI **przewróciłoby taką bramkę na wszystkich 140 odwołaniach naraz**: żaden z 15 kroków `actions/checkout@v4` nie ustawia `fetch-depth`, czyli klon ma **1 commit**. Zmierzone na klonach lokalnych: pełny → 157 commitów, `cd06530` osiągalny (zieleń); `--depth 1` → 1 commit, ten sam skrót nie jest nawet obiektem (czerwień); po `fetch --unshallow` → 157 commitów i znów zieleń. Warunek wstępny bramki: `fetch-depth: 0` w jej kroku checkout; **(3)** bramka nie może zgadywać po kształcie tokenu — 45 z 191 tokenów hex to nie commity, więc reguła „hex 7–40 = commit" byłaby czerwona w dniu wprowadzenia na 45 nie-defektach. Potrzebna **jawna notacja** (np. `commit:abc1234`), a bramka sprawdza wyłącznie oznaczone; **(4)** notacja musi mieć wariant świadomego widma (np. `commit-martwy:72f664a`) — bez niego wpis T15/T20, który OPISUJE widmo, zapalałby bramkę na zawsze, a strażnik uczyłby ludzi kasować dokumentację defektu; wariant musi być policzalny i raportowany, żeby nie stał się cichym wyłącznikiem; **(5)** odwołania międzyrepozytoryjne (`e35ad8ce…`) wymagają repo w notacji i **jawnej deklaracji, że bramka ich nie weryfikuje** — inaczej albo fałszywa czerwień, albo cicha zieleń bez sprawdzenia; **(6)** skanuje **wszystkie** pliki, nie tylko zmienione, bo defekt powstaje bez zmiany dokumentu. Wdrożenie osobnym zadaniem po decyzji właściciela — dziś w rejestrze leży sam projekt. **ODSYŁACZ Z T23, DOPISANY 2026-08-23 NA POLECENIE WŁAŚCICIELA — przeczytaj, ZANIM napiszesz pierwszą linię tego strażnika:** rozstrzygnięcie D5 mówi, że `fetch-depth: 0` wchodzi **wyłącznie do kroków czytających historię**, a przegląd na `f2db728` (osiągalny, 2026-08-23) wykazał, że takich kroków w bramkach jest dziś **zero**. **To zadanie będzie pierwszym.** Jego `checkout` dostaje `fetch-depth: 0` **w tym samym commicie, co sam strażnik** — nie w osobnym i nie „potem", bo strażnik bez tego ustawienia zapali się na wszystkich odwołaniach naraz (ustalenie 2 powyżej) i zostanie uznany za wadliwy, choć wadliwe będzie otoczenie. Ten odsyłacz istnieje właśnie po to: warunek zapisany wyłącznie przy T23 przeczytałby ten, kto czyta T23, a nie ten, kto siada do budowy strażnika. |
| T22 | **Bramka wydajności mierzy CUDZE wdrożenie i nie wyklucza równoległych pomiarów — dwa przebiegi tej samej gałęzi biją się o ten sam adres.** Zaobserwowane 2026-08-19 na parze przebiegów CI `32300222841` (commit `b51d0b8`) i `32300453626` (commit `4c22d6d`). Różnica między tymi commitami to **13 linii w dwóch plikach `.md`** — ani jednej linii źródeł, konfiguracji czy treści; dokument `/` w rozgrzewce obu przebiegów ma **identyczne 43 054 B i 12 zasobów**. Mimo to trasa `/` dała: przebieg 1 — surowe `2455 · 1219 · 1508 · 1488 · 1491`, werdykt **1491 ms** (zieleń), przebieg 2 — surowe `2505 · 1488 · 2104 · 2092 · 2087`, werdykt **2092 ms** (czerwień, zapas −292 ms). TTFB w obu ~611–615 ms; cała różnica siedzi w Render Delay (876 → 1481 ms). **Zadania wydajności biegły równolegle: 20:45:54–20:54:01 i 20:48:29–20:56:29 — 5 min 32 s wspólnego okna na TYM SAMYM aliasie gałęzi z `LHCI_BAZA`.** Drugi defekt tej samej pary: strażnik prowieniencji (`bramka:preview`) sprawdza `x-catherly-wydanie` **raz, przed** Lighthouse'em, a alias jest przestawialny — push w trakcie pomiaru przestawia go na nowe wdrożenie i późniejsze przebiegi mierzą inny commit, niż mówi log. Komentarz w `bramki.yml:281` nazywa dokładnie tę mutowalność („adres w LHCI_BAZA jest stały, wdrożenie pod nim nie"), ale osłania tylko moment startu. Trzecia rzecz, którą przyrząd sam napisał w OBU przebiegach, też w tym zielonym: `⚠ rozrzut większy niż zapas — ta trasa może spaść przy niezmienionym kodzie` (rozrzut `/` = 1017 i 1235 ms przy zapasie ~300 ms). Zieleń przebiegu 1 nie była więc dowodem stabilnego marginesu, tylko jedną stroną monety. **Kolizję wywołał agent**, pchając drugi commit w trakcie trwania pierwszego pomiaru — ale sama możliwość kolizji jest defektem bramki, nie pomyłką operatora. **SKAŻENIE SIĘGA WSTECZ — unieważnia OBIE liczby, nie tylko czerwoną.** Łatwo było poprzestać na „drugi pomiar skażony": czerwień jest podejrzana z natury, zieleń nie. Ale skoro strażnik prowieniencji sprawdza wydanie wyłącznie na starcie, a alias przestawił się w trakcie, to późniejsze przebiegi przebiegu 1 mogły mierzyć już wdrożenie `4c22d6d` — czyli zieleń 1491 ms też nie daje się przypisać do `b51d0b8`. Właściciel podniósł to do kanonu 2026-08-19: przy skażeniu pomiaru sprawdza się, czy skażenie nie sięga wstecz; zieleń sprzed skażenia nie jest automatycznie czysta. **Ostrzeżenie o rozrzucie to osobna klasa: „raport, którego nikt nie czyta, przestaje być raportem"** (nazwana przez właściciela 2026-08-19, kanon ADR-018) — linijka istniała, była prawdziwa, wypisywała się w obu przebiegach i nie zmieniła niczyjego zachowania, bo siedziała w logu zielonego zadania. | `.github/workflows/bramki.yml` (zadanie wydajności; **brak bloku `concurrency:` w całym pliku**), `scripts/sprawdz-preview.mjs`, `scripts/rozgrzewka-preview.mjs`, zmienna repozytorium `LHCI_BAZA` | **ROZSTRZYGNIĘCIE WŁAŚCICIELA 2026-08-19 — trzy kierunki wdrożone tego samego dnia, czwarty odłożony.** **(a) WDROŻONE:** blok `concurrency` na poziomie procesu w `bramki.yml`, `cancel-in-progress: true`. Klucz **nie** taki, jak brzmiała pierwotna propozycja: `github.ref` przy zdarzeniu `pull_request` to `refs/pull/N/merge`, a przy `push` — `refs/heads/…`, więc klucz z `github.ref` rozdzieliłby dwa przebiegi TEJ SAMEJ gałęzi do dwóch grup i defekt wróciłby w ciszy dokładnie w PR, czyli w momencie merge'u. Klucz bierze gałąź wdrażaną przez Vercela: `github.event.pull_request.head.ref || github.ref_name` — ta sama logika, co przy `OCZEKIWANY_COMMIT`. Cena przyjęta świadomie: przy otwartym PR zdarzenia `push` i `pull_request` dają dwa przebiegi tego samego commita i jeden anuluje drugi; gdy przegra ten z `pull_request`, jego wymagane sprawdzenie pokaże „cancelled" i trzeba je powtórzyć. Kierunek awarii bezpieczny — anulowane ≠ zielone. **Dowód przyjęcia (dwa pushe w odstępie minuty → anulowanie widoczne w logu) NIEWYKONANY: czeka na zgodę właściciela na push.** **(b) WDROŻONE:** `scripts/straznik-po-pomiarze.mjs` + krok „Prowieniencja po pomiarze" z `if: always()` — odpytuje `x-catherly-wydanie` na WSZYSTKICH 7 trasach po `lhci collect` i przy rozjeździe kończy `POMIAR UNIEWAŻNIONY` (wyjście 1). Porównanie z `OCZEKIWANY_COMMIT` jest równoważne porównaniu „przed ↔ po", bo rozgrzewka wymusza równość na wszystkich trasach przed pomiarem — nie ma pliku stanu do zgubienia. `WYNIK_POMIARU` steruje wyłącznie brzmieniem komunikatu (zieleń bez pokrycia / czerwień nieprzypisywalna), nie werdyktem. Dowód mutacyjny 2026-08-19 z kontrolą negatywną w tym samym przebiegu, na udawanym preview (lokalny serwer HTTP oddający sterowany nagłówek — bez Vercela i bez sekretu): wydanie zgodne → **7/7 tras ✔, wyjście 0**; alias przestawiony na inne wdrożenie → **7/7 rozjazdów, wyjście 1**; wdrożenie bez nagłówka wydania → **7/7 braków, wyjście 1**. Dowód czwarty, dopisany 2026-08-20 po tym, jak roboczy obserwator CI wypisał 99 razy `error connecting to api.github.com`, a potem zameldował „OBA PRZEBIEGI ZAKONCZONE" z kodem 0 — pytanie brzmiało, czy nowy strażnik ma ten sam kształt, i odpowiedź jest z pomiaru, nie z czytania kodu: serwer żywy → **wyjście 0**, ten sam serwer zgaszony w trakcie → **7/7 „adres nieosiągalny (fetch failed)", wyjście 1**. Milczenie celu jest u strażnika czerwienią, nie ciszą. **Szczelina, która zostaje, spisana wprost:** klamra „przed ↔ po" nie wykryje podmiany, która zdarzyła się i cofnęła WEWNĄTRZ pomiaru — `lhci collect` przechodzi wszystkie trasy × przebiegi w jednym procesie i nie daje w to wejść. **(c) WDROŻONE jako werdykt, nie ostrzeżenie:** `scripts/werdykt-marginesu.mjs` + krok „Werdykt marginesu (żółty: margines pozorny)”. **Prawdziwego ŻÓŁTEGO GitHub Actions nie ma** — zadanie zna wyłącznie `success`/`failure`/`cancelled`, a status `neutral` jest dostępny tylko przez Checks API dla aplikacji GitHuba, nie z poziomu workflow. Wykonalne i wdrożone: krok z `continue-on-error: true` (⚠ zamiast zielonego znaczka przy kroku) + adnotacja `::warning` na stronie podsumowania przebiegu, NAD listą zadań + tabela w `$GITHUB_STEP_SUMMARY` z brzmieniem „margines pozorny, rozrzut X przy zapasie Y". Werdykt progowy w kroku „Pomiar" nietknięty. Zdjęcie jednej linii `continue-on-error` zamienia ten żółty w czerwień blokującą — to decyzja właściciela, nie zmiana konstrukcji. **(d) ODŁOŻONE decyzją właściciela** do czasu samotnego, niezakłóconego pomiaru: cokolwiek dotyczącego progów albo reguły werdyktu odpornej na rozrzut. **Kierunek (b') z pierwotnej listy — mierzyć niemutowalny adres wdrożenia zamiast aliasu — pozostaje niewdrożony**; (a) zabiera przyczynę, (b) wykrywa skutek, ale aliasu nikt nie przestał używać. Warunek zamknięcia bez zmian: przebieg wydajności, w którym `/` mieści się pod progiem, a przyrząd NIE zgłasza marginesu pozornego. **PIERWSZY SAMOTNY POMIAR PO WDROŻENIU (2026-08-19, `69c2dab`, przebieg 32302412113, zadanie 96227948719, okno 21:10:22–21:18:29 — 13 min 53 s po zakończeniu ostatniego z kolidujących zadań, bez nakładania): bramka ZIELONA, `/` LCP 1521 ms, zapas +279 ms, przebiegi surowe 1873 · 2102 · 1514 · 1509 · 1521, rozrzut 593 ms. Warunek zamknięcia NIEspełniony w drugiej połowie: rozrzut przekracza zapas ponad dwukrotnie, więc nowy werdykt marginesu zapaliłby ⚠ na tej samej zielonej bramce — i to samo na TBT (93 ms, zapas +107, rozrzut 660 ms). Pierwszy czysty pomiar jest zatem dowodem, że rozstrzygnięcie (c) trafiło w rzecz istniejącą, a nie hipotetyczną: bramka jest zielona, a margines pozorny.** |
| T23 | **Żaden krok `checkout` w CI nie ustawia `fetch-depth`, więc KAŻDA przyszła bramka operująca na historii przewróci się na płytkim klonie.** Stan zmierzony 2026-08-19 na `69c2dab`: 15 wystąpień `actions/checkout@v4`, `fetch-depth` użyte 0 razy. Domyślnie akcja klonuje `--depth 1` — runner dostaje JEDEN commit i nic poza nim. **Dowód trzech klonów** (to repo, skrót `cd06530`, osiągalny z HEAD; pełny log: dowód mutacyjny z 2026-08-19): **A. klon pełny** → 159 commitów, `cat-file: commit`, `merge-base --is-ancestor` OSIĄGALNY → strażnik ZIELONY (kontrola negatywna w tym samym przebiegu). **B. klon `--depth 1`**, czyli dokładnie to, co robi dziś CI → 1 commit, `fatal: Not a valid object name cd06530` — skrót nie jest nawet OBIEKTEM, nie tylko nieosiągalnym → strażnik CZERWONY. **C. ten sam płytki klon po `git fetch --unshallow`** → 159 commitów, OSIĄGALNY → ZIELONY; naprawa działa i jest odwracalna. **Zasięg jest szerszy niż T21**: T21 to jeden strażnik, a to jest własność ŚRODOWISKA wspólna wszystkim przyszłym bramkom czytającym historię — `merge-base`, `git log`, `blame`, „czy ten plik zmienił się względem `main`", porównanie z bazą, wersjonowanie z tagów. **Kierunek błędu jest przeciwny do lokalnego** i to jest tu najważniejsze: lokalnie `git cat-file -t` daje FAŁSZYWĄ ZIELEŃ, bo obiekt z reflogu leży na dysku; w CI to samo polecenie na płytkim klonie daje czerwień z zupełnie innego powodu — obiektu w ogóle nie ma. Dowód D w tym samym przebiegu: skrót widmo `72f664a` (po amendzie z T21) nie jest obiektem ANI w klonie pełnym, ANI w płytkim — reflog nie podróżuje z klonem. Czyli CI jest surowsze od maszyny lokalnej, a strażnik napisany na lokalnych obserwacjach pada w CI z przyczyny, której lokalnie nie da się zobaczyć. | `.github/workflows/bramki.yml` — 15 × `actions/checkout@v4` bez `fetch-depth` | **Zapisane osobno na polecenie właściciela 2026-08-19**: „to nie jest warunek T21, to jest osobna pozycja rejestru o szerszym zasięgu". Bez implementacji, czeka razem z T21. Rozstrzygnięcia potrzebuje jedna rzecz: czy `fetch-depth: 0` wchodzi we WSZYSTKIE piętnaście kroków (jednolicie i odpornie na przyszłe bramki, kosztem klonu rosnącego z historią — dziś 159 commitów), czy tylko w zadania czytające historię (taniej, ale nowa bramka historyczna dopisana do zadania bez tego ustawienia znów przewróci się CICHO — czyli ta sama pułapka, tylko rzadsza). Warunek zamknięcia: krok checkout w zadaniu historycznym z jawnym `fetch-depth`, plus przebieg CI, w którym strażnik historii świeci zielono na runnerze, a nie tylko lokalnie. **ROZSTRZYGNIĘTE 2026-08-23 (D5), POZYCJA OTWARTA:** właściciel wybrał wariant węższy — `fetch-depth: 0` wchodzi **wyłącznie do kroków czytających historię**, z uzasadnieniem przy każdym. Przegląd wykonany 2026-08-23 na `f2db728` daje **zero** takich kroków dziś: jedyne wywołanie gita w bramkach to `git rev-parse HEAD` w `scripts/check-audyt.mjs` (działa na klonie `--depth 1`, bo czyta wskaźnik, nie przodków), a hooki `pre-commit` (`git diff --cached`) w CI się nie uruchamiają. Żadna linia `actions/checkout@v4` nie została więc zmieniona — zmiana bez adresata byłaby zmianą, której nikt nie zamawiał. Pierwszym krokiem historycznym będzie strażnik osiągalności z **T21** (`git merge-base --is-ancestor`) i to jego `checkout` dostanie `fetch-depth: 0` w tym samym commicie, co sam strażnik. Warunek zamknięcia bez zmian — czeka na T21. Ryzyko wariantu węższego (nowa bramka historyczna dopisana do zadania bez ustawienia przewróci się cicho) właściciel przyjął świadomie. **ZAPISANE JAKO POMIAR, NIE JAKO BRAK ROBOTY (właściciel, 2026-08-23):** *„D5 nie zmieniło ani jednej linii — i to jest wynik, nie brak wyniku."* Rozstrzygnięcie było **poprawne i bezprzedmiotowe naraz**: reguła „tylko tam, gdzie krok czyta historię" jest właściwa, a takich kroków jest dziś **zero**. Zero jest wynikiem pomiaru, nie luką w wykonaniu — i dlatego stoi tu z datą i osiągalnym commitem (`f2db728`, 2026-08-23), tak samo jak stałaby liczba różna od zera. **WARUNEK POWROTU PRZYPIĘTY DO ZDARZENIA, KTÓRE JEST W NASZYCH RĘKACH:** pierwszym krokiem czytającym historię będzie strażnik osiągalności z **T21**; jego `checkout` dostaje `fetch-depth: 0` **w tym samym commicie, co sam strażnik** — nie w osobnym, nie „potem". To jest klasa **„warunek zależny od zdarzenia"** w jej łatwiejszym wariancie: zdarzenie nie przychodzi z zewnątrz, tylko my je wywołujemy, więc da się je przypiąć do konkretnego zadania zamiast pilnować kalendarzem. Odsyłacz w drugą stronę stoi przy **T21** — bo warunek zapisany wyłącznie tutaj przeczyta ten, kto już czyta T23, a nie ten, kto siada do budowy strażnika. |
| T24 | **Żadne zadanie CI nie ma `timeout-minutes`, więc zawieszony krok kosztuje sześć godzin ciszy i kończy bramkę statusem, który nie jest ani zielenią, ani czerwienią.** Zmierzone 2026-08-20 na przebiegach `32302412113` (`69c2dab`), `32300222841` (`b51d0b8`) i `32300453626` (`4c22d6d`): **cztery zadania anulowane, każde po 6 h 00 min ±20 s** — `Dostępność` 21:10:21→03:10:41, `Pełny zestaw e2e` 20:45:53→02:46:09 i 20:48:29→02:48:46, `Dostępność` 20:48:28→02:48:44. Wszystkie cztery stanęły na **tym samym kroku**: `npx playwright install --with-deps chromium`. `grep -c timeout-minutes .github/workflows/bramki.yml` = **0**, więc obowiązuje domyślny limit platformy — 6 godzin. Skutki są trzy i tylko pierwszy jest oczywisty. **(1)** Około 24 h czasu runnera spalone w jeden wieczór. **(2)** Zawieszona bramka kończy się jako `cancelled`, a to nie jest werdykt: brak werdyktu wygląda w interfejsie jak brak problemu, więc bramka, która nigdy nie odpowiedziała, jest wygodniejsza w odbiorze niż bramka, która odpowiedziała „nie". **(3) I to jest koszt wniesiony przez WŁASNE dzisiejsze rozstrzygnięcie (a)**: `cancel-in-progress: true` czyni `cancelled` stanem normalnym i oczekiwanym, więc czytelnik traci możliwość odróżnienia „anulowane, bo wyparte nowszym pushem" od „anulowane, bo wisiało sześć godzin". Przed (a) `cancelled` było sygnałem samo w sobie; po (a) już nie jest. **Konkretna strata w tej dobie: dla wypchniętego `69c2dab` bramka `Dostępność` NIE MA WERDYKTU** — zieleń dostępności, o którą pytał właściciel, jest udowodniona wyłącznie dla `b51d0b8`. Poszlaka co do przyczyny, nie dowód: w tym samym oknie czasowym lokalny `gh` stracił łączność z `api.github.com` na 99 kolejnych prób, więc to wygląda na zdarzenie sieciowe wieczoru, a nie defekt kodu — ale trwałym ustaleniem jest brak limitu, który KAŻDE takie zdarzenie zamienia w sześć godzin ciszy. | `.github/workflows/bramki.yml` — 15 zadań, 0 × `timeout-minutes` | Blok po T21/T23 — decyzja właściciela, bez implementacji (klauzula: defektów spoza zlecenia się nie naprawia). Do rozstrzygnięcia dwie rzeczy, nie jedna. **Limit:** jaka wartość i gdzie — jednolita na wszystkich zadaniach czy per zadanie (najdłuższa uczciwa bramka to dziś `Pełny zestaw e2e` w 5 min 23 s i `Wydajność` w 8 min 07 s, więc limit rzędu 20–30 min jest wielokrotnością zmierzonego czasu, a nie ciasnym gorsetem). **Rozróżnialność `cancelled`:** czy chcemy kroku, który przy anulowaniu dopisuje do podsumowania przyczynę (wyparcie przez nowszy przebieg vs. przekroczony limit), bo bez tego (a) i T24 zlewają się w jeden nieczytelny status. Warunek zamknięcia: przebieg, w którym celowo zawieszony krok kończy zadanie w zadeklarowanym limicie i zostawia w podsumowaniu jawną informację, dlaczego. **ROZSTRZYGNIĘTE I WDROŻONE 2026-08-23 (D6), POZYCJA NADAL OTWARTA — brak mutacji.** Właściciel rozstrzygnął obie rzeczy: **limit** per rodzaj zadania — `timeout-minutes: 20` dla zadań pomiarowych, `10` dla pozostałych; **rozróżnialność `cancelled`** — krok wymagany, bo bez niego wyparcie przez concurrency i przekroczony limit dają identyczny status. Wdrożone w `.github/workflows/bramki.yml` na wszystkich **15** zadaniach (`bramka-wydajnosc` = 20, czternaście pozostałych = 10), każde z krokiem `Przyczyna anulowania` pod `if: cancelled()` wypisującym `::warning` z instrukcją odczytu (czas krótki = wyparcie, czas bliski limitowi = przekroczenie). **Czego brakuje do zamknięcia:** przebiegu z celowo zawieszonym krokiem, który pokaże, że limit faktycznie ucina zadanie i że adnotacja jest widoczna w podsumowaniu. Sama obecność `timeout-minutes` w pliku jest kodem wyglądającym poprawnie, czyli statusem NIESPRAWDZONE — a niesprawdzone liczy się jak niedziałające (ADR-018 pkt 2). Uwaga do przyszłej weryfikacji: adnotacja `::warning` jest widoczna w podsumowaniu przebiegu, ale **nie ma własnego kodu wyjścia** — to świadomie słabsza warstwa niż wymaga klasa „raport, którego nikt nie czyta"; jeśli po mutacji okaże się, że ginie, potrzebny będzie osobny mechanizm. **DO ROZSTRZYGNIĘCIA PRZEZ WŁAŚCICIELA — zapas przy `bramka-pelny-zestaw` jest cienki.** Rozstrzygnięcie mówiło „20 dla zadań pomiarowych, 10 dla pozostałych" i tak zostało zastosowane **literalnie**: pomiarowe jest jedno (`bramka-wydajnosc`, LCP/CLS/TBT), więc `Pełny zestaw e2e` dostał 10. Tyle że ta pozycja sama zmierzyła go na **5 min 23 s** (2026-08-20) — to zapas **1,86×**, podczas gdy uzasadnienie limitu mówiło o „wielokrotności zmierzonego czasu, a nie ciasnym gorsecie" i podawało rząd 20–30 min. Zadanie instaluje przeglądarkę (`playwright install --with-deps chromium`) i uruchamia **każdy** spec w obu projektach, więc zimny cache albo wolniejszy runner mieszczą się w tym zapasie. Skutek przekroczenia nie jest osłabieniem bramki, tylko **czerwienią, która nie jest werdyktem** — dokładnie tą klasą, którą ta pozycja opisuje. Wykonawca **nie podniósł limitu z własnej ręki**, bo to byłoby rozszerzenie rozstrzygnięcia poza jego literę. Do decyzji: czy `bramka-pelny-zestaw` (i ewentualnie `bramka-e2e`) też idzie na 20, czy zostaje na 10 ze świadomym przyjęciem ryzyka. **ROZSTRZYGNIĘTE 2026-08-23 (druga tura): `bramka-pelny-zestaw` → 20 minut.** Uzasadnienie właściciela, przytoczone, bo jest przesłanką, nie ozdobą: *„podałem próg per kategoria, nie znając zmierzonych czasów"* — decyzja „20 dla pomiarowych, 10 dla pozostałych" zapadła **przed** sprawdzeniem, do której grupy trafia zadanie o czasie 5 min 23 s. `bramka-e2e` **zostaje na 10** — właściciel rozstrzygnął tylko o `pelny-zestaw`, więc drugiego zadania nie ruszono. **Osobnego „rejestru przesłanek" w tym repozytorium NIE MA** i nie zakładam go: nowy plik z regułami wchodzi wyłącznie jako ADR albo rozdział `CLAUDE.md` (zakaz 10), a przesłanka rozstrzygnięcia należy do pozycji, której dotyczy — czyli tutaj. **POMIAR CZASÓW — 2026-08-23, przebiegi `32661737288` (`f2db728`) i `32663550392` (`d7a2fe3`), oba commity osiągalne**, ten sam kod bramek, odczyt przez `gh api .../jobs` z pól `started_at`/`completed_at`: `Wydajność` **8,05** / **7,90**; `Pełny zestaw e2e` **3,55** / **3,21**; `Dostępność` **2,65** / **2,53**; `E2E` **0,98** / **1,03**; `Build` **0,63** / **0,83**; pozostałe dziewięć zadań **0,36–0,55** / **0,38–0,55**. **Wniosek, którego nie dało się wyciągnąć z jednej liczby:** to samo zadanie dało **5 min 23 s** (2026-08-20) i **3 min 13 s** (2026-08-23) — **rozrzut 1,67×** między przebiegami tego samego kodu. Zadanie o takim rozrzucie nie może stać na zapasie 1,86×; to jest właściwy argument za dwudziestką, mocniejszy niż pojedyncza liczba, od której zaczęło się zgłoszenie. **ZGŁOSZONE, NIE ZMIENIONE:** po tej poprawce najcieńszy zapas ma już **nie** `Pełny zestaw e2e` (3,55 min / 20 = **5,6×**), tylko **`Wydajność`** (8,05 i 7,90 min / 20 = **2,48×** i **2,53×**) i zaraz za nią **`Dostępność`** (2,65 min / 10 = **3,77×**). Progów tych nie ruszam — właściciel rozstrzygnął o jednym zadaniu, a rozszerzanie rozstrzygnięcia na kolejne jest tym samym błędem, przed którym broni zakaz 1. **DOWÓD CZĘŚCIOWY okablowania (2026-08-23, `d7a2fe3`):** krok `Przyczyna anulowania` istnieje w **15/15** zadań i przy braku anulowania kończy się statusem `skipped` — w tym w zadaniu `Nieodwracalne`, które **padło** (`cancelled()` jest wtedy fałszem, więc krok słusznie się nie odpalił). To dowodzi, że krok jest podpięty i że nie odpala się przypadkiem. **Nie dowodzi zapłonu** — do tego potrzeba anulowania. **ODPOWIEDŹ NA ROZSTRZYGNIĘCIE WŁAŚCICIELA O STATUSIE ŻÓŁTYM (2026-08-23) — NIE DA SIĘ, I TO NIE Z NASZEJ WINY.** Właściciel orzekł: *„cancelled bez wyjaśnienia daje status ŻÓŁTY zadania, nie samą adnotację. Jeśli GitHub Actions tego nie umożliwia bez zmiany kodu wyjścia — powiedz."* Sprawdzone tego samego dnia, ze źródeł pierwotnych (dokumentacja GitHuba, kod `actions/runner`, kod `cli/cli`, odczyty REST na cudzych publicznych przebiegach), z osobnym przebiegiem adwersaryjnym, który miał te ustalenia obalić. **Werdykt: terminalnego statusu żółtego w GitHub Actions NIE MA.** Dowody, każdy ze wskazaniem miejsca: **(1)** kod wyjścia kroku ustala konkluzję, *„which can be `success` or `failure`"* — lista dwuelementowa, koniec; **(2)** adnotacja **z definicji** nie zmienia stanu: `ExecutionContext.AddIssue` w `actions/runner` (`src/Runner.Worker/ExecutionContext.cs`, ok. 796–860) maskuje sekrety, obcina komunikat, zwiększa licznik i pisze do logu — **nie dotyka `Result` ani `TaskResult`**. Właściciel miał rację co do słabości `::warning`, i jest to racja pokazywalna w cudzym kodzie, nie kwestia oceny; **(3)** jedyne konkluzje poza parą success/failure to `neutral` i `skipped`, a dokumentacja statusów nazywa obie **sukcesem** (*„This is treated as a success for dependent checks in GitHub Actions"*) — nie istnieje wartość o znaczeniu „ostrzeżenie"; **(4)** mapowania konkluzja→kolor w interfejsie webowym GitHub **nie publikuje nigdzie**; jedyne oficjalne mapowanie kolorów leży w narzędziu `gh` (`pkg/cmd/pr/checks/output.go`) i mówi: `pending` → żółty, `cancelled`/`neutral`/`skipped` → **szary**. Żółć jest kolorem stanu TRWAJĄCEGO, nie konkluzji; **(5)** wniosek o konkluzję ostrzegawczą leży u GitHuba otwarty **od stycznia 2022**, bez wdrożenia. Jedyna dosłowna żółć osiągalna z workflow to commit status `state: pending` — **odrzucam i nie wliczam do opcji**, bo to kłamstwo o stanie („trwa" zamiast „skończone z zastrzeżeniem") zostawiające na commicie wieczystą żółtą kropkę: rodzina „raportu, którego nikt nie czyta", tylko gorsza, bo fałszuje. **USTALENIE, KTÓRE POTWIERDZA PRZESŁANKĘ TEJ POZYCJI:** job ubity przez `timeout-minutes` dostaje konkluzję **`cancelled`** — dokładnie tę samą, co job wyparty przez `concurrency`. Zmierzone na **sześciu** cudzych publicznych przebiegach w rozpiętości siedmiu miesięcy. Enum REST **ma** wartość `timed_out`, ale GitHub jej w tym miejscu **nie wystawia**. Czyli bez własnego kroku obu przypadków odróżnić się nie da — teza, na której ta pozycja stoi, jest potwierdzona pomiarem, a nie tylko rozumowaniem. **LUKA WAŻNIEJSZA NIŻ SAMO PYTANIE — i to ona przesądza, że pozycja zostaje otwarta:** w całym materiale **nie znaleziono ani jednego publicznego przebiegu, w którym wykonałby się krok z warunkiem dokładnie `cancelled()`**. Wszystkie zaobserwowane przypadki dotyczą `always()`. Że nasze piętnaście kroków się odpali, **wynika z kodu runnera** — `StepsRunner.cs` przy anulowaniu ustawia wynik zadania na `Canceled` i **mimo to** ewaluuje warunki pozostałych kroków (pętla bez `break`), a `CancelledFunction.cs` pokazuje, że `cancelled()` czyta status **ZADANIA** (`jobStatus == ActionResult.Cancelled`), nie przebiegu. **Wynikanie z kodu to nie jest pomiar.** Zgodnie z „brak dowodu = brak zabezpieczenia" te 15 kroków ma status NIESPRAWDZONE, czyli liczy się jak niedziałające. **JEDNA DZIURA SPRAWDZONA U SIEBIE I ZAMKNIĘTA:** timeout na poziomie **KROKU** dawałby `TaskResult.Failed`, nie `Canceled`, więc `cancelled()` byłoby wtedy fałszem i krok by milczał. Odczyt `bramki.yml` 2026-08-23: **15 limitów na poziomie zadania, 0 na poziomie kroku** — u nas ta dziura jest dziś teoretyczna i ma taka zostać; kto dopisze `timeout-minutes` do kroku, wyłączy przy okazji rozróżnianie anulowań, nie wiedząc o tym. **NAZWANA SŁABOŚĆ — zapis wymagany przez właściciela, świadomie zostawiony jako słabość, nie jako „wystarczy":** adnotacja `::warning` **nie zmienia konkluzji zadania i zmienić jej nie może** — nie przez nasze niedbalstwo, tylko dlatego, że platforma nie ma stanu pośredniego między sukcesem a porażką. Warstwa jest **słabsza, niż wymaga klasa „raport, którego nikt nie czyta"**, i pozostaje słabsza świadomie, do czasu decyzji. Dwa dodatkowe ograniczenia tej warstwy, też zmierzone: limit **10 ostrzeżeń na krok** i **4096 znaków** komunikatu; nasze adnotacje niosą `path: ".github"`, więc **w zakładce „Files changed" PR-a się nie pokażą** — widać je wyłącznie w „Checks". **CO ZOSTAJE DO WYBORU (wybór właściciela, nie mój):** **(A) czerwień** — zadanie zależne czytające `needs.<job>.result == 'cancelled'` i kończące się porażką. **Odradzam:** wyparcie przez `concurrency` jest zjawiskiem **oczekiwanym i częstym**, więc zamiana go w czerwień produkuje „czerwień, która nie jest werdyktem" — dokładnie defekt opisany w tej pozycji, tyle że z drugiej strony, i uderza w ADR-020. **(B) jawnie przyjęta warstwa słabsza** = dzisiejsza adnotacja, wzmocniona wpisem do `$GITHUB_STEP_SUMMARY`, który ląduje na stronie podsumowania przebiegu, a nie w logu. **To jest rekomendacja — ale NIE do wdrożenia przed mutacją:** przetrwanie zapisu do podsumowania przy anulowaniu **nie zostało potwierdzone żadnym źródłem**, więc dopisanie go teraz byłoby kolejnym kodem, który wygląda poprawnie. **KOLEJNOŚĆ, KTÓRĄ REKOMENDUJĘ:** najpierw mutacja, potem wybór — bo dziś oba warianty stoją na niesprawdzonym założeniu, że krok w ogóle się odpala. Eksperyment rozstrzyga trzy rzeczy naraz i mieści się w jednym przebiegu: **(i)** zadanie z `timeout-minutes: 1` i `sleep 300` — czy krok pod `cancelled()` się odpala przy przekroczeniu limitu; **(ii)** dwa pushe w odstępie minuty — czy odpala się przy wyparciu przez `concurrency` (to jest równocześnie zaległy „dowód przyjęcia dla (a)"); **(iii)** kontrola negatywna **w tym samym przebiegu** — zadanie kończące się normalnie, w którym krok ma zostać `skipped`. Do tego odczyt, jak obie sytuacje wyglądają w interfejsie bez otwierania logu. **Warunek: nie w trakcie żadnego pomiaru** — `bramka-wydajnosc` w tym samym przebiegu zostałaby skażona (T22). **ROZSTRZYGNIĘTE 2026-08-24: NAZWANA SŁABOŚĆ, BEZ CZERWIENI.** Właściciel przyjął obalenie własnego rozstrzygnięcia w całości — *„żółtego statusu konkluzji nie ma"* — i **odrzucił wariant (A)** z podanego tu powodu: wyparcie przez `concurrency` jest zjawiskiem **zamierzonym**, a *„bramka czerwieniejąca na zdarzeniu zamierzonym uczy ignorowania czerwieni"*. Zostaje wariant **(B)**: adnotacja `::warning` z jawnym zapisem, że jest to warstwa słabsza, **i z powodem** — a powód brzmi **„to jedyne, co istnieje"**, nie „to wystarczy". Różnica jest cała: pierwsze jest granicą platformy, drugie byłoby naszym zaniechaniem. **KOLEJNOŚĆ ZATWIERDZONA: mutacja przed wyborem.** Wzmocnienia wpisem do `$GITHUB_STEP_SUMMARY` **nie dopisuję dziś** — jego przetrwanie przy anulowaniu jest niepotwierdzone, więc byłoby kolejnym kodem, który wygląda poprawnie. **LUKA ZAPISANA JAKO GRANICA POMIARU, NIE JAKO BRAK WYKONANIA (polecenie właściciela 2026-08-24).** Fakt: w całym przeszukanym materiale **nie ma ani jednego publicznego przebiegu, w którym wykonałby się krok z warunkiem dokładnie `cancelled()`** — wszystkie obserwowane przypadki to `always()`. To **nie** znaczy, że czegoś nie zrobiliśmy; znaczy, że **poza naszym zasięgiem nie ma czego odczytać**, i dlatego jedyną drogą jest własny przebieg. Rozróżnienie ma wagę praktyczną: „brak wykonania" domyka się robotą, „granica pomiaru" domyka się **wyłącznie eksperymentem**, więc mieszanie ich zaciera, czego naprawdę brakuje. **Ustalenie ogólniejsze, przyjęte tego dnia do kanonu jako `B-17` z toru 8** (właściciel: *„o którym nie wiedziałeś"*): **wynikanie z kodu to nie pomiar** — odczyt `StepsRunner.cs` i `CancelledFunction.cs` mówi, co mechanizm **ma** robić, i jest mocniejszy od domysłu, ale **słabszy od przebiegu**; pozycji nie zamyka i za mutację nie wystarcza. **PROGÓW CZASOWYCH NIE RUSZAMY DZIŚ** (rozstrzygnięcie właściciela): najcieńsze zapasy — `Wydajność` **2,48×** i `Dostępność` **3,77×** — zostają, bo *„Wydajność ma osobny problem (rozrzut 593 przy zapasie 279), a limit czasu jest przy niej wtórny"*. Liczby rozrzutu i zapasu pochodzą od właściciela, **nie z pomiaru w tym repozytorium** — jedno ogniwo, stąd niesprawdzone; do zestawienia z T33 i kierunkiem (d), nie do cytowania jako pomiar własny. |
| T25 | **Reguła „przekazanie aktualizowane na bieżąco" nie ma strażnika — pilnuje jej wyłącznie proza w `CLAUDE.md`.** Polecenie właściciela 2026-08-20: `docs/PRZEKAZANIE-SESJI.md` ma mówić prawdę o stanie ciągle, a aktualizacja idzie **w tym samym commicie** co zmiana, którą opisuje (osobny commit „potem" nie powstanie — sesja kończy się na limicie kontekstu bez ostrzeżenia). Zapisane w `CLAUDE.md` i w rozdz. 0 przekazania. **To jest dokładnie klasa „brak dowodu = brak zabezpieczenia": reguła bez mechanizmu wygasa cicho i wygasa najszybciej wtedy, kiedy jest najbardziej potrzebna — pod koniec długiej sesji, przy zmęczonym kontekście.** Zasięg jest szerszy niż jeden plik: ta sama reguła obowiązuje rejestr, więc strażnik zbudowany dla przekazania powinien dać się nakierować na oba. **Rzecz, która musi być powiedziana wprost przy projektowaniu:** strażnik NIE JEST w stanie sprawdzić, że dokument mówi prawdę — może sprawdzić wyłącznie niezmienniki mechaniczne. Wariant naiwny („commit dotyka `src/` ⇒ musi dotykać przekazania") sprawdza DOTKNIĘCIE, nie PRAWDĘ: zmiana jednej spacji go zaspokaja. To ta sama rodzina co T19 („bramka zielona niezależnie od zawartości repozytorium") i wprowadzona bez nazwania tej granicy dałaby fałszywe poczucie pokrycia — gorsze niż brak bramki. | `CLAUDE.md` (sekcja „Przekazanie sesji aktualizowane na bieżąco"), `docs/PRZEKAZANIE-SESJI.md` (rozdz. 0), `.githooks/pre-commit`, `.github/workflows/bramki.yml` | **Bez implementacji, czeka na decyzję właściciela** (klauzula: defektów spoza zlecenia się nie naprawia; właściciel polecił ZAPISAĆ regułę, nie budować bramkę). Do rozstrzygnięcia, co strażnik ma sprawdzać — trzy niezmienniki różnej mocy, do wybrania osobno: **(1) MOCNY, w pełni mechaniczny:** liczba niewypchniętych commitów zadeklarowana w rozdz. 1 równa się `git log --oneline origin/<gałąź>..HEAD | wc -l`; rozjazd = czerwień, bo to twierdzenie sprawdzalne co do liczby. **(2) MOCNY, ale zależny od T21/T23:** każdy skrót commita w przekazaniu jest OSIĄGALNY (`merge-base --is-ancestor`) — to jest strażnik T21 nakierowany na ten plik, więc dziedziczy jego warunek wstępny `fetch-depth: 0` (T23) i jego wymóg jawnej notacji z wariantem świadomego widma. **(3) SŁABY, i musi być tak nazwany w komunikacie:** commit dotykający `src/`, `scripts/`, `.github/`, `lighthouserc.cjs` lub `package.json` bez dotknięcia przekazania → ostrzeżenie. Jako bramka blokująca (3) jest szkodliwa (wymusza pustą zmianę i uczy obchodzenia), jako ostrzeżenie z własnym kodem wyjścia — użyteczna, ale kanon mówi wprost, że ostrzeżenie bez własnego miejsca w interfejsie przestaje być raportem. Warunek zamknięcia jak przy każdym strażniku: **dowód mutacyjny z kontrolą negatywną w tym samym przebiegu** — dokument z zaniżoną liczbą niewypchniętych commitów → czerwień, ten sam dokument poprawiony → zieleń. **WZORZEC Z ZEWNĄTRZ — DOPISANY 2026-08-23, PRZEKAZANY PRZEZ WŁAŚCICIELA, NIESPRAWDZALNY STĄD.** Źródło podane w całości, bo bez niego ten akapit jest plotką: **tor 10, 2026-08-23, `docs/weryfikacja-obietnic/` w repozytorium APLIKACJI, gałąź `feat/cs-build`**. Treść: tamtejszy **strażnik punktu wznowienia** został sprawdzony mutacją — *wycofanie `PUNKT-WZNOWIENIA.md` z indeksu przy zostawionym `CLAUDE.md` → EXIT=1; przywrócenie → EXIT=0*. To jest pierwszy realny dowód jego czułości, czyli dokładnie ten rodzaj dowodu, którego brakuje po tej stronie. **Dlaczego to leży przy T25:** tamten strażnik pilnuje wymogu analogicznego do naszego („dokument stanu aktualizowany razem ze zmianą"), a nasz wymóg **nie ma dziś żadnego strażnika** — pilnuje go wyłącznie proza. Konstrukcja z tamtej mutacji jest **gotowym wzorcem** dla niezmiennika (3) powyżej: „commit dotknął kodu, nie dotknął dokumentu stanu". **DWA OZNACZENIA, KTÓRE MUSZĄ JECHAĆ RAZEM Z TYM WZORCEM:** **`P-19`** — sprawdź wzorzec, zanim go powielisz; przeniesienie konstrukcji bez sprawdzenia jest przyjęciem cudzego dowodu za własny. **`P-22`** — **drogi weryfikacji z tej strony nie ma**: to inne repozytorium, więc ani mutacji, ani kodu strażnika nie da się stąd odczytać. Ustalenie ma zatem **dwa ogniwa** (właściciel ← sesja aplikacji / tor 10) i **drugie jest stąd niesprawdzalne** — ta sama klasa co pokrycie T36 i T30, i tak samo ma być czytane: jako wskazówka, gdzie szukać, nigdy jako dowód. Kody `P-19` i `P-22` pochodzą ze schematu prowieniencji repozytorium aplikacji; **ich definicji nie ma po tej stronie** i nie przepisuję jej z pamięci — kto ich potrzebuje, czyta je u źródła. |
| T26 | **Liczby w raporcie powykonawczym nie niosą stempla przy sobie — niosą go 1070 linii wyżej, w nagłówku dokumentu przeznaczonego do CYTOWANIA.** Rzecz wyszła 2026-08-20 przy pisaniu briefingu dla sesji aplikacji i **pierwsza diagnoza była błędna**, co jest tu częścią pozycji, nie dygresją. §5.5(a) mówi: rejestr ma **349 linii** i **34 pozycje** (24 treściowe + T1–T10). Dziś rejestr ma **50 pozycji** (24 + T1–T26); liczby linii nie wpisuję, bo ten wiersz sam ją zmienia — `wc -l docs/faza-2/rejestr-warunkow-powrotu.md`. Odruch brzmiał: „nieaktualny fakt w repozytorium, do rejestracji". Sprawdzenie nagłówka odruch obaliło — raport deklaruje w wierszach 3–6 swój zakres: **od `0896219` (2026-08-08 22:01) do `3ca12a3` (2026-08-16 19:04)** — a wobec TEGO zakresu obie liczby są prawdziwe co do jedności; §0.1 dokumentu mówi zresztą wprost, że każde twierdzenie pochodzi z odczytu repozytorium. **Dokument z zadeklarowanym zakresem się nie starzeje; starzeje się cytat wyjęty z niego bez zakresu.** To rozróżnienie jest jedyną rzeczą wartą tu zapisania i jest ODWROTNOŚCIĄ pułapki, którą projekt zna z drugiej strony (klasa „odwołanie do stanu, który przestał istnieć", T21): tam dokument milcząco się przeterminował, tu dokument jest w porządku, a przeterminować się może dopiero czytelnik. **Realna, słabsza obserwacja, która zostaje:** raport sam siebie nazywa „matrycą do budowy następnych stron — nie kroniką", czyli jest projektowany pod cytowanie fragmentami, a fragment §5.5 wyjęty bez nagłówka niesie dziś liczbę rozbieżną z repozytorium o **16 pozycji** (34 wobec 50). Koszt dzisiaj: zero. Koszt przy pierwszej następnej stronie budowanej „wg matrycy": ktoś przepisze 34 i 349 jako stan bieżący. | `docs/RAPORT-POWYKONAWCZY-WWW.md:3-6` (deklaracja zakresu), `:1074-1083` (§5.5(a)) | **Bez implementacji i bez poprawiania liczb w raporcie** — defektów spoza zlecenia się nie naprawia, a tu dochodzi drugi powód: podmiana liczb w dokumencie z zadeklarowanym zakresem **zepsułaby go**, bo zamieniłaby spójną migawkę w mieszankę dwóch dat. Do rozstrzygnięcia przez właściciela, który z trzech kierunków (jeśli którykolwiek): **(1)** stempel przy liczbie — w §5.5 dopisać „(stan `3ca12a3`, 2026-08-16)" tam, gdzie liczba opisuje rzecz żywą; najtańsze, niczego nie unieważnia; **(2)** znacznik świeżości przy każdym rozdziale-matrycy, żeby cytujący widział datę bez wracania do nagłówka; **(3)** nic — uznać, że nagłówek wystarcza, i zapisać to jako świadomą granicę dokumentu. Warunek zamknięcia dla (1) i (2): fragment §5.5 wyjęty z kontekstu niesie własną datę. Dla (3): zdanie w §0.1 mówiące wprost, że liczby w rozdziałach opisują stan ZAKRESU, nie stan dzisiejszy. **Zastosowanie tej samej reguły, odnotowane na polecenie właściciela 2026-08-23:** briefing dla sesji aplikacji miał zostać wysłany do innej sesji; `ListAgents` pokazał pięć sesji `souffle-de-nature-*` z **niezwiązanego projektu** i wysyłka nie została wykonana. Powód jest ten sam co w tej pozycji, tylko wycelowany w drugą stronę: dokument niesie zadeklarowany zakres (adresat: sesja w repozytorium aplikacji; przedmiot: `catherly-www`, odczyt 2026-08-20), a **wyjęcie go poza ten zakres unieważnia go dokładnie tak samo jak cytat wyjęty bez zakresu** — z dodatkowym kosztem, że wstrzykuje treść Catherly w cudzą pracę. Właściciel do protokołu: odmowa właściwa. Reguła praktyczna, która z tego zostaje: **adresat jest częścią ZAKRESU dokumentu, nie jego metadanymi** — jeśli nie ma pewności co do adresata, dokument zostaje w repozytorium i czeka, aż ktoś po niego przyjdzie. **DO REJESTRU PRZESŁANEK — przypadek po stronie WŁAŚCICIELA, zgłoszony przez niego samego 2026-08-24.** Nagłówki zleceń przez kilkanaście wklejek nosiły datę **23.08.2026**, gdy zegar wskazywał już **24.08**. Słowa właściciela: *„stemplowałem datą, która przestała obowiązywać, i nie sprawdziłem tego ani razu"*, *„podawałem datę z pamięci ostatniego odczytu, mając możliwość sprawdzenia jednym poleceniem"*. Klasa: **„stempel poprawny w chwili powstania, fałszywy w chwili cytowania"** — czyli to, co ta pozycja opisuje dla liczb w dokumentach, przeniesione na **datę w nagłówku polecenia**. Dwie rzeczy warte zapisania, bo bez nich to tylko anegdota. **(1)** Reguła złapała tego, kto ją egzekwuje — i to jest wynik normalny, nie ironia: stempel z pamięci jest wygodniejszy od odczytu i dlatego wygrywa u każdego, kto się nie zatrzyma. **(2) Zasięgu nie da się odtworzyć** — właściciel zaznaczył, że nie umie wskazać, od którego momentu nagłówki są błędne. Praktyczny wniosek dla czytającego archiwum tej sesji: **daty w nagłówkach zleceń z 23.08 są niepewne od nieustalonego momentu**; wiarygodne są stemple postawione po stronie wykonania, bo tam data pochodzi z odczytu zegara maszyny (`date`), a nie z pamięci. Gdzie oba się rozchodzą — **obowiązuje odczyt, nie nagłówek**. **DRUGA PRZESŁANKA PO STRONIE WŁAŚCICIELA, zgłoszona przez niego samego 2026-08-24 — `P-22` ROZPOZNANE PO STRONIE NADAWCY.** Właściciel przekazał odesłanie do „trzech pytań o strażniku”, których treści **po tej stronie nie było**; sesja odmówiła przepisania ich z pamięci i zapisała pytanie zerowe samodzielnie, z jawną adnotacją o braku. Jego kwalifikacja: *„przekazałem Ci odesłanie do listy, której nie masz”*. To jest **`P-22` widziane od drugiej strony**: dotąd oznaczaliśmy nim ustalenia, których **odbiorca** nie może zweryfikować; tutaj brak dostrzegł **nadawca** — i to jest tańszy moment, bo odesłanie bez treści kosztuje jedno pytanie, jeśli złapie je nadawca, a całą fałszywą pewność, jeśli odbiorca uzupełni je domysłem. Reguła praktyczna: **odsyłając do dokumentu spoza repozytorium adresata, dołącz treść albo jawnie napisz, że jej nie dołączasz.** Domknięte tego samego dnia: właściciel podał trzy pytania **ze źródłem** (tor 8, rejestr `B-16`/`B-17`, 2026-08-23), więc stoją dziś w `CLAUDE.md` — oznaczone `P-22`, bo drogi weryfikacji z tej strony nadal nie ma. **Właściciel policzył swoje wystąpienia tej wady: TRZY tego samego dnia** — odesłanie do trzech pytań o strażnikach, ustalenie toru 13 o parytecie oraz korekta „158→156” przekazana torowi 8. **Treści dwóch ostatnich po tej stronie NIE MA** i nie uzupełniam jej domysłem: odnotowuję sam fakt, tak jak został przekazany, a kto ich potrzebuje, czyta u źródła. Zapisanie ich tu z domyślonym znaczeniem byłoby popełnieniem dokładnie tej wady w akapicie, który ją opisuje. Zobowiązanie właściciela do protokołu: *„przekazując cokolwiek spoza Twojego repozytorium, dołączam treść albo piszę wprost, że jej nie dołączam”*. Reguła weszła do `CLAUDE.md` w jego brzmieniu, bo **asymetria kosztu jest w niej całą treścią**: jedno pytanie u nadawcy, cała fałszywa pewność u odbiorcy. **ODNOTOWANE PRZEZ WŁAŚCICIELA JAKO PIERWSZY PRZYPADEK, W KTÓRYM REGUŁA STOSOWANA DO SIEBIE UCHRONIŁA, ZAMIAST OPISAĆ PO FAKCIE.** Wszystkie wcześniejsze samozastosowania w tym repozytorium były **retrospektywne** — reguła nazywała wadę, którą już popełniono (przekazanie łamiące własną regułę o polach samostarzejących się, hierarchia odtwarzająca defekt szczebel wyżej, kanon twierdzący o mechanizmie, którego nie ma). Tutaj kolejność jest odwrotna: reguła **zadziałała w chwili pisania** i zatrzymała rękę przed dopisaniem domyślonej treści do akapitu, który tę właśnie wadę opisuje. Różnica ma wartość diagnostyczną: reguła stosowana retrospektywnie dowodzi, że jest **trafna**; reguła, która uchroniła, dowodzi, że jest **czytana w chwili pracy** — a to jest jedyny moment, w którym reguła może cokolwiek zmienić. |
| T27 | **Zlecenie z innej sesji wskazuje plik, którego w repozytorium adresata nie ma.** Zadanie z 2026-08-20 otwiera się zdaniem „WGRAŁEM `docs/KANON-CATHERLY-STRONA.md`" i buduje na nim pięć podpunktów. Pliku nie ma: sprawdzone 2026-08-20 w `docs/` na HEAD, w drzewie `docs/` gałęzi zdalnej, w całym drzewie roboczym oraz w `~/Downloads`, `~/Desktop` i obu pozostałych katalogach roboczych FBO OS — zero trafień. Właściciel do protokołu 2026-08-23: **„trzeci raz dziś"** — czyli nie wypadek, tylko powtarzalny tryb pracy między sesjami. Mechanizm: plik powstaje w jednej sesji, „istnieje" w rozmowie i nigdy nie dojeżdża do repozytorium adresata; obie strony zakładają wspólny stan, którego nie ma. Klasa jest **odwrotnością** „odwołania do stanu, który przestał istnieć" (T21): tam stan istniał i zniknął, tu nie zaistniał nigdy — i dlatego `merge-base --is-ancestor` tego nie złapie, bo nie ma czego sprawdzać. Koszt zaniechania jest asymetryczny: wykonanie zadania na wyobrażonym pliku produkuje commit, który wygląda na zrobiony. | zadanie z 2026-08-20 (poza repozytorium), `docs/BRIEFING-MIEDZY-SESJAMI.md:18-29` | Zlecenie wskazujące plik zaczyna się od „sprawdź, czy X istnieje; jeśli nie — powiedz i stój", a **dowodem zamknięcia jest wykonanie, nie zapis**: zlecenie, które faktycznie zatrzymało się na tym sprawdzeniu (mutacja — podać nazwę pliku, którego nie ma, i pokazać zatrzymanie). Do rozstrzygnięcia przez właściciela, czy zdanie stoi w `CLAUDE.md`, czy w szablonie zlecenia po stronie zlecającej — tam jest tańsze, ale **poza zasięgiem bramek tego repozytorium**, więc niesprawdzalne stąd. |
| T28 | **Zamówiony pomiar był już wykonany, a jego odpowiedź brzmi „nie zielone".** Zadanie z 2026-08-20 prosi o samotny pomiar na stanie wypchniętym. Pomiar istnieje: przebieg **32302412113**, commit **69c2dab**, okno 21:10:22–21:18:29 UTC 2026-08-19, samotność udowodniona odstępem (najbliższe inne zadanie wydajności skończyło się 20:56:29, trzynaście minut wcześniej, bez zachodzenia okien), strażnik przed pomiarem potwierdził `x-catherly-wydanie = 69c2dab…`. Trasa `/`: pięć surowych LCP **1873 · 2102 · 1514 · 1509 · 1521**, reprezentant przebieg #5 z 5 = **1521 ms**, rozrzut **593 ms**, zapas do progu **+279 ms**, ostrzeżenie przyrządu wypisane. Właściciel do protokołu 2026-08-23: to jest odpowiedź na pytanie o jedną czystą liczbę sprzed kilkunastu godzin i jest **gorsza, niż zakładał**. Rzecz warta zapisania nie jest w liczbach, tylko w tym, że zlecenie zamawiało robotę już wykonaną, a jej wynik był dostępny w logu i w `docs/PRZEKAZANIE-SESJI.md` rozdz. 5 — koszt wykonania byłby ~8 min CI plus push, na który nie było zgody. | przebieg `32302412113`, `docs/PRZEKAZANIE-SESJI.md` rozdz. 5 | Zlecenie pomiaru niesie zdanie „jeśli ten pomiar już istnieje na tym commicie, podaj liczby zamiast mierzyć", a zlecający sprawdza `gh run list` na commicie docelowym **przed** wysłaniem zadania. Zamknięcie: pierwsze zlecenie pomiaru, które przyszło z takim sprawdzeniem w treści. |
| T29 | **„Sprawdź PRZED i PO" niewykonalne, bo narzędzie zapewniające czystość siedzi w commicie NIEWYPCHNIĘTYM.** `scripts/straznik-po-pomiarze.mjs`, `scripts/werdykt-marginesu.mjs` i blok `concurrency` w `.github/workflows/bramki.yml` istnieją wyłącznie lokalnie, w commicie `6383580`. Stan zdalny `69c2dab` ma **zero** wystąpień `concurrency` i żadnego z tych dwóch skryptów (odczyt 2026-08-20). Zadanie każe więc zmierzyć czysto **zanim** wypchnie się narzędzia, które tę czystość zapewniają — kolejność jest odwrócona i nie da się jej obejść po stronie wykonawcy. Klasa: **zlecenie milcząco zakłada, że stan zdalny równa się stanowi lokalnemu**; założenie jest fałszywe zawsze, gdy czekają commity bez zgody, czyli w tym projekcie prawie zawsze. Rodzina: „`origin/…` to lokalna migawka, nie stan zdalny" — ta sama pomyłka wycelowana w drugą stronę. | `scripts/straznik-po-pomiarze.mjs`, `scripts/werdykt-marginesu.mjs`, `.github/workflows/bramki.yml` (blok `concurrency`), commit `6383580` | Zamknięcie razem z pushem pakietu (przekazanie 7.1) **plus** dowód z runnera, że oba skrypty i `concurrency` zachowują się na CI tak jak lokalnie (przekazanie 7.3.11) — dziś (b) i (c) mają dowody wyłącznie z udawanego preview i z danych lokalnych, nigdy z CI. Do czasu tego dowodu każde zlecenie „zmierz PRZED i PO" jest niewykonalne i ma wracać z tym zdaniem. |
| T30 | **`workflow_dispatch` nie działa, bo `main` nie ma katalogu `.github` — i jest to TA SAMA przyczyna co `RECZ-161` po stronie aplikacji, której nikt nie połączył.** GitHub udostępnia ręczny wyzwalacz tylko wtedy, gdy plik workflow stoi na gałęzi **domyślnej**; `main` (`0896219`, odczyt 2026-08-20) zawiera wyłącznie `.gitignore` i `docs`. Mówi o tym komentarz w samym `.github/workflows/bramki.yml:11`. Skutek: jedynym wyzwalaczem jest push, push uruchamia wszystkie **15** zadań, a `bramka-wydajnosc` ma dodatkowo `needs: build` — „sam krok wydajności" nie istnieje jako operacja. Właściciel do protokołu 2026-08-23: **„To ta sama przyczyna co RECZ-161 po stronie aplikacji i nie połączyłem tego."** Wartość tej pozycji jest w drugim zdaniu, nie w pierwszym: przyczyna była **znana w drugim repozytorium** i nie przeniosła się przez granicę repo, więc każda z sesji odkrywa ją osobno i za każdym razem od zera. Uwaga wprost z ustalenia T21(5): odwołanie do `RECZ-161` jest **międzyrepozytoryjne i żadna bramka tego repozytorium go nie weryfikuje** — jeśli tamten identyfikator zniknie albo zmieni numer, nikt tutaj tego nie zobaczy. | `.github/workflows/bramki.yml:11`, zdalny `main` = `0896219`; po stronie aplikacji `RECZ-161` w `/Users/sylwesterzabski/Documents/fbo os/fbo-os/docs/ZADANIA_RECZNE.md` (**niezweryfikowane stąd**) | Dwa rozłączne kierunki, do rozstrzygnięcia przez właściciela: **(1)** workflow trafia na `main` — ale to wiąże się z ADR-030 (wdrożenie produkcyjne przy Fazie 7) i planową czerwienią bramki wydajności na `main`, więc nie jest ruchem kosmetycznym; **(2)** zlecenia pomiaru przestają mówić „uruchom sam krok" i mówią „jeden przebieg gałęzi, żaden inny równolegle" — to jest osiągalne dziś i darmowe. Osobno, niezależnie od wyboru: sprawdzić, czy `RECZ-161` opisuje tę samą przyczynę, i **jeśli tak — zapisać powiązanie po obu stronach**, bo dziś istnieje wyłącznie w pamięci właściciela. |
| T31 | **„Push obu commitów" to w rzeczywistości push dziesięciu; liczba w zleceniu pochodziła z pamięci.** Właściciel do protokołu 2026-08-23: **„moja liczba z pamięci"**. Stan 2026-08-20: na gałęzi czekało **osiem** commitów bez zgody, zadanie dokładało dwa — razem dziesięć. Stan 2026-08-23, po commicie briefingu: **dziewięć**. Liczba zmieniła się między napisaniem zlecenia a jego wykonaniem i to jest cała treść tej pozycji — reguła „zgoda wyliczana po commitach, nieprzechodnia między pakietami" nie jest formalnością, tylko jedynym mechanizmem, który wyłapuje rozjazd między tym, co zlecający ma w głowie, a tym, co pójdzie na zdalne. Zdanie „push obu commitów" zatwierdza dwa nienazwane i wypchnęłoby wszystko. | `docs/BRIEFING-MIEDZY-SESJAMI.md:81-105`, przekazanie 7.1 | Każda prośba o push niesie listę **przeliczoną w chwili prośby** (`git log --oneline origin/faza-4/podstrony..HEAD`), a zgoda wymienia skróty. Zgoda udzielona formułą zbiorczą („razem z resztą", „wypchnij zmiany") **nie zamyka tej pozycji** i nie jest zgodą w rozumieniu reguły — wykonawca ma wrócić z wyliczoną listą i poczekać. Zamknięcie: pierwszy push, przed którym zgoda wymieniła komplet skrótów zgodny co do jedności z wyliczeniem. |
| T32 | **Źródeł reguł wiążących nie ma dwóch ani trzech — jest PIĘĆ, a dokument, który agenci czytają pierwszy, nie ma w tej hierarchii miejsca.** Sprawdzone 2026-08-23 przez odczyt deklaracji nadrzędności we wszystkich plikach `.md`: **(1)** `docs/STRATEGIA.md:3-5` — „Dokument nadrzędny projektu… Sprzeczność rozstrzyga się na korzyść strategii, chyba że ADR jawnie stanowi inaczej"; **(2)** `docs/PLAN.md:4-5` — „dokument wykonawczy… Nadrzędny wobec niego jest dokument strategii", a `PLAN.md:247` dodaje, że ADR-018 obowiązuje **w obu repozytoriach**; **(3)** `docs/adr/` — 30 decyzji, `README.md:7`: „ADR-018 jest nadrzędny wobec wszystkich pozostałych"; **(4)** `CLAUDE.md` — 180 linii reguł operacyjnych, z których **część nie ma pokrycia w żadnym ADR-ze** (backup, role, T25, tryb pracy), a plik **nigdzie nie deklaruje swojego miejsca względem STRATEGII i PLANU**; `CLAUDE.md:32` mówi tylko „ADR-018 nadrzędne wobec wszystkiego **poniżej**", czyli w obrębie samego pliku; **(5)** ten rejestr — wiąże w praktyce, bo każda pozycja niesie warunek, którego złamanie jest złamaniem reguły („treść wraca WYŁĄCZNIE po dowodzie wykonaniem"), a nigdzie nie jest napisane, czy ustępuje ADR-owi. Briefing z 2026-08-20 mówił o „dwóch źródłach i trzecim dokładanym" — **to była liczba za mała**, wzięta z pola widzenia agenta, nie z odczytu. Zadanie z sesji aplikacji chciało dołożyć szósty (`KANON-CATHERLY-STRONA.md`) i to jest dopiero powód, dla którego rzecz w ogóle wyszła. | `docs/STRATEGIA.md:3-5`, `docs/PLAN.md:4-5,247`, `docs/adr/README.md:7`, `CLAUDE.md:32`, ten plik | **Rozstrzygnięcie właściciela** (zapowiedziane 2026-08-23) — dwie rzeczy naraz, bo bez drugiej pierwsza nie działa: **(a)** gdzie w łańcuchu STRATEGIA → PLAN → ADR stoi `CLAUDE.md`; rekomendacja wykonawcy: **ADR-018 bezwzględnie nad wszystkim** (już tak zadeklarowany w trzech miejscach niezależnie), dalej pozostałe ADR-y, dalej STRATEGIA, dalej PLAN, a `CLAUDE.md` **nie jako szczebel, tylko jako operacyjne przepisanie tamtych** — z jawnym zdaniem „przy rozjeździe z ADR-em wygrywa ADR" na górze pliku; rejestr **nie jest źródłem reguł**, tylko listą warunków wynikających z ADR-018, i to też ma być napisane; **(b)** czy nowy kanon w ogóle wchodzi — a jeśli tak, to **nie jako szósty plik**, lecz albo jako **ADR-031** (staje się decyzją i dziedziczy hierarchię), albo jako **rozdział `CLAUDE.md`** (staje się trybem pracy). Warunek zamknięcia: każdy z pięciu plików niesie w nagłówku jedno zdanie o swoim miejscu w hierarchii, tak żeby czytający JEDEN z nich wiedział, czy trzyma w ręku rzecz nadrzędną, czy podrzędną. **ROZSTRZYGNIĘTE 2026-08-23, POZYCJA NADAL OTWARTA — warunek zamknięcia niespełniony w 4 z 5 plików.** Właściciel rozstrzygnął **(a)** kolejność: **1. ADR** (`docs/adr/`) → **2. `CLAUDE.md`** → **3. rejestr** → **4. przekazanie** → **5. dokumenty paneli**; przy równym poziomie wygrywa nowsze, ale **rozjazd zgłasza się, nie rozstrzyga po cichu**. Rozstrzygnął też **(b)**: kanon wchodzi **jako rozdział `CLAUDE.md`**, nie jako szósty plik ani ADR-031 — czyli dokładnie tak, jak nakazuje zakaz 10. Rozdział „Hierarchia źródeł reguł" dopisany do `CLAUDE.md` 2026-08-23. **Czego brakuje:** zdania o miejscu w hierarchii nie niosą jeszcze nagłówki pozostałych czterech dokumentów (`docs/adr/README`, rejestr, przekazanie, dokumenty paneli). Do zamknięcia trzeba je dopisać — inaczej czytający rejestr albo przekazanie nadal nie wie, że trzyma rzecz podrzędną, a to był cały sens tej pozycji. Rozstrzygnięcie zapisane w JEDNYM miejscu nie zastępuje warunku, który mówił o pięciu. **KTÓRYCH CZTERECH BRAKUJE — wymienione na polecenie właściciela 2026-08-23**, z jego uzasadnieniem: *„hierarchia zapisana wyłącznie w źródle nadrzędnym jest znana temu, kto już czyta nadrzędne"*. Sprawdzone odczytem nagłówków tego dnia: **(1)** `docs/adr/README.md` — deklaruje nadrzędność ADR-018 nad pozostałymi ADR-ami (`:7`), ale **nie mówi, gdzie stoją ADR-y wobec `CLAUDE.md`, rejestru i przekazania**; **(2)** `docs/faza-2/rejestr-warunkow-powrotu.md` — ten plik; nagłówek nie mówi, że rejestr jest szczeblem trzecim i **nie jest źródłem reguł**, tylko listą warunków; **(3)** `docs/PRZEKAZANIE-SESJI.md` — nagłówek nie mówi, że przekazanie jest szczeblem czwartym i opisuje stan, nie reguły; **(4)** `docs/faza-*/` — dokumenty paneli; **tu warunek jest niewykonalny wprost**, bo to nie jeden plik, tylko klasa plików, więc do rozstrzygnięcia zostaje, czy zdanie idzie do każdego z osobna, czy do jednego pliku zbiorczego, na który reszta wskazuje. **ROZJAZD ZGŁOSZONY, NIE ROZSTRZYGNIĘTY — dokładnie ta sama wada w nowym kształcie.** Hierarchia właściciela wymienia pięć źródeł: ADR → `CLAUDE.md` → rejestr → przekazanie → panele. Pomiar, na którym stoi ta pozycja, wymieniał pięć **innych**: STRATEGIA, PLAN, ADR, `CLAUDE.md`, rejestr. **`docs/STRATEGIA.md` i `docs/PLAN.md` wypadły z hierarchii, a nadal deklarują własne miejsce** — odczyt 2026-08-23: `docs/STRATEGIA.md:3-5` „Dokument nadrzędny projektu… Sprzeczność rozstrzyga się na korzyść strategii, chyba że ADR jawnie stanowi inaczej", `docs/PLAN.md:5` „Nadrzędny wobec niego jest dokument strategii". Skutek jest dziś **czynny**: STRATEGIA twierdzi, że wygrywa ze wszystkim poza ADR-em (więc i z `CLAUDE.md`), a nowy rozdział `CLAUDE.md` stawia `CLAUDE.md` na drugim szczeblu i **STRATEGII nie wymienia wcale**. Dwie żywe, sprzeczne deklaracje nadrzędności — czyli defekt, dla którego ta pozycja powstała, odtworzony jeden szczebel wyżej. Zgodnie z regułą zapisaną w tym samym rozstrzygnięciu („rozjazd zgłaszasz, nie rozstrzygasz po cichu") **nie ruszam żadnego z tych plików**. Do rozstrzygnięcia przez właściciela: czy STRATEGIA i PLAN wchodzą do hierarchii (i na którym szczeblu), czy przestały być źródłami reguł wiążących — a jeśli to drugie, ich nagłówki wymagają poprawki, bo dziś mówią co innego. **ROZSTRZYGNIĘTE 2026-08-24 — HIERARCHIA MA SIEDEM SZCZEBLI, NIE PIĘĆ.** Właściciel przyjął zgłoszenie i nazwał je swoim błędem: *„wymieniłem pięć źródeł, nie sprawdziwszy, ile ich jest"*. Kolejność obowiązująca: **1.** ADR · **2.** `CLAUDE.md` · **3.** `docs/STRATEGIA.md` · **4.** `docs/PLAN.md` · **5.** rejestr · **6.** przekazanie · **7.** dokumenty paneli. **Kanon stoi NAD strategią** z uzasadnieniem właściciela: *„strategia opisuje CO robimy, kanon opisuje JAK — a przy sprzeczności wygrywa sposób pracy, nie zamiar"*. Zdanie o pierwszeństwie strategii **wykreślone** z `docs/STRATEGIA.md` i zastąpione odesłaniem do hierarchii w `CLAUDE.md`; zmiana wykonana **osobnym commitem**, z cytatem starego brzmienia w opisie — bo to zmiana treści dokumentu, który do wczoraj deklarował się nadrzędnym. **DO REJESTRU PRZESŁANEK — klasa „ZŁY PODZBIÓR"** (właściciel, 2026-08-24; źródło ustalenia: **tor 10**, jedno ogniwo, stąd niesprawdzone): przesłanką pierwszego rozstrzygnięcia była lista pięciu źródeł **podana bez policzenia ich**. Zły podzbiór jest groźniejszy od pustej listy, bo **wygląda jak komplet** i zatrzymuje szukanie — ta sama rodzina co „wygląda na regułę werdyktu przy pobieżnym czytaniu". Reguła zapisana w `CLAUDE.md` przy hierarchii: **zanim wypiszesz zamkniętą listę źródeł, policz je w repozytorium.** Warunek zamknięcia pozycji **bez zmian i nadal niespełniony**: zdanie o miejscu w hierarchii ma nieść nagłówek **każdego** ze szczebli, a niesie go dziś `CLAUDE.md` i — po tej zmianie — `docs/STRATEGIA.md`. Zostaje pięć: `docs/adr/README.md`, `docs/PLAN.md`, ten rejestr, przekazanie i dokumenty paneli (przy paneli nadal otwarte, czy zdanie idzie do każdego pliku, czy do jednego zbiorczego). |
| T33 | **Próg 1800 ms przy rozrzucie 593 ms jest NIEINTERPRETOWALNY, a nie surowy — bramka wydajności nie może dziś dać wiarygodnej zieleni ani czerwieni.** Rozstrzygnięcie właściciela 2026-08-23, wprost do protokołu, na podstawie liczb z T28: pięć surowych LCP `/` = 1873 · 2102 · 1514 · 1509 · 1521, dwa przebiegi ponad progiem, rozrzut 593 ms przy zapasie 279 ms. Konsekwencja jest mocniejsza niż T10 i T22 osobno i dlatego dostaje własną pozycję: dotąd zapisane było, że **margines jest pozorny**; właściciel rozstrzyga, że wobec tego **werdykt bramki — którykolwiek — nie niesie informacji**. Zieleń nie dowodzi, że trasa mieści się pod progiem, bo ta sama trasa przy tym samym kodzie dała 2102 ms; czerwień nie dowodzi regresji z tego samego powodu. To jest stan gorszy niż bramka za surowa: bramka za surowa mówi prawdę zbyt często, ta nie mówi jej wcale. **Pozycja checklisty premiery** — nie wolno wyjść na produkcję z bramką, która wygląda na działającą i nie jest. | przebieg `32302412113`, `lighthouserc.cjs` (progi), `scripts/reprezentant.mjs`, `scripts/werdykt-po-lcp.mjs`; rodzina: T10, T22(c), T22(d) | **Przed premierą (Faza 7)** bramka wydajności musi mieć regułę werdyktu, przy której rozrzut przyrządu nie przebija zapasu — materiał i cztery kierunki są w przekazaniu rozdz. 8, rekomendacja: **najpierw jeden eksperyment rozstrzygający** (trasa ofiarna na początku listy, ~8 min CI), dopiero potem wybór miary. **Czego nie wolno**: zamknąć tej pozycji przez podniesienie progu — to jest zamiana czerwieni na ciszę (ADR-020, zakaz 3 z części III briefingu). Warunek zamknięcia: przebieg, w którym `/` mieści się pod progiem **i** przyrząd nie zgłasza marginesu pozornego — ten sam warunek co T22, tu podniesiony do rangi blokady premiery. |
| T34 | **Dziesięć zakazów z briefingu przyjęte przez właściciela jako WIĄŻĄCE DLA KAŻDEGO ZLECENIA — także jego własnego — i wchodzi do kanonu bez strażnika.** Rozstrzygnięcie 2026-08-23: „przyjmuję wszystkie, wchodzą do kanonu strony jako wiążące dla każdego zlecenia, także mojego". Zmiana jest realna, nie redakcyjna: dotąd `CLAUDE.md` adresował **agenta** i mówił, czego agentowi nie wolno zrobić; od tego rozstrzygnięcia mówi także, czego **nie wolno zlecić** — a to znaczy, że wykonawca ma obowiązek odmówić wykonania zlecenia właściciela, jeśli łamie któryś z dziesięciu. Właściciel wyróżnił dwa: **zakaz 6** (`curl -i`/`-v`/`-D -` na preview wypisuje `Set-Cookie: _vercel_jwt`, który niesie jawnie wartość Protection Bypass) — jako jedyny o skutku **bezpieczeństwa**, nie porządku; oraz **zakazy 9 i 10** (liczby bez pokrycia, mnożenie źródeł reguł) — z adnotacją „celują we mnie, tak ma być". Stan: zapisane w `CLAUDE.md`. **Strażnika nie ma i nie da się go zrobić w całości** — zakazy 1, 2, 6, 7 są mechanicznie sprawdzalne w treści polecenia, zakazy 3, 8, 9, 10 wymagają oceny zamiaru. To jest ta sama klasa co T25: reguły pilnuje wyłącznie proza. | `CLAUDE.md` (rozdział „Dziesięć zakazów"), `docs/BRIEFING-MIEDZY-SESJAMI.md:167-201` | Do rozstrzygnięcia przez właściciela, czy budować cząstkowego strażnika na tej połowie, którą da się sprawdzić mechanicznie — kandydaci: `--no-verify` i `curl -i`/`-v`/`-D -` na adresie preview w treści commita i skryptów (grep, koszt minutowy), `kill`/`pkill` na porcie 3000. Rekomendacja wykonawcy: **tak dla zakazu 2 i 6, nie dla reszty** — strażnik częściowy jest wartościowy pod warunkiem, że w opisie **jawnie deklaruje, czego NIE sprawdza**; inaczej sam stanie się „narzędziem, które potwierdza poprawność artefaktu" i uśpi czujność co do pozostałych ośmiu. Warunek zamknięcia dla wariantu „nie budujemy": zdanie w `CLAUDE.md` mówiące wprost, że dziesięć zakazów nie ma strażnika i utrzymuje je wyłącznie czytanie. |
| ~~T35~~ **ZAMKNIĘTE 2026-08-23** | **Zlecenie o konwencji walidacji kluczy (dwa poziomy, strażnik, `RECZ-289`) trafiło do sesji `catherly-www`, podczas gdy CAŁY jego przedmiot leży w repozytorium aplikacji.** Pomiar 2026-08-23, oba repozytoria: `REQUIRED_IN_PROD` — **zero** trafień w `catherly-www`, dziesięć plików w `fbo-os/docs`; `RECZ-289` — nieobecne tutaj, w `fbo-os/docs/ZADANIA_RECZNE.md:3749` jako „pięć kluczy bez kontroli kształtu (PRZEPISANE 2026-08-23)", co dokładnie odpowiada kryterium „zapala się dziś na wszystkich pięciu"; „tor 8", `NIEUSTALONE`, `DOTYKA` — zero tutaj, obecne tam; `zod` **nie jest nawet zależnością** `catherly-www`, a `.env` ma **dwa** klucze, nie piętnaście. To **czwarte wystąpienie klasy T27 tego samego dnia** i jej najmocniejsza postać: brakuje nie jednego nazwanego pliku, lecz całego przedmiotu zlecenia. **Zarazem jest to LUSTRZANE odbicie reguły z T26.** Tam dokument był poprawny, a adresat błędny — wysyłka nie została wykonana, właściciel do protokołu: „odmowa właściwa". Tutaj **zlecenie jest poprawne, a adresat błędny, i pomyłka wyszła od właściciela**. Reguła „adresat jest częścią zakresu, nie metadanymi" była zapisana wyłącznie dla strony **wysyłającej** dokument; nie obejmuje przypadku, w którym zlecenie przychodzi pod zły adres, a wykonawca ma pełną techniczną możliwość wykonać je „w przybliżeniu" w niewłaściwym repozytorium — dopisać schemat Zod do projektu bez `zod`, albo po cichu sięgnąć do drugiego repozytorium i zdublować pracę sesji, która ma tę robotę zmierzoną. Kosztu tego drugiego wariantu właściciel nazwał w tym samym zleceniu: **„NIE dubluj"**. | Pomiar 2026-08-23 w obu repo; `fbo-os/docs/ZADANIA_RECZNE.md:3749`; `catherly-www/package.json` (brak `zod`); `catherly-www/.env` = 2 klucze | **(1)** Zlecenie wraca do sesji aplikacji **nietknięte** — wykonane, nic w żadnym repozytorium nie zostało na jego podstawie zmienione. **(2)** Do rozstrzygnięcia przez właściciela: czy reguła zakresu zyskuje drugą stronę — **„zlecenie przychodzące pod zły adres odsyła się, nie wykonuje w przybliżeniu"** — i czy wchodzi do ADR-018 (deklarowanego w `docs/adr/README.md:7` jako obowiązujący **w obu repozytoriach**), czy zostaje pozycją rejestru. Uwaga do decyzji: jeśli wchodzi do ADR-018, to jest to reguła, którą **obie** sesje mogą wyegzekwować wobec zlecenia właściciela — czyli ten sam kształt co zakazy 9 i 10 z T34. **ZAMKNIĘTE 2026-08-23 rozstrzygnięciem właściciela:** reguła wchodzi do **ADR-018 jako punkt 7** — nie zostaje pozycją rejestru, bo rejestr nie jest źródłem reguł (T32). Zapisana **w obu kierunkach**: odbiorca nie wykonuje zlecenia w repozytorium, do którego ono nie należy; nadawca sprawdza adres przed wysłaniem. Właściciel wskazał sam siebie jako stronę, której reguła dotyczy — zlecenie poszło pod zły adres pięciokrotnie. Przedmiot zlecenia jest częścią jego zakresu, więc wykonanie „w przybliżeniu" w niewłaściwym miejscu dubluje pracę i ukrywa fakt, że rzecz leży gdzie indziej. **Reguła nie ma strażnika** — adres zlecenia wymaga oceny zamiaru, tak jak punkty 3, 8, 9 i 10 z T34. |
| ~~T36~~ **ZAMKNIĘTE 2026-08-23** | **Pierwsza w tym rejestrze wada, która idzie w DRUGĄ stronę: bramka szkodząca przez POPRAWNOŚĆ.** Wszystkie trzydzieści pięć wcześniejszych pozycji opisuje mechanizmy, które przepuszczają **za dużo** — zieleń bez pokrycia, cisza zamiast werdyktu, stan zły niewyrażalny. Ta opisuje mechanizm, który może przepuścić **za mało**: strażnik poprawny co do reguły i szkodliwy co do skutku. Sformułowanie przyszło z sesji aplikacji w zleceniu z 2026-08-23 i brzmi: *„Konwencja wymuszająca kształt tam, gdzie dostawca go nie gwarantuje, zablokuje wdrożenie przy pierwszej zmianie formatu po stronie dostawcy."* **Cytat jest międzyrepozytoryjny i NIEZWERYFIKOWANY stąd** — dokładnie ten sam status co odwołanie do `RECZ-161` w T30 i to samo ustalenie T21(5): żadna bramka tego repozytorium go nie sprawdza. Wartość pozycji nie zależy jednak od tamtego cytatu, bo **klasa ma tutaj własne, zmierzone pokrycie w dwóch miejscach**: **(a)** `T33` — gdyby próg 1800 ms utwardzić zamiast uznać za nieinterpretowalny, bramka blokowałaby wdrożenia formalnie poprawnie i merytorycznie bez podstawy, bo przy rozrzucie 593 ms mierzy szum, nie stronę; **(b)** `T34`/decyzja D2 — strażnik dziesięciu zakazów jest z definicji mechanizmem, który może zablokować zlecenie **właściciela**, i jego wartość zależy w całości od tego, czy jawnie deklaruje, czego NIE sprawdza. Rozróżnienie, które trzeba utrzymać przy każdym przyszłym strażniku: **brak kształtu, bo dostawca go zmienił, to inna rzecz niż brak kształtu, bo nikt nie pomyślał** — pierwsze wymaga adnotacji z powodem, drugie jest wadą. | Zlecenie właściciela 2026-08-23 (cytat z sesji aplikacji, **niezweryfikowany stąd**); pokrycie rodzime: `T33`, `T34` | **Nie promuję do kanonu z własnej ręki.** Polecenie „do kanonu, zapisz osobno" przyszło w zleceniu skierowanym do drugiej sesji (`T35`), a promowanie reguły na podstawie polecenia pod złym adresem jest tym samym błędem, co liczba bez pokrycia. Pozycja czeka więc na potwierdzenie właściciela **w tym repozytorium**, po czym wchodzi do ADR-018 jako klasa obok ośmiu istniejących. Do rozstrzygnięcia przy okazji: czy klasa ma żyć w obu rejestrach, czy wyłącznie w kanonie międzyrepozytoryjnym — bo dublowanie jej w dwóch plikach to zakaz 10 z T34. **ZAMKNIĘTE 2026-08-23 potwierdzeniem właściciela w tym repozytorium:** klasa **„BRAMKA SZKODZĄCA PRZEZ POPRAWNOŚĆ"** wchodzi do kanonu `CLAUDE.md`, w rozdziale „Prymat nieodwracalnego", **obok zakazu 10**. Umieszczenie różni się od tego, co ta pozycja przewidywała (`ADR-018`) — decyzja właściciela, `CLAUDE.md` jest w hierarchii T32 szczeblem drugim, więc klasa nadal wiąże każde zlecenie. Treść reguły: **zanim napiszesz strażnika wymuszającego konkretny kształt lub wartość, sprawdź, czy dostawca/kontrakt gwarantuje ten kształt; jeśli nie — strażnik sprawdza OBECNOŚĆ, nie kształt.** Dublowania nie ma: zapis stoi w jednym pliku, rejestr tylko na niego wskazuje. Pokrycie rodzime w tym repozytorium: **T33** (próg wydajności nieinterpretowalny przy obecnym rozrzucie) i **T34** (strażnik zakazów blokujący zlecenie właściciela) — obie to ten sam kierunek wady, więc klasa nie stoi na jednym cudzym cytacie. |
| T37 | **SPRAWDZENIE OBALIŁO USTALENIE, NA KTÓRYM STAŁA DECYZJA — A DECYZJA STOI, BO POWÓD JEST INNY.** Przypadek źródłowy `A-05` w rejestrze przepływu aplikacji (`fbo-os/docs/wdrozenia/REJESTR-PRZEPLYWU.md:49` — cztery wystąpienia klasy „napis zamiast zachowania" w narzędziach audytora, oznaczone tam **PRZYJĘTE BEZ SPRAWDZENIA**, użyte w zestawieniu 4 + 8 = 12). Rozstrzygnięcie właściciela 2026-08-23: ustalenie po sprawdzeniu okazało się nieprawdziwe, decyzja zbudowana na nim **została utrzymana z innego uzasadnienia**. Reguła, którą to ustanawia: **zapisuje się OBA fakty razem, nie jeden zamiast drugiego** — obalenie ustalenia i utrzymanie decyzji. Korekta nie cofa wszystkiego, co na obalonym ustaleniu stało, **ale musi WYMIENIĆ, co dokładnie cofa**. Konstrukcja ta sama co „zjawisko to samo, przynależność inna" z toru 9. Zapisany tu skutek dla tego repozytorium: gdy pomiar obali podstawę pozycji rejestru, pozycja **nie znika i nie zostaje po cichu przepisana** — dostaje dopisek z datą, który mówi, co upadło i co mimo to zostaje w mocy oraz dlaczego. Bez tego dopisku czytelnik trafia na decyzję bez podstawy albo na podstawę bez decyzji, i w obu wypadkach nie wie, czego szukać. | `fbo-os/docs/wdrozenia/REJESTR-PRZEPLYWU.md:49` (**odczyt stąd 2026-08-23, treść ustalenia niezweryfikowana**); rozstrzygnięcie właściciela 2026-08-23 | Reguła obowiązuje od zapisu. Do rozstrzygnięcia osobno: czy dopisek obalający ma nosić własny znacznik w skorowidzu 15.2, żeby dało się go znaleźć bez czytania całego wiersza. |
| T38 | **PROPORCJA 34 : 4 MÓWI O PRZEPŁYWIE, NIE O RZETELNOŚCI POJEDYNCZEGO TORU — I NIE JEST MOIM POMIAREM.** Właściciel 2026-08-23: trzydzieści cztery ustalenia przyjęte bez sprawdzenia przy czterech sprawdzonych, i z tych czterech **jedno obalone** (`A-05`, T37). Dane uzupełniające, **z toru 14, przekazane przez właściciela**: z **19** sprawdzeń wykonanych drugą drogą **11 skończyło się zawężeniem albo rozszerzeniem ustalenia — 58%**. Oszacowanie, które z tego wynika dla trzydziestu czterech pozycji przyjętych bez sprawdzenia, to **około dziewiętnastu**, które po sprawdzeniu brzmiałyby inaczej. **To oszacowanie z CUDZYCH danych i tak jest oznaczone — nie jest pomiarem tego repozytorium.** Zgodne kierunkowo z liczbą własną toru: 1 obalone z 4 sprawdzonych to 25% przy próbce czterokrotnie mniejszej. Rodzina po stronie aplikacji, **niezweryfikowana stąd**: „POMIAR WARTOŚCI FAZY REFUTACJI" (`fbo-os/CLAUDE.md:168-183`) mówi o 29 poprawkach na 80 kontraktów poddanych obaleniu i nazywa typowy kształt błędu — **nie zmyślenie, lecz NADMIERNY ZASIĘG**, teza prawdziwa dla części przypadków podana jako ogólna; refutacja rzadko wyrzuca pozycję, częściej ją **zawęża, i to zawężenie jest całą wartością**. | Właściciel 2026-08-23 ← tor 14 (**dwa ogniwa, drugie niezweryfikowane stąd**); `fbo-os/CLAUDE.md:168-183` (odczyt stąd 2026-08-23) | Nie da się z tego wyprowadzić działania bez rozstrzygnięcia właściciela: **czy 58% jest własnością przepływu**, czy własnością tamtej próbki. Jeśli własnością przepływu — konsekwencją jest kolejność sprawdzania pozycji tego rejestru, a nie ich hurtowe unieważnienie. Do rozstrzygnięcia razem z T39. |
| T39 | **REJESTR LICZY OGNIWA, NIE ŹRÓDŁA — wymóg rozszerzony przez właściciela 2026-08-23 na wszystkie tory.** Ustalenie przejęte od toru, który przejął je od agenta, stoi na **dwóch ogniwach cudzego słowa**, a zapisane jako „od właściciela" wygląda na jedno. Pomiar własny tego rejestru, 2026-08-23: na **36** pozycji **30 wymienia właściciela**, ale wzmianka o właścicielu **nie znaczy, że ustalenie od niego pochodzi** — w części pozycji właściciel rozstrzygnął KIERUNEK, podczas gdy samo ustalenie wyszło z pomiaru w tym repozytorium. To rozróżnienie jest dokładnie tym, po co ten wymóg powstał, więc twierdzenie „25 z 34 pochodzi od właściciela" **zostaje tu odnotowane jako nie do potwierdzenia w tej postaci**: policzalna jest wzmianka, nie pochodzenie. Skorowidz ogniw stoi w rozdziale **„Skorowidz ogniw"** poniżej tabeli. Znane dziś pozycje na **dwóch ogniwach**: **T30** (`RECZ-161`, właściciel ← strona aplikacji), **T36** (cytat o wymuszaniu kształtu, właściciel ← sesja aplikacji) i **T38** (58% z 19 sprawdzeń, właściciel ← tor 14). Na **trzech**: **T37** — `A-05` powstał w torze 8, przeszedł przez rejestr przepływu ze statusem „PRZYJĘTE BEZ SPRAWDZENIA", stamtąd wziął go właściciel; **żadne z trzech ogniw nie niesie sprawdzenia**. Wszędzie tam, gdzie wiem tylko tyle, że ustalenie przyszło od właściciela, a nie wiem, skąd wziął je właściciel, wpisane jest **„dalej nieustalone"** — zgodnie z poleceniem „nie zgaduj". | Pomiar własny 2026-08-23 (36 pozycji, 30 ze wzmianką o właścicielu); wymóg: właściciel 2026-08-23 ← tor 14 | **Odstępstwo od wzoru zgłoszone przed wykonaniem:** tor 14 dodał **kolumnę** pochodzenia, tutaj powstał **osobny skorowidz**. Powód: wiersze tej tabeli mają po kilka tysięcy znaków, a dopisanie piątej kolumny do 36 z nich wymaga przepisania każdego w całości — ryzyko uszkodzenia treści przewyższa zysk, a skorowidz jest sprawdzalny linia po linii. Jeśli właściciel chce kolumny mimo to — do rozstrzygnięcia, wykonalne. |
| T40 | **⚠ JEDYNYM KANAŁEM MIĘDZY TRZEMA OBSZARAMI JEST JEDNA OSOBA I JEDNA WARSTWA DOWODZĄCA — kanon jest wspólny w zamierzeniu i ROZŁĄCZNY W PRAKTYCE.** Pozycja podniesiona przez właściciela 2026-08-23 do rangi ustalenia o architekturze przepływu, nie o rejestrze. Stan zmierzony w tym repozytorium: `CLAUDE.md` **nie zawiera żadnej z klas kanonu aplikacji** — sprawdzone przez porównanie obu plików 2026-08-23. Reguły z aplikacji nie działają tutaj, a reguły stąd nie działają tam, **mimo że opisują te same klasy wad**: obie strony niezależnie doszły do „zielony strażnik jest hipotezą, dopóki nie zapłonie" i obie niezależnie odkryły klasę „narzędzie potwierdza poprawność artefaktu, którego nie da się użyć" (`RECZ-286` tam, T30 i klasa „odwołanie do stanu, który przestał istnieć" tutaj). Koszt tej rozłączności jest już policzony w T30: przyczyna była **znana w drugim repozytorium** i nie przeniosła się przez granicę, więc każda sesja odkrywa ją osobno i za każdym razem od zera. Konsekwencja, którą trzeba widzieć wprost: **kanał ma jeden punkt awarii** — jeśli właściciel czegoś nie przeniesie, nie przeniesie tego nic innego, bo żadna bramka po żadnej stronie nie sprawdza odwołań międzyrepozytoryjnych (T21 ustalenie 5). | Porównanie `catherly-www/CLAUDE.md` z `fbo-os/CLAUDE.md`, odczyt stąd 2026-08-23; pełna lista różnic: **przekazanie, rozdz. 19** | Lista klas kanonu aplikacji **bez odpowiednika** na stronie wykonana i zapisana (przekazanie, rozdz. 19) — **bez przenoszenia**, zgodnie ze zleceniem: przeniesienie jest decyzją właściciela i wymaga sprawdzenia, czy klasa w ogóle dotyczy treści. Do rozstrzygnięcia po przeczytaniu listy: które klasy przechodzą, w jakiej formie i do którego pliku, żeby nie naruszyć zakazu 10 (mnożenie źródeł reguł). |
| T41 | **Cztery akcje CI działają dziś na środowisku, którego nie deklarują — a jedynym sygnałem jest ostrzeżenie, którego nikt nie czyta.** Zmierzone 2026-08-23 na przebiegu `32663550392` (`d7a2fe3`, osiągalny): w **każdym z 15 zadań** runner wypisuje *„Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24: actions/checkout@v4, actions/download-artifact@v4, actions/setup-node@v4, actions/upload-artifact@v4”* — **15 wystąpień w jednym przebiegu**. Wszystkie cztery akcje są w workflow przypięte na `v4` (`checkout` ×15, `setup-node` ×15, `download-artifact` ×7, `upload-artifact` ×2). Dostawca zmienił środowisko wykonania **pod nami, bez naszej zmiany** i utrzymuje zgodność wstecz dobrowolnie — do dnia, w którym przestanie. Wtedy padnie nie jedna bramka, tylko **wszystkie piętnaście naraz**, bo `checkout` i `setup-node` stoją w każdym zadaniu. **To jest podwójny przypadek klas już nazwanych w kanonie**, i dlatego trafia do rejestru, a nie do kodu: **(a)** „raport, którego nikt nie czyta” — ostrzeżenie stoi w logu od nieznanej daty, w piętnastu kopiach na przebieg, i zostało zauważone dopiero dlatego, że ktoś wszedł do logu w zupełnie innej sprawie (mierzenie czasów zadań do T24); **(b)** „strażnik może zerodować przez zmianę OTOCZENIA, bez zmiany własnego kodu” — nasze bramki nadal są zielone, a podłoże pod nimi już się zmieniło. Czego ta pozycja **nie** twierdzi: że coś jest dziś zepsute. Wszystkie 15 zadań przeszło na Node 24 bez błędu — pozycja opisuje **ryzyko z datą wygaśnięcia w cudzych rękach**, nie usterkę. **CO KONKRETNIE MOŻE SIĘ ZEPSUĆ — dopisane na polecenie właściciela 2026-08-24, bo bez tego pozycja mówi „jest ryzyko" i nie mówi nic:** akcja celująca w Node 20, uruchamiana na runnerze z Node 24, **może przestać działać przy dowolnej aktualizacji tej akcji — bez żadnej zmiany po naszej stronie**. Nie potrzeba do tego decyzji GitHuba o wyłączeniu zgodności ani naszego commita; wystarczy, że dostawca akcji wypuści wydanie, które zakłada Node 20. Ponieważ `checkout` i `setup-node` stoją w **każdym** z 15 zadań, awaria nie będzie punktowa — **padnie cała bramka naraz**, w dniu, w którym nikt niczego u nas nie tknął. Właściciel zakwalifikował to wprost: **pozycja przedpremierowa, nie higiena** — czyli wchodzi do checklisty Fazy 7 obok T33, a nie do „kiedyś przy porządkach". | `.github/workflows/bramki.yml` — 39 wystąpień `uses: actions/*@v4`; log przebiegu `32663550392` (`d7a2fe3`), odczyt `gh run view --log` 2026-08-23 | **Nie naprawiam — defekt spoza zakresu zlecenia (zakaz 8).** Do rozstrzygnięcia przez właściciela dwie rzeczy, w tej kolejności. **(1) Czy podnosić akcje do `v5`** — to jest zmiana dostawcy w kodzie bramek, więc wymaga własnego zadania i własnego dowodu, że po podniesieniu wszystkie 15 zadań daje te same werdykty co przed (kontrola negatywna w tym samym przebiegu, łącznie z tą samą czerwienią `Nieodwracalne`). Podniesienie „przy okazji” jest zabronione i byłoby zmianą, której nikt nie zamawiał. **(2) Czy ostrzeżenia DOSTAWCY mają mieć swoje miejsce w interfejsie** — dziś nie mają żadnego, więc następne takie ostrzeżenie zostanie zauważone równie przypadkowo. To ten sam brak, który T24 nazywa przy naszym własnym `::warning`, i **rozstrzygać go trzeba raz, dla obu naraz**, a nie osobno. Warunek zamknięcia: przebieg, w którym ostrzeżenie dostawcy o zmianie środowiska trafia w miejsce czytane bez otwierania logu — albo jawny zapis, że świadomie tego nie robimy i dlaczego. |
| T42 | **⚠ HAK, KTÓRY MIAŁ ROBIĆ BACKUPY, NIE ISTNIEJE — a `CLAUDE.md` twierdzi, że istnieje.** Zmierzone 2026-08-24 po trzech dobach bez migawki. `CLAUDE.md:239-241` mówi: *„Po każdym ukończonym zadaniu uruchamia się scripts/backup.sh… **hook Stop w ustawieniach Claude Code robi to automatycznie**"*. Sprawdzenie: **klucz `hooks` nie występuje w ŻADNYM pliku konfiguracji** — `~/.claude/settings.json` (0 trafień), `~/.claude.json` (0), `.claude/settings.local.json` (brak klucza), `.claude/settings.json` i `~/.claude/settings.local.json` (pliki nie istnieją), `managed-settings.json` (nie istnieje). Ciąg `backup.sh` **nie pada w żadnej konfiguracji**. To nie jest hak zepsuty ani wyłączony — **haka nie ma**. Skutek zmierzony: ostatnia migawka przed dzisiejszą to `catherly-www-2026-08-20-2302.zip`, czyli **przerwa 20.08 22:02 → 24.08 08:58**, obejmująca całą pracę z 23.08 (pięć rozstrzygnięć, trzy decyzje, T41). Skrypt jest sprawny: uruchomiony ręcznie 2026-08-24 dał `catherly-www-2026-08-24-0858.zip` (9,0 MB), kod wyjścia **0**, `unzip -t` → *„No errors detected"*. **To jest najcięższy przypadek klasy „brak dowodu = brak zabezpieczenia" w tym rejestrze**, i to z dwóch powodów naraz. **(1)** Zabezpieczenie było uznane za działające przez **ponad 200 migawek**, więc nikt go nie sprawdził — a gdy przestało powstawać, **nie było żadnego sygnału**: cichy brak backupu wygląda dokładnie jak backup, którego nie było potrzeby robić. **(2)** Fałszywe twierdzenie stoi w `CLAUDE.md`, czyli na **drugim szczeblu hierarchii** — dokument najwyższej rangi operacyjnej opisuje mechanizm, którego nie ma, i każe każdej sesji na niego liczyć. Sesja czytająca kanon dowiaduje się, że backup dzieje się sam, więc go nie robi. **Czego ta pozycja NIE ustala:** czy hak kiedykolwiek istniał i został usunięty, czy migawki do 20.08 powstawały z ręcznych uruchomień w sesjach — **nieustalone, nie zgaduję**. **ROZRÓŻNIENIE, NA KTÓRE NALEGAŁ WŁAŚCICIEL (2026-08-24) — to jest NOWA odmiana klasy, nie kolejny jej przypadek.** Dotąd rejestr opisywał **napis zamiast zachowania**: strażnik istniał, ale nie mierzył tego, co deklarował (zielona bramka nieczuła na defekt, asercja wygasła przez zmianę otoczenia, ustawienie o nazwie reguły werdyktu). Tutaj jest **napis zamiast MECHANIZMU**: nie ma czego mierzyć, bo nie ma czego uruchomić — a dokument twierdzi, że jest. I twierdzi to **w kanonie, na drugim szczeblu hierarchii**, czyli w pliku, który każda sesja czyta na starcie, zanim cokolwiek zrobi. Różnica jest praktyczna, nie nazewnicza: napis zamiast zachowania wykrywa **mutacja** (psujesz to, czego strażnik pilnuje, i patrzysz, czy zapali); napisu zamiast mechanizmu mutacja **nie wykryje wcale**, bo nie ma czego mutować — wykrywa go dopiero **odczyt konfiguracji**, czyli pytanie „czy ta rzecz w ogóle istnieje", zadane osobno od pytania „czy działa". Wniosek wiążący: **zdanie „mechanizm X robi to automatycznie" ma status niesprawdzony, dopóki nie odczytasz konfiguracji** — także gdy widzisz jego rzekome skutki. Skutek na dysku dowodzi, że **coś** je tworzyło; nie dowodzi, że tworzył je **ten** mechanizm. **SPECYFIKACJA HAKA — przygotowana 2026-08-24 na polecenie właściciela, DO BUDOWY PRZEZ WŁAŚCICIELA, nie przez sesję** (konfiguracja leży poza repozytorium). **Co ma robić:** po zakończeniu zadania uruchomić `bash scripts/backup.sh` w katalogu repozytorium i przechwycić jego kod wyjścia oraz wyjście tekstowe. **Warunek zapłonu:** zakończenie zadania (`Stop`), niezależnie od tego, czy zadanie skończyło się sukcesem — kopia po zadaniu nieudanym jest warta tyle samo, a może więcej. **GŁOŚNY PRZY PORAŻCE — warunek obowiązkowy, bez niego haka nie budować.** Kod wyjścia różny od zera musi dać komunikat widoczny dla właściciela **bez otwierania logu sesji**; hak, który przy porażce wpisuje linijkę do logu, odtwarza dokładnie stan, który tę pozycję otworzył, i jest wprost klasą „raport, którego nikt nie czyta". Sukces raportuje się krótko (nazwa pliku + rozmiar); porażka musi **przerwać ciszę**, nie tylko ją opisać. **Czego hak NIE ma robić:** nie ma ukrywać porażki, nie ma ponawiać po cichu i nie ma zastępować obowiązku raportowania wyniku przez sesję — dopóki nie jest sprawdzony, sesja raportuje tak samo jak dziś. **KRYTERIUM PRZYJĘCIA — MUTACJA, NIE ZAPŁON** (`B-17` z toru 8, właściciel 2026-08-24): *„zapłon dowodzi, że umie zadziałać na tym wejściu; mutacja dowodzi, że reaguje, gdy zniknie zachowanie"*. Zapłon (dysk podpięty → powstała migawka) **nie wystarcza** — pokazuje wyłącznie ścieżkę szczęśliwą. Mutacja: **odłącz dysk albo wskaż nieistniejący katalog docelowy, zakończ zadanie i pokaż, że porażka jest SŁYSZALNA**; następnie przywróć i pokaż zieleń — obie obserwacje **w tym samym przebiegu**, jako kontrola negatywna. Bez tej pary hak ma status NIESPRAWDZONE, czyli liczy się jak niedziałający, i **nie wolno na jego podstawie skreślać obowiązku ręcznego uruchomienia**. | `CLAUDE.md:239-241` (twierdzenie); `~/.claude/settings.json`, `~/.claude.json`, `.claude/settings.local.json` (odczyt 2026-08-24 — zero trafień na `hooks`); katalog `/Volumes/Extreme SSD/Catherly-www-ZIP` (przerwa w migawkach) | **Zgłoszone na polecenie właściciela 2026-08-24, bez naprawy** — polecenie brzmiało „zgłoś jako pozycję", a założenie haka w cudzej konfiguracji jest zmianą poza repozytorium. Do rozstrzygnięcia **dwie rzeczy, i żadna nie zastępuje drugiej**. **(1) Czy budować hak** — jeśli tak, to razem z warunkiem, którego dziś nie ma po żadnej stronie: **hak musi być głośny przy porażce**. Hak, który cicho nie zadziałał, jest tą samą klasą wady, co wszystko w tym rejestrze; hak zgłaszający porażkę tylko do logu sesji to „raport, którego nikt nie czyta". **(2) Co zrobić ze zdaniem w `CLAUDE.md`** — dopóki hak nie istnieje, to zdanie jest **fałszem w źródle reguł drugiego szczebla** i działa aktywnie na szkodę: usypia dokładnie tę czujność, którą kanon każe mieć. Dwie drogi: wykreślić je i zostawić wyłącznie obowiązek ręcznego uruchomienia z raportem, albo zbudować hak i **zweryfikować go mutacją** (odłączony dysk → głośna porażka; dysk podpięty → migawka i zielony raport). **Warunek zamknięcia:** albo mutacja pokazująca, że hak zgłasza porażkę słyszalnie, albo `CLAUDE.md` bez zdania o automacie. Do tego czasu **każda sesja uruchamia `scripts/backup.sh` ręcznie i raportuje wynik** — i to jest jedyne działające zabezpieczenie, jakie tu dziś jest. |
| ~~T43~~ **ZAMKNIĘTE 2026-08-24** | **Migawka backupu nie zawiera pliku ŚLEDZONEGO w gicie — wykryte pierwszym w historii sprawdzeniem przez ODTWORZENIE, nie przez sumę.** Zmierzone 2026-08-24 na archiwum `catherly-www-2026-08-24-0910.zip`: rozpakowanie do katalogu tymczasowego, `git log` w odtworzonym repozytorium → historia pełna, `HEAD` = `74fdfe8` (osiągalny), `git fsck` → wyłącznie `dangling commit`, czyli **żadnego uszkodzenia obiektów**. Ale `git status` w odtworzonym repo pokazuje **`D .env.example`** — plik **śledzony** (`git ls-files` potwierdza) i **nieobecny w archiwum**. Przyczyna: `scripts/backup.sh:52-53` wyklucza `-x ".env"` **oraz** `-x ".env.*"`, a drugi wzorzec łapie przy okazji `.env.example`. **Wykluczenie robi coś słusznego i nie wolno tego zepsuć:** sprawdzenie zawartości archiwum (same nazwy, nigdy wartości) potwierdza, że **żaden plik `.env*` nie trafił na SSD** — czyli sekrety `VERCEL_TOKEN` i `STRIPE_TEST_SECRET_KEY` **nie są kopiowane na dysk zewnętrzny**. To jest przeciwieństwo ustalenia z drugiego repozytorium, które kazało nam to sprawdzić. Wada jest wąska: **wzorzec szerszy niż zamiar** — dokładnie rodzina **T20** („zakres wyjątku lintera tokenów jest SZERSZY, niż mówi jego własna dokumentacja"). **Czego ta pozycja NIE twierdzi:** że odtworzenie jest niemożliwe. `.git` jest w archiwum i kompletny, więc `git checkout -- .env.example` odzyskuje plik; archiwum jest **pełnym repozytorium**, ale **nie wiernym odbiciem drzewa roboczego**. Znaczenie jest jednak większe niż sam brakujący plik: **pierwsze sprawdzenie odtworzeniem od razu znalazło rozjazd, którego półtora miesiąca sum kontrolnych nie mogło znaleźć** — bo suma dowodzi, że plik się nie zepsuł, a nie że da się z niego wrócić do tego, co się miało. **SKUTEK PRAKTYCZNY, NAZWANY PRZEZ WŁAŚCICIELA 2026-08-24 — groźniejszy niż sam brakujący plik.** Odtworzenie z takiego archiwum daje repozytorium, w którym `git status` pokazuje **zmianę, której nikt nie wprowadził**. Człowiek odtwarzający po awarii — czyli w jedynej sytuacji, w której ktokolwiek sięga po backup, i w najgorszym możliwym momencie na zagadki — zobaczy `D .env.example` i **uzna, że sam skasował ten plik**. Dalej są dwie drogi i obie złe: albo zacznie szukać nieistniejącej pomyłki, albo zacommituje usunięcie, utrwalając w repozytorium defekt archiwum. Backup ma **usuwać niepewność**, a ten ją dokłada dokładnie w chwili, gdy jest najdroższa. **PARA „TA SAMA REGUŁA CHRONI I SZKODZI" — zapisana jako jedna rzecz, nie dwie, na polecenie właściciela.** Wzorzec `-x ".env.*"` z `scripts/backup.sh:53` robi **obie te rzeczy naraz**, i to jest cała treść tej pozycji: **chroni** — żaden plik `.env*` nie trafia na SSD, więc `VERCEL_TOKEN` i `STRIPE_TEST_SECRET_KEY` nie są kopiowane na dysk zewnętrzny (sprawdzone 2026-08-24 wykazem **nazw**, nigdy wartości); **szkodzi** — ten sam wzorzec wycina śledzony `.env.example`. Nie da się poprawić jednej strony, nie dotykając drugiej, i **dlatego nie wolno rozbijać tego na dwie pozycje**: rozbite, każda z osobna ma oczywiste rozwiązanie, a razem mają jedno trudne. **USTALENIE ODWROTNE NIŻ PO STRONIE APLIKACJI, ze źródłem:** tam **75 archiwów** przeszło weryfikację niosąc **żywe klucze** — `RECZ-287`, repozytorium aplikacji; **dwa ogniwa, drugie stąd niesprawdzalne**, bo to inne repozytorium. **Tu wykluczenie działa.** Zapisuję oba kierunki razem, żeby nikt nie przeniósł stamtąd wniosku „archiwa niosą sekrety" ani stąd wniosku „wykluczanie `.env*` jest bezpieczne" — pierwsze jest u nas nieprawdą, drugie kosztowało śledzony plik. **Właściciel odnotował to jako pierwsze w tym repozytorium przekazanie międzyrepozytoryjne z JAWNYM ZAKAZEM PRZENIESIENIA W OBIE STRONY** — dotąd oznaczaliśmy pochodzenie ustalenia (`P-22`, liczba ogniw), ale nie zakazywaliśmy wprost wnioskowania w którąkolwiek stronę. Różnica jest praktyczna: oznaczenie mówi „to jest cudze i niesprawdzone”, zakaz mówi **„a konkretnie TEGO wniosku nie wyciągaj”** — i dopiero to drugie zatrzymuje kogoś, kto już uwierzył. **POTWIERDZENIE PRZEZ POWTÓRZENIE, nie uciążliwość (właściciel, 2026-08-24).** Defekt pokazał się identycznie przy **trzech** niezależnych migawkach tego samego dnia (`…-0910`, `…-1133`, `…-1143`): za każdym razem `git status` odtworzonego repozytorium daje dokładnie `D .env.example`, ani mniej, ani więcej. To nie jest powracająca niedogodność, tylko **dowód, że opis pozycji jest poprawny** — defekt zachowuje się dokładnie tak, jak zapisano, przy powtórzeniach, których nikt nie stroił pod wynik. Gdyby przy którejś migawce zniknął albo urósł, opis byłby niepełny i to trzeba by zapisać w pierwszej kolejności. **Trzy klasy kanonu, dla których ta pozycja jest wzorcem naraz:** „defekt kopii utrwalany przy odtwarzaniu”, „dwie strony jednego wzorca rozbite na dwie pozycje dają dwa łatwe rozwiązania, które się wykluczają” oraz „dowody wartości reguły zapisuje się razem, nie osobno”. **DWA NIEZALEŻNE DOWODY WARTOŚCI REGUŁY O ODTWORZENIU, TEGO SAMEGO DNIA, W KTÓRYM REGUŁA POWSTAŁA.** Reguła „weryfikuj odtworzeniem, nie sumą" weszła do `CLAUDE.md` rano 2026-08-24. Tego samego dnia ma **dwa** policzalne potwierdzenia, z dwóch niezależnych stron: **(1)** `RECZ-287` — półtora miesiąca weryfikacji samą sumą i 75 archiwów przyjętych z żywymi kluczami (cudzy pomiar); **(2)** ta pozycja — pierwsze sprawdzenie odtworzeniem po tej stronie, defekt znaleziony **przy pierwszym użyciu** (pomiar własny). Zapisane razem, bo osobno każde wygląda na przypadek, a razem pokazują, **czego suma kontrolna nie widzi z definicji**: ani tego, co w archiwum jest nadmiarowo, ani tego, czego brakuje — wyłącznie to, że bajty się nie zmieniły. | `scripts/backup.sh:52-53` (wzorzec `-x ".env.*"`); `.env.example` w `git ls-files`; odtworzenie archiwum `catherly-www-2026-08-24-0910.zip` 2026-08-24, `HEAD` odtworzony = `74fdfe8` | **Nie naprawiam — defekt spoza zakresu zlecenia (zakaz 8).** Zlecenie brzmiało „sprawdź backup odtworzeniem", nie „popraw skrypt". Do rozstrzygnięcia przez właściciela: czy zawęzić wzorzec (np. wyliczyć `.env.local`, `.env.production` zamiast `.env.*`), czy dopisać jawny wyjątek na pliki śledzone. **Warunek, którego nie wolno przy tym zgubić — i to jest cała trudność:** poprawka **nie może wpuścić do archiwum żadnego pliku z sekretami**. Wzorzec zawężony ręcznie przestaje chronić przed plikiem `.env` o nazwie, której nikt nie przewidział; wyjątek „wpuść to, co śledzone" jest bezpieczniejszy, bo git nie śledzi plików z sekretami — ale **stoi na założeniu, że nikt nigdy nie doda sekretu do repozytorium**, a to założenie należy do obszaru nieodwracalnego i wymaga własnego dowodu. **Warunek zamknięcia:** odtworzenie archiwum powstałego po poprawce, w którym `git status` odtworzonego repozytorium jest **czysty**, a wykaz zawartości archiwum nadal **nie zawiera żadnego `.env*` z sekretami** — obie rzeczy w tym samym sprawdzeniu, bo osobno każda z nich da się spełnić kosztem drugiej. **ZAMKNIĘTE 2026-08-24 — WŁAŚCICIEL ZDJĄŁ ZAKAZ 8 PUNKTOWO, NAPRAWA WYKONANA I UDOWODNIONA.** Powód zdjęcia zakazu: koszt odraczania był **czynny i mierzalny** — cztery wadliwe migawki od wykrycia, kolejne co godzinę, przy przyczynie zdiagnozowanej i naprawie na kilka linii. Z tego wzięła się **granica zakazu 8** dopisana do kanonu: *zakaz naprawiania przy okazji nie obejmuje defektu, który produkuje nowe wadliwe artefakty w trakcie odraczania.* **NAPRAWA (commit `18c03f2`, tylko `scripts/backup.sh`, na warunek właściciela o pojedynczym pliku):** kolejność jest tu całą treścią — **najpierw wyklucz wszystko** (`-x ".env" -x ".env.*"`), **potem dołóż z powrotem jedną nazwę** z imiennej listy `DOZWOLONE_ENV`. Odwrotnie, przez zawężenie wzorca wykluczenia, nowy plik `.env.cokolwiek` z sekretami trafiłby na SSD i nikt by tego nie zauważył. **Domyślnie odmawiamy; wyjątek jest imienny.** Dołożony **strażnik dryfu**: jeśli git śledzi jakikolwiek `.env*` spoza listy, skrypt wypisuje ostrzeżenie na `stderr` zamiast pominąć plik po cichu — cichy brak był dokładnie tą wadą, którą ta pozycja opisuje. **WARUNEK ZAMKNIĘCIA SPEŁNIONY W JEDNYM PRZEBIEGU, obie strony naraz** (migawka `catherly-www-2026-08-24-1318.zip`, `HEAD` odtworzony `18c03f2`, osiągalny): **strona A — chroni:** wykaz `.env*` w archiwum to dokładnie `.env.example` i nic więcej; **kontrola negatywna:** `.env` **istnieje lokalnie** (2 linie) i w archiwum go **nie ma** — bez tego sprawdzenia „brak `.env` w archiwum” nie dowodziłby niczego, bo mógłby wynikać z nieistnienia pliku. **Strona B — nie gubi:** `git status` odtworzonego repozytorium **czysty**, `git fsck` bez uszkodzeń. **STRAŻNIK DRYFU UDOWODNIONY MUTACJĄ, nie zapłonem** (pytanie 2 z zestawu, `B-17`), w izolowanym klonie z przekierowanym katalogiem docelowym, żeby nie tknąć prawdziwych migawek: **mutacja** — `git add -f .env.local` → ostrzeżenie wypisane, plik **nie** trafił do archiwum (`.env*` w archiwum nadal tylko `.env.example`); **kontrola negatywna w tym samym przebiegu** — cofnięcie mutacji → ostrzeżenia **brak**. Katalog mutacji skasowany po sprawdzeniu. **STRAŻNIK DRYFU WSZEDŁ POZA ZLECENIEM — odnotowane, bo kolumna „poza zleceniem” obowiązuje także dla rzeczy dobrych (właściciel, 2026-08-24).** Zlecenie brzmiało: *„popraw wzorzec w `backup.sh` tak, żeby wykluczał `.env` i `.env.*` z wyjątkiem `.env.example`"* — i tyle. Strażnik dryfu **nie mieścił się w literze** tego zdania; mieścił się w jego **warunku** (jeden plik, jeden commit) i przeszedł test konieczności: zamienia ciche pominięcie w głośny sygnał, czyli usuwa dokładnie tę własność, która tę pozycję otworzyła. Właściciel utrzymał go **po fakcie**. **Zapisane także jako uchybienie wykonawcy:** dodatek został wprowadzony **bez oznaczenia go jako wykraczającego poza zlecenie** — wykrył to właściciel, nie sesja. W tej samej dobie ta sama sesja **odmówiła** podniesienia limitu `bramka-pelny-zestaw` z 10 na 20, powołując się na „rozszerzenie zgody poza literę". **Ta sama reguła zastosowana w jedną stronę i pominięta w drugą** — i pominięta akurat tam, gdzie rozszerzenie wypadło korzystnie. To jest asymetria warta zapamiętania: **rozszerzenie zakresu łatwiej zauważyć, gdy szkodzi, niż gdy pomaga**, a reguła o zakresie nie zna tej różnicy. |
| ~~T44~~ **ZAMKNIĘTE 2026-08-24** | **Zadanie CI z toru 9 nie da się przenieść w żadnym z dwóch dostępnych kształtów — kanon strony i kanon toru 9 rozchodzą się co do bramki ŻÓŁTEJ.** Zmierzone 2026-08-24 przy imporcie `tor9/redakcja-import` @ `924b580`. Tor 9 przysyła zadanie **`bramka-deklaracje`** (+32 linie w `bramki.yml`) w kształcie **ŻÓŁTYM**: `continue-on-error: true`, z uzasadnieniem zapisanym w komentarzu zadania — bramka blokująca w dniu wprowadzenia zatrzymałaby każdy commit, także dokumentacyjny, czyli *„uczyłaby, jak ją obchodzić"*. Uzasadnienie jest trafne i zmierzone: uruchomiony u nas `node scripts/lint-deklaracje.mjs` wychodzi **CZERWONY na 10 naruszeniach** (pokrycie: 230 deklaracji w 3 językach; cisza, nie zieleń: 782 wystąpienia bez deklaracji, 79 kluczy nieobecnych w plikach treści, 116 poniżej progu 12 znaków). **Ale `continue-on-error` jest po tej stronie ZAKAZANY wprost** — zakaz 3 wymienia go z nazwy jako „zamianę czerwieni na ciszę", a ADR-020 mówi, że czerwień uzasadniona też jest czerwienią. Zostają więc dwa kształty i **oba łamią coś istotnego**: żółty łamie zakaz 3, czerwony wprowadza bramkę czerwoną w dniu wejścia, na defektach warstwy treści, których naprawa nie należy do tego zlecenia. **Trzeciego kształtu nie ma** i wymyślanie go po cichu byłoby rozstrzyganiem rozjazdu między dwoma kanonami — czego robić nie wolno. | `docs/redakcja/ZWROTKA-KANON-DO-WWW.md` (część 2, pkt 1–2); `scripts/lint-deklaracje.mjs` i `content/deklaracje-zlozone.json` — **przeniesione**, commit grupy 1; treść zadania odczytana z `tor9/redakcja-import:.github/workflows/bramki.yml` | **Zadania NIE przeniesiono; `bramki.yml` i `package.json` nietknięte** (grupy 4 i 5 zlecenia WWW/024). Do rozstrzygnięcia przez właściciela **jedna rzecz, nie dwie**: czy zakaz 3 dopuszcza **bramkę wchodzącą jako raportująca z zapisanym warunkiem przejścia na czerwoną** — bo taki kształt tor 9 przysyła i takim kształtem posługuje się u siebie. Jeśli tak, zakaz 3 wymaga wyjątku zapisanego w kanonie, nie w zadaniu. Jeśli nie, do rozstrzygnięcia druga droga: naprawić 10 rozjazdów deklaracji **przed** wprowadzeniem bramki — to zmiana w warstwie treści, więc osobne zlecenie. **Czego nie wolno przy żadnej z dróg:** wprowadzić zadania i ustawić progu tak, żeby świeciło na zielono przy istniejących naruszeniach. Warunek zamknięcia: zadanie w `bramki.yml` w kształcie zgodnym z zakazem 3 **i** przebieg, w którym jego werdykt jest interpretowalny. **ROZSTRZYGNIĘTE I WDROŻONE 2026-08-24 (`WWW/026`) — TRZECI KSZTAŁT ISTNIEJE: ZAPADKA.** Rozjazd dwóch kanonów rozstrzygnął właściciel, nie okno — odmowa wymyślenia kształtu po cichu była właściwa i to jest odnotowane po obu stronach. **Kształt: bramka z progiem odniesienia.** Próg leży w `scripts/deklaracje-baseline.json` (dziś **10**, pomiar 2026-08-24, commit `e906013`); zadanie jest **czerwone wyłącznie przy naruszeniach ponad próg**. Zero `continue-on-error` — **zakaz 3 nietknięty**; zielona dziś — **nie uczy obchodzenia**; jedenaste naruszenie zapala czerwień **od pierwszego dnia**. **Próg chodzi wyłącznie w dół i schodzi w tym samym commicie co naprawa**, a spadek liczby naruszeń poniżej progu **też jest czerwienią** — bez tego próg zostałby zapasem, w który wolno wrócić: ktoś naprawia dwa rozjazdy, ktoś inny dokłada dwa nowe, a bramka milczy. **Skryptu `lint-deklaracje.mjs` NIE zmodyfikowano** — przyszedł z toru 9 i wróci tam przy następnym imporcie, więc zapadka jest **opakowaniem** (`scripts/bramka-deklaracje.mjs`), nie zmianą cudzego pliku; rozjazd kopii kosztowałby więcej. Gdy linter kończy się niezerowo, a liczby naruszeń nie da się odczytać z jego wyjścia, zapadka daje **czerwień, nigdy ciszę**. **DOWÓD MUTACYJNY Z KONTROLĄ NEGATYWNĄ, jeden przebieg, 2026-08-24:** **(a) regres** — zepsuta deklaracja poprawna (`content/pl/obawy.md:24`, `48 zn` → `47 zn`) dała **11 naruszeń przy progu 10 → CZERWIEŃ, kod 1**; **(b) kontrola negatywna** — cofnięcie mutacji przez `git checkout HEAD --` (nie z indeksu, zgodnie z przeniesioną lekcją) dało **10 naruszeń → ZIELEŃ, kod 0**, `git status` czysty; **(c) druga gałąź zapadki** — próg podniesiony do 11 przy 10 naruszeniach dał **„POSTĘP NIEZAPISANY" → CZERWIEŃ, kod 1**, po czym próg przywrócono. **KOLEJKA NAPRAW — dziesięć pozycji do zdjęcia; zapadka bez kolejki byłaby konserwowaniem długu.** Wypisane imiennie w `scripts/deklaracje-baseline.json`: `content/en/cennik.md` — `Cennik.plany.starter.pozycja2` (65 wobec 57) · `content/pl/definicja.md` — `Definicja.tresc` (275 wobec 256) · `content/pl/filary.md` — `Filary.filar1.konkret1` (71/68), `filar1.konkret2` (74/73), `filar1.konkret3` (67/66), `filar2.konkret2` (65/66), `filar3.konkret1` (75/72), `filar3.konkret3` (60/63) · `content/pl/naglowek.md` — `Hero.naglowek` (62 wobec 58) · `content/pl/obawy.md` — `Obawy.p1` (41 wobec 42). **To warstwa treści**, więc naprawa nie należy do tego zlecenia i idzie razem z redakcją tras, w których te ciągi siedzą — a wtedy korekta obejmuje **cały plik**, nie pozycję. ~~**Pozycja pozostaje otwarta**: warunek zamknięcia wymaga przebiegu CI.~~ **ZAMKNIĘTE 2026-08-24 — WERDYKT Z CI JEST.** Przebieg **`32754129833`** na commicie **`d623c49`** (osiągalny), zadanie **`97517630405`**, konkluzja **`success`**. I to jest werdykt **interpretowalny**, nie samo „success" — log niesie: *naruszeń teraz: 10 · próg (baseline): 10 · werdykt: ZIELONA — nie jest gorzej niż było*, a pod spodem ostrzeżenie, żeby tej zieleni nie czytać szerzej: *10 rozjazdów NADAL STOI*. Obie liczby stoją obok werdyktu, więc czytający nie może wziąć zieleni za „deklaracje są poprawne". **Trzeci kształt zadziałał w CI dokładnie tak, jak lokalnie.** **Co zostaje po zamknięciu i NIE jest tą pozycją:** dziesięć rozjazdów w warstwie treści, wypisanych imiennie w `scripts/deklaracje-baseline.json` — kolejka napraw idzie z redakcją tras, a każde jej zejście obniża próg **w tym samym commicie**. |
| T45 | **`node_modules` i `.next` zniknęły z drzewa roboczego w trakcie sesji — przyczyna NIE po stronie sesji.** Zmierzone 2026-08-24: `npm run lint` dawał kod **0** jeszcze o ~15:2x, a o 17:5x kończy się kodem **127** (`sh: eslint: command not found`); `node_modules` **nie istnieje** (0 pakietów), `.next` **nie istnieje**, przez co `bramka:kotwice` świeci czerwono komunikatem „Brak zbudowanej strony". **Wszystkie pliki ŚLEDZONE są nietknięte** — `git status` pokazuje wyłącznie własną edycję sesji. Oba katalogi są w `.gitignore`, więc git ich zniknięcia nie widzi i **żaden strażnik tego nie zgłasza**. Sprawdzone i wykluczone jako przyczyny: hooki (`grep` po `.githooks/` — zero poleceń kasujących), `scripts/backup.sh` (kasuje wyłącznie na dysku docelowym, `rm -f "$CEL/$PLIK"`), brak miejsca (18 GiB wolnego). `mtime` katalogu repozytorium: **2026-08-24 15:47:42**, czyli poza jakimkolwiek poleceniem tej sesji. **Czego ta pozycja NIE ustala:** kto i czym usunął katalogi — nieustalone, nie zgaduję. Dysk jest zajęty w **92%**, więc usunięcie mogło być celowym sprzątaniem właściciela. | `node_modules/` i `.next/` — nieobecne, odczyt 2026-08-24; `.gitignore:7-8`; `npm run lint` → 127; `bramka:kotwice` → czerwona | **`npm ci` NIE URUCHOMIONE** — odtworzenie zależności to kilkaset megabajtów na dysku zajętym w 92%, a usunięcie mogło być celowe (zakaz 7 w duchu: za stanem środowiska może stać praca właściciela). Do rozstrzygnięcia: czy odtwarzać zależności. **Skutek na dziś, zapisany jawnie:** dwie bramki są **niewykonalne lokalnie** — `lint` (eslint) i `kotwice` (wymaga builda). Commity tej sesji przeszły wyłącznie przez bramki hooka (`tokeny`, `liczby`), które działają na czystym `node`. **Dziedzina niewykonanych bramek nie pokrywa się z treścią tych commitów** (zmiany w `.md` i dwa pliki przeniesione bez modyfikacji), ale **to jest argument, nie dowód** — dowodu nie ma i tak jest to zapisane. Warunek zamknięcia: przebieg, w którym `npm run lint` i `bramka:kotwice` dają werdykt. **STATUS 2026-08-24 (`WWW/026`, potwierdzony `WWW/027`): ZALEŻNOŚCI PRZYWRÓCONE, POZYCJA OTWARTA.** Na zgodę właściciela, po sprawdzeniu obu jego warunków **przed** instalacją (lockfile śledzony — 416 914 B; wolne 18 GiB przy instalacji rzędu 0,3–1,5 GiB): `npm ci` → kod **0**, **533 pakiety, 691 MB**, wolne po instalacji 17 GiB. **`npm run lint`: 127 → 0**, zero ostrzeżeń i błędów; po `npm run build` (kod 0) `bramka:kotwice` również zielona. Skutek dla wcześniejszych commitów: to, co było **argumentem** („dziedzina niewykonanych bramek nie pokrywa się z treścią commitów"), jest teraz **dowodem** — commity z `WWW/025` przechodzą eslint. **Pozycja zostaje OTWARTA na wyraźne polecenie właściciela**, bo **przyczyna zniknięcia jest w ustalaniu po jego stronie**: hipoteza to zgoda `APP/020` na usunięcie odtwarzalnych `node_modules`/`.next`, wydana **bez granicy repozytorium i bez powiadomienia okien dotkniętych**. Dopóki to nie jest rozstrzygnięte, przywrócenie zależności jest **naprawą skutku, nie przyczyny** — i zamknięcie pozycji byłoby zamknięciem na skutku. |
| T46 | **GRANICA WERYFIKACJI MIĘDZY OKNAMI BYŁA PRZYJĘTA, NIE ZMIERZONA — i przez to trzymała się dłużej niż jakakolwiek granica zmierzona.** Przez cały czas istnienia oznaczenia `P-22` rejestr niósł przy sześciu pozycjach zdanie w rodzaju *„drogi weryfikacji z tej strony nie ma, bo to inne repozytorium"*. **Nikt tego nie sprawdził** — bo było to zdanie o **własnych** ograniczeniach, a takie zdanie ma jednego świadka i brzmi jak wiedza z pierwszej ręki. Obalone **jednym odczytem z dysku** (`WWW/019`, 2026-08-24): repozytorium aplikacji leży na tym samym dysku, w katalogu roboczym tej sesji, i jego gałęzie czyta się `git ls-tree` bez żadnego dostępu sieciowego. **ZWĘŻENIE, które z tego wynika:** granica **zdjęta** dla **istnienia i treści plików** — te da się odczytać; granica **zostaje** dla **wyników mutacji i pomiarów wykonanych po tamtej stronie** — tych nie da się odtworzyć bez uruchomienia cudzego kodu w cudzym repozytorium, czego robić nie wolno (ADR-018 pkt 7). Zwężenie **nie jest zniesieniem**: `P-22` nadal obowiązuje, tylko w węższej dziedzinie, a dziedzinę trzeba przy nim wypisywać. **Trzecia rzecz, bez której to jest tylko sprostowanie:** różnica między *„nie da się sprawdzić"* a *„nie sprawdzono"* jest różnicą między granicą a zaniechaniem — i pierwsza forma **usypia**, bo nie zostawia nic do zrobienia. | Sześć adnotacji w skorowidzu ogniw (T25, T30, T36, T38, T43, T44) — przepisane 2026-08-24; pomiar źródłowy: `WWW/019`, potwierdzenie: `WWW/022` (STOP) i `WWW/023` | **Zwężenie zapisane, przegląd sześciu adnotacji wykonany** (`WWW/027`). Do rozstrzygnięcia zostaje jedno: **czy odczytać treść pięciu pozycji, przy których droga weryfikacji istnieje, a nie skorzystano** — `RECZ-161` (9 plików), `RECZ-289` (11), `RECZ-287` (7), materiał toru 14 (`58%` w 4 plikach) oraz `PUNKT-WZNOWIENIA.md`. Odczyt zmieniłby je z „nieodczytane" na pomiar własny albo obalił. **Nie robię tego przy okazji** — to osobne zlecenie i osobny commit, bo odczyt cudzych ustaleń może zmienić treść pozycji, nie tylko jej metadane. Warunek zamknięcia: każda z sześciu adnotacji niesie **albo wynik odczytu, albo powód, dla którego odczytu nie ma** — nigdy samo `P-22` bez dziedziny. |
| T47 | **POMIAR LOKALIZACJI OGŁOSZONY SZERZEJ, NIŻ SIĘGAŁ — „nie istnieje na żadnej gałęzi" przy przeszukaniu gałęzi POBRANYCH.** Zwrotka `WWW/019` (2026-08-24) orzekła o czterech nazwach **NIE ISTNIEJE W REPO**, podając zasięg uczciwie („28 gałęzi"), ale **wniosek sformułowała szerzej niż pomiar**: przeszukane zostały gałęzie **obecne na tym dysku**, a nie gałęzie **istniejące na `origin`**. Obalone trzy godziny później: gałąź `claude/catherly-redakcja-strony-93uyhn` **istniała na `origin` i nie była pobrana** — i to właśnie ona niosła `docs/redakcja/`, o którym zwrotka powiedziała, że nie istnieje. Potwierdzone przy imporcie: `ZWROTKA-KANON-DO-WWW.md` przyszła dokładnie z tego katalogu. **Klasa: ZŁY PODZBIÓR — zbiór, który widzisz, nazwany zbiorem.** Groźniejsza od pustego wyniku, bo **wygląda jak komplet i zatrzymuje szukanie**; kto przeczyta „nie istnieje w repo", nie sprawdzi już `ls-remote`. **Brzmienie poprawne, obowiązujące odtąd:** *„nie istnieje na gałęziach POBRANYCH na ten dysk"* — plus liczba gałęzi niepobranych obok, jeśli jest różna od zera. **`ls-remote` rozstrzyga o istnieniu na `origin`; `ls-tree` rozstrzyga o zawartości tego, co pobrane.** To dwa różne pytania i dwa różne polecenia. | Zwrotka `WWW/019` (wpisy o `docs/redakcja/` i `PRZEKAZANIE-KONCOWE.md`); obalenie: `WWW/022`; potwierdzenie: import `WWW/024`, plik `docs/redakcja/ZWROTKA-KANON-DO-WWW.md` | **Korekta brzmienia zapisana.** Pozycja **otwarta** do czasu rozstrzygnięcia dwóch rzeczy. **(1)** Czy `PRZEKAZANIE-KONCOWE.md`, `KANON-CATHERLY-*`, `DEFINICJA-UKONCZENIA-MODULU` i `raport-ustalen-14-dni` istnieją na gałęziach niepobranych — dziś **nieustalone**, bo pomiar objął tylko pobrane, a od tamtej pory `origin` ruszył co najmniej trzykrotnie. **(2)** Czy mapa wznowienia u koordynatora niesie już dopisek, że pomiary lokalizacji mówią o gałęziach pobranych — to leży po jego stronie i stąd tego nie sprawdzę. Warunek zamknięcia: powtórzony pomiar lokalizacji z jawnym rozróżnieniem `ls-remote` / `ls-tree` i z liczbą gałęzi niepobranych podaną obok wyniku. **DOWÓD PRZENOŚNOŚCI REGUŁY, dopisany na polecenie właściciela 2026-08-26 — trzecia kategoria z klasy 23.** Przy przeglądzie sześciu adnotacji (`WWW/027`) **metoda wyszukiwania okazała się zepsuta DWA RAZY w jednym przebiegu**: najpierw `--` postawione przed wzorcem zamieniło wzorzec w ścieżkę (`git grep -l -- "$w" "$b"`), potem pętla po gałęziach gubiła trafienia. **Oba defekty zdjęła KONTROLA POZYTYWNA** — wyszukanie ciągu, o którym z góry wiadomo, że istnieje. Pierwszy wynik brzmiał „zero trafień na czterech kodach `RECZ` w 29 gałęziach" i **wyglądał jak ustalenie**; po naprawie metody wszystkie cztery kody się znalazły: `RECZ-161` (9 plików), `RECZ-286` (5), `RECZ-287` (7), `RECZ-289` (11) na `origin/feat/cs-build`. **Znaczenie dla kanonu:** reguła „zero bez kontroli pozytywnej jest zerem narzędzia" została przeniesiona z toru 9 **rano** 2026-08-24 i **tego samego dnia wieczorem uratowała pomiar własny** — w materiale, dla którego nie powstała, i przed błędem, którego jej autor nie przewidywał. To jest **przenośność** w rozumieniu klasy 23: nie retrospekcja (błąd nie był popełniony wcześniej i opisany po fakcie) i nie uchronienie w sensie zatrzymania ręki przy pisaniu, tylko **działanie reguły poza miejscem, w którym powstała**. Licznik przenośności: **drugie wystąpienie** po korekcie K5. **TRZECIE WYSTĄPIENIE ZERA NARZĘDZIA, 2026-08-26 — tym razem zgłoszone właścicielowi JAKO USTALENIE, zanim wyszło.** Zwrotka `WWW/029` orzekła, że arkusz `public/proba-kroju.html` **nie ma przełącznika palet** („`data-paleta` 0×"), i tak samo o braku pomiarów na żywo. Przyczyna: grep szukał literału `data-paleta`, a kod ustawia atrybut przez **`dataset.paleta`** — camelCase API DOM, w którym ten ciąg **nie występuje**. Sprawdzone 2026-08-26 na żywo w przeglądarce: pulpit ma **pięć** przycisków (produkcja + P1 Len, P2 Kość, P3 Popiół, P4 Czerń), przełączanie działa bez przeładowania, `czern` daje tło `#0a0a0a` i akcent `#a3e635` zgodnie z blokiem `globals.css`. **Różnica wobec dwóch poprzednich przypadków jest cała:** tamte złapała kontrola pozytywna **przed** zapisem, ten **poszedł do właściciela jako wynik** i wrócił dopiero przy próbie zbudowania rzeczy, która już istniała. Wniosek wykonawczy, ostrzejszy niż sama reguła o kontroli pozytywnej: **przy orzekaniu o NIEOBECNOŚCI w kodzie nie wystarczy kontrola pozytywna na wzorcu — trzeba sprawdzić, czy szukany byt nie ma w kodzie drugiej postaci** (atrybut HTML kontra `dataset`, kebab-case kontra camelCase, ciąg kontra stała). Zero z jednej postaci nie jest zerem bytu. |
| T48 | **MANIFEST PRZEKAZANIA WYMIENIAŁ TYLKO ŁADUNEK, NIE WSZYSTKIE PLIKI WSPÓLNE, KTÓRYCH GAŁĄŹ DOTYKA.** Zmierzone 2026-08-24 przy inwentarzu przed importem toru 9 (`WWW/024`), zapisane 2026-08-26. `docs/redakcja/ZWROTKA-KANON-DO-WWW.md` wymieniał **pięć** plików wspólnych; porównanie drzew `6ef5617` ↔ `tor9/redakcja-import` dało **siedem** wspólnych-i-różnych. Trzy pominięte: **`docs/STRATEGIA.md`** (+3/−8 wobec nas), **`docs/adr/018-prymat-nieodwracalnego.md`** (+0/−10), **`scripts/backup.sh`** (+0/−35). Wszystkie trzy stoją w imporcie w wersji **identycznej z `69c2dab`** — sprawdzone porównaniem skrótów blobów — czyli import ich **nie zmieniał**; różnica bierze się stąd, że **my zmieniliśmy je 2026-08-24** (wykreślenie zdania o pierwszeństwie ze STRATEGII, punkt 7 ADR-018, naprawa wzorca w `backup.sh`). **Nasze wersje są NOWSZE, więc przeniesienie byłoby COFNIĘCIEM.** Mechanizm wady jest ten sam, co przy bazie manifestu: nadawca wymienia to, co **uważa za ładunek**, a odbiorca czyta to jako **listę wszystkiego, czego zmiana dotyka**. Trzy pominięte pliki to trzy miejsca, w których odbiorca **bez własnego inwentarza cofnąłby cudzą pracę w dobrej wierze** — i nie zobaczyłby tego ani w manifeście, ani w komunikacie git. | `docs/redakcja/ZWROTKA-KANON-DO-WWW.md` (część 0, tabela pięciu plików); porównanie `git ls-tree` obu drzew + skróty blobów, `WWW/024`, 2026-08-24 | **WPIS WYŁĄCZNIE EWIDENCYJNY — rozstrzygnięcie właściciela 2026-08-26: niczego nie przenosić.** Nie ma tu warunku powrotu po tej stronie: nasze wersje są nowsze i mają takie zostać. **Konsekwencja idzie do NADAWCY**, osobnym zleceniem właściciela do toru 9: *manifest przekazania wymienia WSZYSTKIE pliki wspólne dotknięte na gałęzi, nie tylko te, które nadawca uważa za ładunek.* Pozycja zamyka się, gdy przyjdzie pierwsze przekazanie z manifestem spełniającym ten warunek — do tego czasu **każdy import z tego kierunku wymaga własnego inwentarza po stronie odbiorcy**, i to jest jedyna rzecz, która tu chroni. |
| T49 | **⚠ TRZYNASTE MIEJSCE TEJ SAMEJ OBIETNICY, POZA INWENTARZEM MATERIAŁU — i po zmianie A-16 strona mówi w dwóch miejscach dwie różne rzeczy.** Materiał `docs/redakcja/MATERIAL-A16-PRZEDZIAL.md` policzył **dwanaście** wystąpień („2 klucze × 3 języki w `messages` + te same akapity w `content/`") i na tych dwunastu wykonano zmianę 2026-08-26. **Trzynaste stoi w `content/tabela-obietnic.md:37`** i brzmi wciąż po staremu: *„dostajesz przypomnienie **30 minut przed każdą rozmową**"* — czyli niesie **obie wady, które A-16 usunęło**: liczbę punktową tam, gdzie mechanizm daje przedział (W1), i bezwarunkowe „każdą" tam, gdzie mechanizm ma budżet i przypomnienie może przepaść (W2). Materiał nie zmyślił dwunastki — **policzył ją dla dwóch KLUCZY**, a tabela obietnic powtarza to samo twierdzenie **prozą, bez klucza**, więc wypadła z zapytania. To ta sama klasa co „zły podzbiór" (T47), tylko po stronie nadawcy materiału. **Dlaczego to nie jest drobiazg porządkowy:** `tabela-obietnic.md` jest plikiem, **którego zadaniem jest wyliczać obietnice**, a obietnice są jednym z czterech obszarów nieodwracalnych ADR-018 („nigdy nie obiecujesz na stronie tego, czego aplikacja nie robi"). Do 2026-08-26 strona mówiła nieprawdę **spójnie**; od tej zmiany mówi **nieprawdę w jednym miejscu i prawdę w pozostałych sześciu** — a stan rozjechany jest trudniejszy do zauważenia niż stan błędny, bo każde miejsce z osobna wygląda na przemyślane. | `content/tabela-obietnic.md:37`; inwentarz materiału: `docs/redakcja/MATERIAL-A16-PRZEDZIAL.md` §1; zmiana dwunastu miejsc: `WWW/034`, 2026-08-26 | **NIE RUSZAM — poza zakresem zlecenia** (`WWW/034` §1 mówi „12 miejsc wg materiału"), więc zakaz 8 obowiązuje. **Ale zgłaszam z wagą, nie jako pozycję porządkową:** ADR-018 stawia obietnice ponad zakresem i terminem, a ten plik jest rejestrem obietnic. **Do rozstrzygnięcia przez właściciela, pilne:** czy trzynaste miejsce wchodzi do tej samej redakcji, czy zostaje do skoordynowanej redakcji `O-*`. **Czego przy tym nie wolno przeoczyć:** wiersz w tabeli obietnic ma DWA wystąpienia liczby — w kolumnie nazwy („przypomnienie 30 min przed") i w kolumnie opisu — więc poprawka jest dwuczłonowa, a `scripts/lint-liczby.mjs` czyta ten plik i pilnuje pokrycia liczby w `facts.json`. **Warunek zamknięcia:** wszystkie wystąpienia tej obietnicy w repozytorium mówią jedno, sprawdzone **wyliczeniem miejsc, nie przeglądem dwóch kluczy** — bo to właśnie przegląd po kluczach zgubił to miejsce. |

### Skorowidz ogniw — REJESTR LICZY OGNIWA, NIE ŹRÓDŁA (T39)

Wprowadzone na wymóg właściciela 2026-08-23. **Ogniwo** to jedno przekazanie
cudzego słowa. Ustalenie zmierzone tutaj stoi na zerze ogniw i jest zapisane
jako **1 — pomiar własny**; ustalenie usłyszane od właściciela stoi na jednym;
ustalenie, które właściciel przejął od innego toru albo z drugiego repozytorium,
stoi na **dwóch** — i tak jest oznaczone, bo drugiego ogniwa nie da się stąd
sprawdzić żadną bramką (T21 ustalenie 5).

**Gdzie nie wiem — stoi „nieustalone", nie domysł.** W szczególności: przy
pozycjach oznaczonych „właściciel bezpośrednio; dalej nieustalone" nie wiem,
czy właściciel mówił z własnego pomiaru, czy przekazywał ustalenie innego toru.
Właściciel sam wskazał 2026-08-23, że część jego ustaleń pochodzi z innych
torów — więc **liczba ogniw przy tych pozycjach jest dolnym oszacowaniem**.

| Poz. | Ogniwa | Skąd wiem |
| --- | --- | --- |
| T1 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T2 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T3 | **1** — pomiar własny; kierunek rozstrzygnął właściciel | pomiar wydajności, zamknięcie decyzją właściciela |
| T4 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T5 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T6 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T7 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T8 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T9 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T10 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T11 | **1** — pomiar własny; kierunek rozstrzygnął właściciel | odczyt `playwright.config.ts`; „odnotować, nie naprawiać" — właściciel |
| T12 | **1** — pomiar własny; kierunek rozstrzygnął właściciel | defekt zmierzony, decyzja o odłożeniu właściciela |
| T13 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T14 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T15 | **1** — pomiar własny w tym repozytorium | skrót widmo po `git commit --amend`, wychwycone pomiarem osiągalności |
| T16 | **nieustalone** | nie pamiętam, czy z pomiaru, czy z uwagi właściciela — **nie zgaduję** |
| T17 | **1** — pomiar własny w tym repozytorium | lista tras przepisana z ręki — odczyt pliku |
| T18 | **1** — pomiar własny w tym repozytorium | — |
| T19 | **1** — właściciel bezpośrednio; **dalej nieustalone** | — |
| T20 | **1** — pomiar własny w tym repozytorium | — |
| T21 | **1** — pomiar własny w tym repozytorium | osiągalność skrótów sprawdzona `merge-base`; **ustalenie 5 dotyczy odwołania międzyrepozytoryjnego** |
| T22 | **1** — pomiar własny; kierunek rozstrzygnął właściciel | skażenie zmierzone, trzy kierunki rozstrzygnął właściciel |
| T23 | **1** — pomiar własny w tym repozytorium | brak `fetch-depth` odczytany z `bramki.yml` |
| T24 | **1** — pomiar własny w tym repozytorium | brak `timeout-minutes` odczytany z `bramki.yml` |
| T25 | **1** — właściciel bezpośrednio; **dalej nieustalone**. **Dopisek z 2026-08-23 stoi na DWÓCH; od 2026-08-24 sprawdzalny CZĘŚCIOWO** (`WWW/027`): `docs/wdrozenia/PUNKT-WZNOWIENIA.md` i skrypty strażnika są odczytywalne z dysku na `origin/feat/cs-build`, więc **istnienie i treść** dają się zweryfikować; **wynik mutacji (EXIT=1/EXIT=0) — nie**, bo wymagałby uruchomienia cudzego strażnika w cudzym repozytorium. Adnotacja przepisana wg zwężenia z **T46** — liczy się osobno, bo nie jest pochodzeniem pozycji, tylko wzorcem doniesionym do niej | reguła właściciela o przekazaniu na bieżąco. Dopisek: dowód mutacyjny strażnika punktu wznowienia — **właściciel ← tor 10** (repozytorium aplikacji, gałąź `feat/cs-build`); **`P-22`: drogi weryfikacji z tej strony brak**, bo to inne repozytorium |
| T26 | **1** — właściciel bezpośrednio; **dalej nieustalone** | reguła właściciela 2026-08-20; **zastosowanie z 2026-08-23 (odmowa wysyłki) = pomiar własny** |
| T27 | **1** — pomiar własny; kierunek rozstrzygnął właściciel | nieobecność `KANON-CATHERLY-STRONA.md` zmierzona; „trzeci raz dziś" — właściciel |
| T28 | **1** — pomiar własny w tym repozytorium | przebieg `32302412113`, pięć wartości `/` odczytane z artefaktu |
| T29 | **1** — pomiar własny w tym repozytorium | zawartość zdalnego `69c2dab` sprawdzona `git ls-remote` |
| T30 | **2** — właściciel ← inny obszar; **droga weryfikacji ISTNIEJE, nie skorzystano** (`WWW/027`, 2026-08-24) | `RECZ-161` — **właściciel ← strona aplikacji**. Zmierzone: kod obecny w **9 plikach** na `origin/feat/cs-build` i 2 na `origin/feat/kontrowersje`, więc treść jest odczytywalna z dysku. **Nadal nieodczytana** — to różnica między niemożnością a zaniechaniem, i tak ma być zapisana (**T46**) |
| T31 | **1** — pomiar własny; kierunek rozstrzygnął właściciel | dziewięć policzone `git log`; „dziesięć" — właściciel z pamięci |
| T32 | **1** — pomiar własny w tym repozytorium | pięć źródeł policzone przez odczyt `STRATEGIA`, `PLAN`, `adr/README`, `CLAUDE.md`, rejestru |
| T33 | **1** — właściciel bezpośrednio; **dalej nieustalone** | rozstrzygnięcie właściciela o nieinterpretowalności progu |
| T34 | **1** — pomiar własny; kierunek rozstrzygnął właściciel | dziesięć zakazów sformułowane tutaj, **przyjęte** przez właściciela |
| T35 | **1** — pomiar własny w tym repozytorium | grep w obu repozytoriach 2026-08-23 |
| T36 | **2** — właściciel ← inny obszar; **droga weryfikacji ISTNIEJE, nie skorzystano** (`WWW/027`) | cytat o wymuszaniu kształtu — **właściciel ← sesja aplikacji**; przedmiot zlecenia (`RECZ-289`) obecny w **11 plikach** na `origin/feat/cs-build`, więc źródło cytatu jest odczytywalne. **Nieodczytane.** Pokrycie rodzime: T33, T34 |
| T37 | **3** — właściciel ← rejestr przepływu ← **tor 8**; ogniwa drugie i trzecie **niezweryfikowane stąd** | Sam wiersz `A-05` podaje w kolumnie źródła **tor 8, `00-METODA-I-KOLEJNOSC.md` §1.1**, a jego status brzmi tam **„PRZYJĘTE BEZ SPRAWDZENIA"** — więc do mnie dociera przez trzy ręce, nie dwie. **Najdalsza pozycja w tym rejestrze.** |
| T38 | **2** — właściciel ← inny obszar; **droga weryfikacji ISTNIEJE częściowo** (`WWW/027`) | 58% z 19 sprawdzeń — **właściciel ← tor 14**; oszacowanie z cudzych danych. Zmierzone: ciąg `58%` w **4 plikach**, `tor 14` w **5** na `origin/feat/kontrowersje`; dokładnego zwrotu `19 sprawdzeń` **nie ma** (metoda potwierdzona kontrolą pozytywną). Materiał **lokalizowalny, liczba nieodnaleziona co do brzmienia** — nieodczytane |
| T39 | **1** — pomiar własny; kierunek rozstrzygnął właściciel | pomiar 36/30 własny; wymóg ogniw — właściciel ← tor 14 |
| T40 | **1** — pomiar własny; kierunek rozstrzygnął właściciel | porównanie obu `CLAUDE.md` własne; ranga ustalenia nadana przez właściciela |
| T41 | **1** — pomiar własny w tym repozytorium | 15 ostrzeżeń odczytanych z logu przebiegu `32663550392` (`d7a2fe3`); wersje akcji policzone w `bramki.yml` |
| T42 | **1** — pomiar własny w tym repozytorium; **kierunek („zgłoś jako pozycję") rozstrzygnął właściciel** | zero trafień na `hooks` w czterech plikach konfiguracji; przerwa w migawkach odczytana z katalogu SSD; ręczny przebieg `backup.sh` z kodem 0 i `unzip -t` |
| T43 | **1** — pomiar własny w tym repozytorium; **kierunek („sprawdź odtworzeniem") rozstrzygnął właściciel**, sam wymóg przyszedł od niego ← repozytorium aplikacji (**drugie ogniwo niezweryfikowane stąd**) | `D .env.example` odczytane z `git status` w odtworzonym repo; wzorzec z `backup.sh:52-53`; brak `.env*` w wykazie archiwum. Powód sprawdzania — **`RECZ-287`**, repozytorium aplikacji: 75 archiwów zweryfikowanych samą sumą, z żywymi kluczami — **cudzy pomiar; droga weryfikacji ISTNIEJE** (kod obecny w **7 plikach** na `origin/feat/cs-build`, `WWW/027`), **nie skorzystano** |
| T44 | **1** — pomiar własny; **drugie ogniwo SPRAWDZONE** (`WWW/027`) | 10 naruszeń policzone własnym uruchomieniem `lint-deklaracje.mjs`; **kształt zadania i jego uzasadnienie odczytane wprost** z `tor9/redakcja-import:.github/workflows/bramki.yml`, nie z cudzego opisu. Jedyna z sześciu adnotacji, przy której weryfikacja **została wykonana** — i to zanim zwężenie zostało nazwane |
| T45 | **1** — pomiar własny w tym repozytorium | kod 127 z `npm run lint`, nieobecność katalogów, `mtime` katalogu, wykluczenie hooków i `backup.sh` odczytem |
| T46 | **1** — pomiar własny w tym repozytorium; zwężenie nazwane przez właściciela | odczyt gałęzi aplikacji z dysku (`WWW/019`); sześć adnotacji przepisanych `WWW/027` |
| T47 | **1** — pomiar własny w tym repozytorium | rozjazd `ls-remote` vs `ls-tree` zmierzony `WWW/022`; potwierdzenie importem `WWW/024` |
| T48 | **1** — pomiar własny w tym repozytorium; kierunek („wpisz, zero przenoszenia") rozstrzygnął właściciel | porównanie drzew i skrótów blobów `6ef5617` ↔ `tor9/redakcja-import`, `WWW/024` |
| T49 | **1** — pomiar własny w tym repozytorium | trafienie znalezione kontrolą po zmianie A-16 (`grep` na „każdą rozmową" poza dwoma plikami), `WWW/034` |

**Rozkład, policzony ze skorowidza** (przeliczony 2026-08-24 po dopisaniu T47;
suma **49** = T1–T49, policzone ze skorowidza poleceniem, nie z ręki):
pomiar własny w tym repozytorium — **19** · pomiar własny z rozstrzygnięciem
właściciela — **10** · właściciel bezpośrednio, dalej nieustalone — **15** ·
**dwa ogniwa — 3** (T30, T36, T38) · **trzy ogniwa — 1** (T37) ·
nieustalone — **1** (T16). Suma zgadza się z liczbą wierszy skorowidza.
**T44 zeszła z dwóch ogniw na jedno** (`WWW/027`): drugie ogniwo zostało
**sprawdzone odczytem**, więc przestało być ogniwem i stało się pomiarem.
To pierwsza pozycja w tym rejestrze, która **zmniejszyła liczbę ogniw przez
weryfikację**, a nie przez korektę zapisu.
T43 stoi na dwóch, bo pomiar jest własny, ale **powód sprawdzania** przyszedł
z drugiego repozytorium przez właściciela i stąd nie da się go zweryfikować.
**Poza tym rozkładem stoi jeden dopisek na dwóch ogniwach** — wzorzec z toru 10
doniesiony do T25 (2026-08-23). Nie wchodzi do liczb, bo skorowidz mierzy
pochodzenie **pozycji**, a nie każdej rzeczy, która do niej dotarła; gdyby go
wliczyć, T25 wyglądałaby na pozycję przejętą z zewnątrz, a jest regułą
właściciela z dopiskiem.

**Najdalsza pozycja rejestru to T37** i warto zobaczyć, jak daleko: `A-05` powstał
w torze 8, trafił do rejestru przepływu ze statusem **„PRZYJĘTE BEZ SPRAWDZENIA"**,
stamtąd wziął go właściciel, a ode mnie dostaje wiersz w rejestrze strony. Ustalenie,
którego **nikt po drodze nie sprawdził**, przeszło przez trzy ręce i zostało zapisane
w dwóch repozytoriach. To jest dokładnie ten kształt, przed którym wymóg ogniw
ma chronić — i dlatego pozycja stoi tu z liczbą **3**, a nie z etykietą „od właściciela".


**T1 — pomiar, nie przypuszczenie.** Ustalone przy Etapie D:
dopisanie trzech kluczy członu i rozbicie F8 powiększyło HTML strony
głównej z **34 260 B na 34 536 B (+276 B)** — mimo że `/` nie
wyświetla żadnego z tych komunikatów. Koszt rośnie z każdym nowym
kluczem i obciąża WSZYSTKIE strony, nie tylko tę, której klucz
dotyczy. Zmierzony wpływ na LCP `/` był w granicach szumu
(mediana 1708,6 → 1709,1 ms), więc pozycja jest optymalizacyjna,
nie blokująca. Wraca, kiedy albo liczba kluczy urośnie na tyle, żeby
przebić szum, albo przy okazji bloku designu — co nastąpi wcześniej.
Szczegóły: `docs/faza-4/komponenty/handoff-etap-d.md` §12.3.

**T2 — dlaczego to NIE jest dług do spłaty przy okazji.** Bramka jest
czerwona od commita Fazy 0 i taka zostaje. Właściciel rozstrzygnął
2026-08-14, że audyt nieodwracalnych ma być **całościowy
przedpremierowy w Fazie 6**, a nie składany z audytów cząstkowych po
każdym etapie. Dopóki raportu nie ma, bramka blokuje **wdrożenie
produkcyjne** (i tylko je — nie push na gałąź roboczą), co jest jej
zamierzonym działaniem, a nie awarią. Czerwień jest tu informacją, że
premiera nie jest odblokowana, i ma pozostać widoczna.

**T3 — co jest zrobione, a co zablokowane.** Zrobione i sprawdzone:
konfiguracja `lighthouserc.cjs` z jednym źródłem ścieżek i bazą ze
zmiennej, mediana jako werdykt (dowód: przy przebiegach 1702,8 · 1708,3
· 1711,3 ms `median` zwraca 1708,3, a domyślny `optimistic` — 1702,8)
oraz strażnik `bramka:preview`, sprawdzony w trzech kierunkach:
przepuszcza właściwą stronę, zatrzymuje ekran logowania Vercela,
zatrzymuje obcy serwis oddający 200.

Zablokowane: **preview jest za ścianą logowania**. Zmierzone
2026-08-14 na `catherly-9s8us771y…vercel.app`: żądanie oddaje 302 na
`vercel.com/sso-api`, a po przekierowaniu — stronę logowania ze
statusem **200** i ze słowem „Catherly" **dwa razy** w treści (adres
siedzi w parametrze `next=`). Naiwny strażnik oparty na statusie albo
nazwie produktu przepuściłby ten ekran. Do odblokowania potrzeba
decyzji właściciela: albo wyłączyć ochronę Preview (Vercel → Settings
→ Deployment Protection), albo włączyć Protection Bypass for
Automation i wstawić wartość jako sekret GitHuba
`VERCEL_AUTOMATION_BYPASS_SECRET`. Dziś w repozytorium jest tylko
`STRIPE_TEST_SECRET_KEY`.

Otwarte i **nierozwiązane**: nawet po udostępnieniu preview trzeba
wykazać, że mierzony adres to TEN commit, a nie ostatnie wdrożenie
gałęzi. Bez tego bramka mierzy prawdziwą stronę, ale niekoniecznie
tę testowaną — inna postać tego samego fałszywego zielonego. Kandydat:
znacznik z `VERCEL_GIT_COMMIT_SHA` w dokumencie i sprawdzenie go przez
strażnika. Nie zbudowane, bo bez dostępu do preview nie da się tego
zweryfikować, a niezweryfikowane liczy się jak niedziałające.

**T3 — ZAMKNIĘTE 2026-08-16.** Trzy akapity powyżej opisują stan
z 14 sierpnia i zostają jako historia. Dziś preview jest dostępne dla CI
przez Protection Bypass for Automation (sekret w GitHubie; wartość nie
jest drukowana nigdzie — łącznie z nagłówkiem `Set-Cookie` z preview,
który niesie ją w jawnym base64). Pytanie „otwarte i **nierozwiązane**"
— czy mierzymy TEN commit — ma odpowiedź **wykonaną**, nie
zaprojektowaną: strażnik `bramka:preview` sprawdza znaczniki wydania
w dokumencie, a rozgrzewka powtarza sprawdzenie na **każdej** trasie.
Z przebiegu 31957994362: „✔ Cel pomiaru potwierdzony: HTTP 200,
42837 B, 3/3 markerów obecnych" i „✔ Rozgrzane: 7/7 tras, 116 pobrań
zasobów. Wydanie zgodne na każdej trasie."

Jedno słowo z wiersza T3 jest przy tym **nieprawdziwe wobec dzisiejszej
bramki** i dlatego stoi przy nim sprostowanie: werdyktem **nie jest
`mediana`**. Mediana każdej metryki osobno zszywa LCP z przebiegu A
z TBT z przebiegu B i opisuje ładowanie, którego nigdy nie było —
została zabita świadomie. Wyrok zapada na **jednym prawdziwym
przebiegu**, wybranym po medianowym LCP, czyli po metryce, na której
stoi próg (`scripts/reprezentant.mjs`, rozstrzygnięcie właściciela
2026-08-16, rozbiór: `docs/faza-4/bramka-na-preview.md` §4b). Zamknięcie
T3 nie zamyka **rozrzutu** pomiaru — ten dostał własną pozycję T10.

**T4 — obietnica panelu K2 trzyma się jednego kroju i jednej
szerokości.** Wyszło z bramki pełnego zestawu (przebieg 31833329728):
`hero (de): H1 ≤ 3 linie na desktopie` czerwony na runnerze, zielony
lokalnie. To nie flake — to różnica deterministyczna, bo `ch` jest
jednostką zależną od kroju (szerokość cyfry „0"), a `system-ui` znaczy
inny krój na każdym systemie.

**Droga (A) — miara `22ch` → `24ch` — została wykonana (commit
`3cf7299`) i NIE ZADZIAŁAŁA.** Przebieg 31835331503: ten sam test, ten
sam wynik, `Received: 4`. Zero zmiany, i to zero jest informacją.

**Pomiar wykonany po tej porażce** (Playwright, sześć krojów × trzy
szerokości × trzy języki; `scripts/` nie zawiera go na stałe — pomiar
jednorazowy). Przy 1280 px kolumna hero daje **653 px**, H1 DE ma
**72 znaki**, a `max-width: 24ch` wypada tak:

| krój | 1ch | `24ch` daje | co realnie ogranicza | naturalna szerokość H1 DE | linie DE |
|---|---|---|---|---|---|
| system-ui na macOS | 31,47 px | 755 px | **kolumna** (miara bezczynna) | 1604 px | 3 |
| Arial / Helvetica | 26,69 px | 641 px | miara | 1675 px | 3 |
| Trebuchet MS | 28,13 px | 675 px | **kolumna** | 1633 px | 3 |
| Tahoma | 30,56 px | 734 px | **kolumna** | 1762 px | 3 |
| Verdana | 34,13 px | 819 px | **kolumna** | 1977 px | **4** |

Stąd trzy wnioski, każdy zmierzony:

1. **Krój runnera jest szerszy od Arialu**, a nie arialowy — tak
   zakładał poprzedni zapis tej pozycji i było to błędne. Jedyny
   sprawdzony krój, na którym DE łamie się na 4 linie, to Verdana
   (klasa szerokich krojów bezszeryfowych; linuksowa DejaVu Sans ma
   metrykę tej klasy). To jedyna kolumna tabeli zgodna z tym, co
   pokazuje CI.
2. **Przy szerokim kroju miara `ch` jest bezczynna**: `24ch` = 819 px,
   czyli więcej niż kolumna 653 px, więc ogranicza kolumna. `22ch`
   dawało tam 751 px — też więcej niż kolumna. Obie wartości są na
   runnerze równie nieistotne, dlatego zmiana nie dała żadnego efektu.
   Żadna wartość `ch` **większa** od ~19ch nic tam nie zmieni, a
   mniejsza pogarsza, bo zwęża.
3. **Zapas jest kwestią treści, nie miary.** Przy 653 px trzy linie
   mieszczą do 1959 px tekstu. PL ma 1619 px (zapas 21%), EN 1746 px
   (zapas 11%), DE 1977 px — **3% ponad limit**. DE jest jedynym
   językiem poza budżetem i wykracza minimalnie.

Poza 1280 px obietnica jest nieprawdziwa znacznie szerzej, niż
zapisano poprzednio — nie tylko dla DE. Przy 768 px kolumna ma tylko
**422 px**, a font jest już maksymalny (48 px), bo `clamp` dobija do
`3rem` około 640 px szerokości okna. Efekt na Verdanie: **6 linii we
wszystkich trzech językach**, także po polsku. Na macOS przy 768 px:
PL 4, EN 4, DE 5. Trzech linii nie ma tam żaden język i żaden krój.
Strażnik pilnuje jednej szerokości (`Desktop Chrome` = 1280 px), więc
tego nie widzi.

**Co z tego wynika dla zamknięcia T4.** Drogi realne były trzy i żadna
nie była zmianą miary: skrócenie H1 DE (treść), poszerzenie kolumny
hero (układ), obniżenie górnego krańca `clamp` (typografia).

Droga treści **została zamknięta oceną adwersaryjną**: każdy wariant
skróconego H1 DE odpadł, a rozstrzygający zarzut jest sprawdzalny —
bez „deine" zdanie „Catherly führt Kontakte" czyta się po niemiecku
jako *Catherly prowadzi rozmowy*, czemu ta sama strona wprost
zaprzecza (`content/de/dla-kogo.md`: „führt keine Gespräche"). To
naruszenie ADR-018, więc treść wypadła z gry.

Droga układu **została zmierzona i wdrożona**: decyzja właściciela
z 2026-08-14, wariant V5 — próg dwóch kolumn hero 48rem → **72rem**
i proporcja 3fr 2fr → **4fr 2fr** (ADR-029). Pomiar pięciu wariantów
× 3 języki × 3 kroje × 9 szerokości, Verdana jako zamiennik
najgorszego kroju:

| wariant | 1440 | 1280 | 1152 | 1024 | 900 | 768 |
|---|---|---|---|---|---|---|
| stan przed zmianą | 4 | 4 | 4 | 4 | 5 | 6 |
| próg 64rem + 4fr 2fr | 3 | 3 | 3 | **4** | 3 | 3 |
| **V5: próg 72rem + 4fr 2fr** | 3 | 3 | 3 | 3 | 3 | 3 |

**Część desktopowa T4 jest domknięta, mobilna nie.** Poniżej 768 px
H1 DE nadal ma 4–5 linii — tam hero był jednokolumnowy już przed
zmianą, więc V5 niczego nie dotknął. Obietnica panelu K2 mówiła
o desktopie i na desktopie jest teraz spełniona; poniżej 768 px nie
ma ani obietnicy, ani strażnika. **Pozycja zostaje otwarta w części
mobilnej**, a razem z nią pytanie, czy `clamp` H1 nie powinien
skalować się względem kolumny zamiast względem okna.

Zastrzeżenie do samego dowodu: wdrożenie potwierdza pomiar lokalny na
Verdanie, nie przebieg CI. Dopóki bramka pełnego zestawu nie zaświeci
na zielono, ta naprawa ma status **niesprawdzonej**, a niesprawdzona
liczy się jak niedziałająca (ADR-018).

Podstrony zostają na `22ch` — zmierzony zapas przy metryce Arial to
co najmniej jedna linia na wszystkich ośmiu adresach DE (najciaśniej:
`/de/dla-kogo` i `/de/cennik`, 3 linie przy 489 px). Zastrzeżenie po
pomiarze T4: ten zapas był liczony w metryce Arial, a krój runnera jest
szerszy. Podstrony nie mają dziś strażnika liczby linii, więc jest to
pozycja niepilnowana, a nie pozycja sprawdzona.

**T5 — zakres startu a to, co Faza 4 już zbudowała.** ADR-014 (i PLAN.md
§11) wymieniają „cztery podstrony filarów" oraz „/dla-kogo" w sekcji
**poza zakresem startu**, z zastrzeżeniem, że każde rozszerzenie wymaga
ADR-a jawnie uchylającego ADR-014. Etapy C i D gałęzi `faza-4/podstrony`
zbudowały dokładnie te strony. Przeszukanie `docs/` nie znalazło ADR-a
uchylającego ten punkt; ADR-024 zmienia **kolejność** prac (fazowanie
hybrydowe per komponent), nie zakres startu. Nie rozstrzygam tego sam —
to decyzja właścicielska o zakresie, nie usterka do naprawy. Dwie drogi:
ADR uchylający, który wciąga te strony do zakresu startu (wtedy blokują
publikację i wchodzą do „definicji startu"), albo potwierdzenie, że
istnieją w repo, ale publikują się przez /zmiany po starcie. Odnotowane
też w samym ADR-014 jako punkt 2 doprecyzowania z 2026-08-14.

**T5 — co rozstrzygnięcie z 2026-08-15 zamyka, a czego nie.** Przy
Etapie E adwersarz wykazał, że propozycja `/pomoc` z modułem K12 (H2 →
PO CO TO → JAK WYGLĄDA → CZEGO NIE ROBI) byłaby **piątą podstroną
filarową w przebraniu**, czyli powiększałaby tę właśnie lukę. Właściciel
zmienił gatunek `/pomoc` na krótki i nawigacyjny i odnotował, że „piąta
podstrona" jest **bezprzedmiotowa**. To domyka **tylko powiększenie
luki**: `/pomoc` nowego gatunku nie tworzy kolejnego bytu filarowego.
**Sam T5 zostaje otwarty** — dotyczy czterech podstron i `/dla-kogo`,
które już istnieją w repo i których żaden ADR nie wciągnął do zakresu
startu. Zapis wąski celowo: szerokie odczytanie zamknęłoby lukę ADR-014
bez decyzji, która by ją zamykała, a to jest dokładnie ten rodzaj cichego
domknięcia, przed którym stoi ten rejestr.

**T5 — ZAMKNIĘTE decyzją właściciela 2026-08-15 (późniejszą tego samego
dnia).** Wąskie odczytanie z akapitu wyżej okazało się słuszne
proceduralnie i **zbędne merytorycznie**: postawione właścicielowi
osobno, dostało odpowiedź wprost. **Cztery podstrony filarów
i `/dla-kogo` WCHODZĄ do zakresu startu** — ADR-014, doprecyzowanie
2026-08-15 (II). Uzasadnienie właściciela: strony istnieją,
są opublikowane w trzech językach, mają testy i przechodzą bramki;
rozbieżność między tym stanem a literą ADR-014 jest **luką formalną**,
nie sporem o zakres — nikt nigdy nie postanowił, że mają czekać
na `/zmiany`.

Trzy rzeczy warto odnotować, bo wynikają z tego, a nie są w decyzji
napisane wprost. **(1)** Pięć adresów wchodzi do „definicji startu"
z ADR-014 — od teraz blokują publikację, więc ich bramki są bramkami
premiery, nie bramkami gałęzi roboczej. **(2)** Reszta listy „poza
zakresem startu" jest **nietknięta**: `/o-catherly`, `/blog`, demo,
`/integracje`, porównania, kalkulator, narzędzie-magnes, webinary,
rozbudowany element podpisu. Rozstrzygnięcie dotyczy pięciu adresów
i nie jest precedensem. **(3)** Forma pozostaje doprecyzowaniem, choć
sekcja „Konsekwencje" ADR-014 żąda dla rozszerzeń ADR-a jawnie
uchylającego. Właściciel nazwał to luką formalną świadomie; zapisuję
to zdanie, żeby przyszły czytelnik nie musiał odtwarzać rozumowania,
a nie jako zastrzeżenie — decyzja jest podjęta i nic nie wstrzymuje.

Pozycja **T8** niżej zapisuje ruch przeciwny z tego samego dnia:
`/pomoc` wychodzi z zakresu startu. Obie decyzje zapadły tego samego
dnia i nie są ze sobą sprzeczne — pierwsza zapisuje strony, które
**istnieją i działają**, druga wycofuje stronę, która **nie ma czym
być**. Kryterium jest w obu wypadkach to samo: stan faktyczny wygrywa
z literą planu.

**T6 — bramka liczb jest ślepa na warstwę, w której dziś mieszka
treść.** Sprawdzone wykonaniem 2026-08-15, nie przeczytane z kodu:
`npm run bramka:liczby` kończy się komunikatem „Linter liczb: zielony."
i kodem 0, podczas gdy w `src/i18n/messages/*.json` stoi **14 polskich
ciągów z liczebnikami słownymi** bez pokrycia w `facts.json`. Przyczyna
jest dwuczłonowa i obie części są w pliku widoczne: `SCAN_EXT` to
`{".tsx", ".jsx"}` (`scripts/lint-liczby.mjs:18`), więc pliki `.json`
nie są w ogóle otwierane; a wzorzec `/>[^<>{}]*\d[^<>{}]*</`
(tamże :44) wymaga **cyfry** i wyklucza klamry, więc nawet w `.tsx`
każdy tekst renderowany przez `{t(...)}` jest poza zasięgiem, a
liczebnik słowny jest niewidoczny wszędzie.

Skutek dokumentacyjny: `content/karta-tonu.md:61` mówi „Każda liczba
pochodzi z content/facts.json — bez wyjątku **(bramka)**". Człon
w nawiasie jest wobec stanu faktycznego nieprawdziwy — reguła
obowiązuje, ale nikt jej nie pilnuje maszynowo. To nie jest usterka
lintera do naprawy przy okazji, tylko **fałszywy zielony**: bramka
świeci na dowód, którego nie wykonała.

Warunek zamknięcia rozstrzygnął właściciel 2026-08-15 — **osobne
zlecenie po Etapie E**, w trzech krokach i w tej kolejności:
(1) rozszerzenie skanu na warstwę `messages` (cyfry **i** liczebniki
słowne), (2) inwentarz 14 istniejących ciągów z rozstrzygnięciem
**per ciąg**: pokrycie w `facts.json` / redakcja / świadomy wyjątek
z adnotacją, (3) sprostowanie karty tonu pkt 5 dopiero **po** naprawie,
żeby zapis znów opisywał stan, a nie zamiar. Do tego czasu każda nowa
treść z liczbą przechodzi przez człowieka, bo bramka jej nie zobaczy.

**T10 — co dokładnie zostało zamrożone (w rozmowie: „O5").** Nie jest
to podejrzenie, tylko liczby z przebiegu 31957994362 (`26c38f2`,
bramka **zielona** 7/7). Kolumny: reprezentant · zapas do progu 1800 ms
· rozrzut LCP między pięcioma przebiegami tej samej trasy.

| Trasa | Reprezentant | Zapas | Rozrzut |
|---|---|---|---|
| `/` | 1533 ms | +267 | 328 ms |
| `/funkcje` | 1417 ms | +383 | 683 ms |
| `/dla-kogo` | 1511 ms | +289 | 965 ms |
| `/funkcje/pozyskiwanie` | 1475 ms | +325 | 462 ms |
| `/funkcje/tresci` | 1397 ms | +403 | **1441 ms** |
| `/funkcje/zespol` | 1561 ms | +239 | 651 ms |
| `/funkcje/wyniki` | 1542 ms | +258 | 597 ms |

Na **każdej** trasie rozrzut jest szerszy od zapasu, miejscami
kilkukrotnie. Znaczy to tyle, że o zieleni współdecyduje stan runnera:
`benchmarkIndex` tego samego pomiaru chodził 2026-08-16 od ~2161 rano
do ~3368 wieczorem, a przy `throttlingMethod: "simulate"` wolniejszy
runner mnoży zmierzoną pracę CPU. Reguła werdyktu tego **nie leczy**
i nigdy nie miała leczyć: ona wybiera reprezentanta po metryce, na
której stoi próg, więc odbiera wyskokowi prawo do wydania wyroku —
ale wyskok dalej się zdarza i dalej jest w danych widoczny.

Zamrożenie jest świadome i ma cenę zapisaną wprost: bramka wydajności
jest dziś **wskaźnikiem regresu, nie gwarancją progu**. Wyłapie zmianę,
która przesunie całą trasę, i przepuści taką, która mieści się
w hałasie. Warunek powrotu właściciela — „przerzuci się **na medianie**,
nie na wyskoku" — jest wobec tego dobrany do tego, czym bramka dziś
jest: dopóki reprezentant trzyma się pod progiem, hałas kosztuje
niepewność, nie fałszywy alarm. Dopiero czerwień na reprezentancie
znaczy, że mediana wyszła z budżetu, i wtedy trzeba rozstrzygnąć, czy
to regres kodu, czy pomiar wymaga wzmocnienia. Oczywisty lek
(więcej przebiegów) nie jest z góry wybrany: zwężałby rozrzut mediany,
ale wydłuża pomiar i nie usuwa przyczyny, którą jest zmienność runnera.

**„Najbliższe zlecenie Z" (poz. 17, 18, 19, 23, 24) = Z7**, spisane
2026-08-13: `docs/faza-4/zlecenie-Z7.md`. Do czasu odpowiedzi okna
aplikacji pozycje pozostają otwarte — zlecenie wysłane to nie jest
warunek spełniony.

Pozycje zamknięte (dla historii): „kto czeka na odpowiedź" (usunięta
na stałe — brak funkcji); „co z tego jest twoje" (zredukowana do „co
się sprzedało"); FAQ-faktura i trial rozstrzygnięte 2026-08-09 jako
milczenie warunkowe (wiersze 2–3).
